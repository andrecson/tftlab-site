# Ralph Progress Log
Started: Tue Jun 30 04:42:10 PDT 2026
---

## Codebase Patterns
- Stack: Next.js 15.1.7 (App Router), React 18.3.1, TypeScript (strict), Tailwind 3.4.17. Source lives under `src/` with `@/*` alias mapped to `./src/*` (tsconfig paths).
- Quality gates: `npm run typecheck` (tsc --noEmit) and `npm run lint` (next lint). Both must pass before staging. Scripts: dev, build, start, lint, typecheck.
- Browser verification: the Playwright MCP reaches the local dev server at `http://localhost:3000` (NOT host.docker.internal:3001 — that proxy/login flow from CLAUDE.md is not set up in this repo, and there is no auth until US-030). Start dev with `npm run dev` (background), confirm with `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` == 200, then `browser_navigate` to localhost:3000.
- Playwright screenshots: pass a RELATIVE `filename` (e.g. `foo.png`) — the default tries to mkdir `/workspace/screenshots` and fails with EACCES. Screenshots land in the repo root; treat them as throwaway evidence and `rm` them before staging (don't commit test artifacts).
- A favicon.ico 404 console error is expected on a fresh scaffold; ignore it.
- Lang is pt-BR (`<html lang="pt-BR">`); UI copy/content is Portuguese.
- Prisma (US-003): PINNED TO PRISMA 6 (6.19.3) — do NOT upgrade to 7. Prisma 7 removed `url = env("DATABASE_URL")` from the schema `datasource` (it requires `prisma.config.ts` + a driver adapter instead), which breaks the PRD's classic workflow (env-url datasource, `prisma-client-js`, `prisma migrate dev`, `prisma db seed`). Schema lives at `prisma/schema.prisma`. Import the DB via the singleton: `import { db } from "@/server/db"` (PrismaClient cached on globalThis in non-production). After ANY schema change run `./node_modules/.bin/prisma generate` (also wired as `postinstall`); the client generates into gitignored `node_modules/@prisma/client`. `DATABASE_URL` comes from `.env` (gitignored; copy from `.env.example`).
- Local Postgres for migrations (US-004): this env has NO system Postgres (no binaries, no docker, no sudo) and port 5432 is closed by default — but stories that say "prisma migrate dev/db seed" need a live DB. Spin up a throwaway one with `embedded-postgres` (prebuilt PG binary, runs as normal user) installed OUTSIDE the repo so it never touches project deps. Recipe: `cd /tmp/pgscratch && npm i embedded-postgres@17.10.0-beta.17`, then run `node node_modules/@embedded-postgres/linux-x64/scripts/hydrate-symlinks.js` (postinstall is blocked by allow-scripts, so the binary symlinks need manual hydration). Start script (`/tmp/pgscratch/start-pg.js`) is kept; it inits + starts PG on localhost:5432 (user/pass postgres/postgres, db `metacomps` — matches `.env` DATABASE_URL) and stays alive. GOTCHA: the env has no UTF-8 locale, so initdb fails unless you set `LANG=C LC_ALL=C` AND pass `initdbFlags:["--locale=C","--encoding=UTF8"]`. Launch backgrounded, wait for `PG_READY` in its log, then run `prisma migrate dev`. The DB lives in /tmp (gitignored territory) and dies with the session; only the generated `prisma/migrations/**` files are the durable deliverable — future iterations just re-spin PG and `prisma migrate deploy`/`migrate dev` replays them.
- Patch/SiteConfig (US-005): `Patch` (version @unique, set, releasedAt, createdAt) and the singleton `SiteConfig` (`id Int @id @default(1)`, currentSet, `currentPatchId String?` -> `currentPatch Patch?`, updatedAt) live in schema.prisma. Read/write the config with `db.siteConfig.upsert({ where: { id: 1 }, ... })` — there is exactly one row. `currentPatch` is OPTIONAL on purpose (config can exist before a patch). Later stories add MORE reverse relations onto `Patch`: `Comp.patchIntroduced`/`patchUpdated` (US-006) and `CompTierSnapshot.patch` (US-007) — when you introduce those child models, ALSO add their back-relation fields to `Patch` (Prisma requires both sides) or `prisma validate` fails.
- Comp model (US-006): `Comp` lives in schema.prisma with enums `Tier`(S,A,B,C,X), `CompStatus`(DRAFT,PUBLISHED,ARCHIVED), `Difficulty`(EASY,MEDIUM,HARD), `AugmentCategory`(ECON,ITEMS,COMBAT). Comp has TWO FKs to `Patch`, so they MUST be NAMED relations: `patchIntroduced @relation("PatchIntroduced")` (required) and `patchUpdated @relation("PatchUpdated")` (optional) — with the matching back-relations `Patch.compsIntroduced`/`compsUpdated`. `augmentPriority` is a Postgres enum array (`AugmentCategory[]`) that defaults to `[]` (no `@default` needed). Defaults: status=DRAFT, difficulty=MEDIUM, situational=false. `slug` is the public URL key (@unique). US-007 join tables (CompTrait/CompUnit/CompUnitItem/CompItemPriority/CompAugment/CompTierSnapshot) reference `Comp.id` with `onDelete: Cascade` and must add their back-relation fields onto `Comp` (and `CompTierSnapshot` adds another back-relation onto `Patch`).
- Comp join tables (US-007): six child models in schema.prisma — `CompTrait`(level,order), `CompUnit`(role UnitRole, isCarry, carryOrder?, starLevel?, boardRow?, boardCol?, order; relation `items CompUnitItem[]`), `CompUnitItem`(order), `CompItemPriority`(order), `CompAugment`(order), `CompTierSnapshot`(tier Tier) — plus `enum UnitRole {EARLY,CORE,FLEX}`. Read a comp's content via `db.comp.findUnique({ include: { traits, units: { include: { items } }, itemPriority, augments, tierSnapshots } })`. Nested `create` works for the whole tree in one write (see US-007 progress entry). KEY GOTCHA: `CompUnit @@unique([compId,boardRow,boardCol])` uses nullable board coords, and Postgres treats NULLs as DISTINCT in unique indexes, so many EARLY/FLEX units (boardRow/boardCol = null) coexist while on-board CORE units can't collide on a hex — exactly what we want; don't "fix" this by making the columns non-null. ALL FKs are `onDelete: Cascade` (Comp/CompUnit children cascade per AC; catalog/patch FKs also Cascade, matching ChampionTrait), so deleting a Comp wipes its whole subtree and deleting a CompUnit wipes its items. Other @@unique guards: CompTrait[compId,traitId], CompItemPriority[compId,itemId], CompAugment[compId,augmentId], CompTierSnapshot[compId,patchId].
- Catalog seed / Data Dragon (US-009): `src/server/ddragon.ts` `getCatalog()` pulls the FULL TFT catalog from **Community Dragon** (`raw.communitydragon.org/latest/cdragon/tft/en_us.json`), NOT classic ddragon — ddragon's TFT files have no trait breakpoints and no champion<->trait links (both needed by the synergy engine/builder). Current set = highest mainline mutator `TFTSet<number>` (-> `"TFTSet17"`), resolved at fetch (no hardcoded number). The returned `set` token is the canonical value for the `set` column everywhere; future stories stamp `getCatalog().set` into SiteConfig/Comp too. Icons: `.tex` asset paths -> CDN PNG via `assetUrl()` (`.../latest/game/<lowercased>.png`, verified HTTP 200); US-042 must whitelist `raw.communitydragon.org` in next.config images.remotePatterns. Seed runner is **tsx** (`prisma.seed` in package.json); run with **`npm run db:seed`**, NEVER `./node_modules/.bin/prisma db seed` (tsx isn't on PATH there -> `spawn tsx ENOENT`). IDEMPOTENCY: upsert every entity on `@@unique([apiId,set])` (`where:{apiId_set:{apiId,set}}`); for `ChampionTrait`, resolve the trait id DETERMINISTICALLY — champions reference traits by display NAME and some names map to many apiIds (Set 17 "Stargazer" = 8 variants), so pin each name to one canonical apiId (shortest apiId, then lexicographic = base trait) and look up by apiId, never by a name whose winner is decided under concurrent writes (that drifts 133->139 on re-seed). Prove idempotency by seeding twice and diffing the printed counts.
- Sample data seed (US-010): `prisma/seed.ts` runs `seedCatalog()` (US-009) THEN `seedSampleData(set, setNumber)` in one `prisma db seed`. It upserts a previous + current `Patch` (versions `${setNumber}.1`/`.2`) and the `SiteConfig` singleton (`id:1`, currentSet + currentPatchId=current), then builds 3 PUBLISHED demo comps in distinct tiers from REAL catalog rows queried back from the DB (NEVER hardcode apiIds — query champions/items[type=COMPLETED]/augments so it tracks whatever set was loaded). Comp idempotency has NO upsert key besides `slug`, so each comp is `db.comp.deleteMany({where:{slug}})` (cascades all children) then a single nested `db.comp.create({ data: { traits/units{items}/itemPriority/augments + connect } })`; slugs deduped per run. Badges follow US-011 logic — make one comp `patchIntroduced`=current (=> Novo) and one `patchIntroduced`=previous + `patchUpdated`=current (=> Atualizado). Use FIXED ISO dates for releasedAt/publishedAt so re-seeds don't drift. To inspect seeded data, drop a `_foo.ts` in the REPO ROOT, `import { db } from "./src/server/db"`, run with `./node_modules/.bin/tsx ./_foo.ts`, then `rm` it before staging.
- User model (US-008): `enum UserRole {ADMIN,EDITOR}` and `model User` (id cuid, `email @unique`, `name String?`, `role UserRole @default(EDITOR)`, `passwordHash String?`, `createdAt`) live in schema.prisma. User is STANDALONE — no relations yet. `passwordHash` is OPTIONAL on purpose so US-030 (Auth.js) can provision OAuth/credential users without a local password. Admin auth gates on `role` (ADMIN vs EDITOR); there is no public sign-up.
- Unit tests (US-011): the repo uses **Node's built-in test runner via tsx** (no vitest/jest). Run all tests with `npm test` (= `tsx --test "src/**/*.test.ts"`). GOTCHA: keep the glob **quoted** in the script — Node 24 expands `src/**/*.test.ts` itself; an unquoted glob relies on shell globstar and can miss files. Write tests with `import { test } from "node:test"` + `import assert from "node:assert/strict"`, co-located next to the unit under test as `<name>.test.ts` (e.g. `src/server/services/badges.ts` + `badges.test.ts`). These `*.test.ts` files are typechecked by `tsc` and linted by `next lint` too, so they must pass all three gates. Pure-function stories (US-024 synergy engine, US-028 board-code) should follow this same pattern.
- Badges service (US-011): `src/server/services/badges.ts` `getCompBadges(comp, currentPatchId)` returns `{ isNew, isUpdated }`. Rule: `isNew = patchIntroducedId === currentPatchId`; `isUpdated = patchUpdatedId === currentPatchId && !isNew` (**Novo wins** when both). It early-returns both-false when `currentPatchId` is null/undefined (SiteConfig.currentPatchId is nullable), which also dodges a `null===null` false-positive for comps with no `patchUpdatedId`. UI stories (US-015 CompCard, US-017 comp header) should call this — pass `SiteConfig.currentPatchId` as the second arg. Input type `BadgeComp` is structural, so a full Prisma `Comp` row works directly.
- Public read queries (US-012): live in `src/server/queries/` — `config.ts` (`getSiteConfig()`, `getCurrentSet()`), `tierlist.ts` (`getPublishedComps(set?)`, `groupCompsByTier`, `TIER_ORDER`), `comp.ts` (`getCompBySlug(slug)`). RULES for any future public query: (1) ALWAYS filter `status: "PUBLISHED"` — drafts/archived must never reach public pages; (2) `set` defaults to `SiteConfig.currentSet` via `getCurrentSet()` (returns `[]`/`null` if unconfigured); (3) build include shapes with `Prisma.validator<Prisma.CompInclude>()({...})` and export the row type via `Prisma.CompGetPayload<{ include: typeof theInclude }>` (`CompCard`, `CompDetail`) — a plain object / `satisfies` widens `orderBy: "asc"` to `string` and breaks `CompGetPayload`; (4) `getCompBySlug` uses `findFirst` (slug+status), NOT `findUnique` (findUnique's `where` only accepts unique fields), and returns `null` for missing/draft/archived so the page 404s. `CompCard` = main traits + carries-only (`units: { where: { isCarry: true } }`); comp scalars incl. `patchIntroducedId`/`patchUpdatedId` are always present, so pass a card/detail straight into `getCompBadges(comp, SiteConfig.currentPatchId)`. `CompDetail` = full tree (traits, units{champion,items{item}}, itemPriority{item}, augments{augment}, both patch relations) all child lists ordered by `order`; board pos = `boardRow`/`boardCol` scalars (null = off-board).
- ISR + tag revalidation (US-014): public pages that read DB data are kept **statically rendered with ISR** by (a) `export const revalidate = <seconds>` on the page and (b) wrapping the data fetch in `unstable_cache(fn, [keyPart], { tags: ["<tag>"] })` from `next/cache`. The tier list at `/` uses tag **`tierlist`** — US-038's publish/unpublish Server Actions will call `revalidateTag("tierlist")` to refresh it on demand; comp-detail pages (US-017) should use tag `comp:<slug>`. DO NOT touch request-time APIs (cookies/headers/searchParams) in a page you want static, or it silently becomes dynamic (the Next dev overlay shows "Static route" vs "Dynamic route" — check it in the browser). Tailwind GOTCHA on tier colors: dynamic class names like `bg-tier-${tier}` are PURGED — write the full class strings (`bg-tier-s`, `border-l-tier-s`, …) literally in a lookup map so the content scanner keeps them. Tier chips use dark `text-background` for contrast on the bright tier colors.
- Public layout (US-013): every public page renders inside the **`src/app/(public)/`** route group (parens = no URL segment, so `(public)/page.tsx` still serves `/`). Its `layout.tsx` is an **async server component** that calls `getSiteConfig()` and wraps children with `<SiteHeader>`/`<SiteFooter>` (from `src/components/`) in a `min-h-screen flex-col` shell (footer sticks to bottom via `flex-1` on `<main>`). NEW public pages (US-014 tier list at `/`, US-017 `/comps/[slug]`, US-025 `/builder`) go UNDER `(public)/` so they inherit the header/footer — do NOT put them at `src/app/*` top level. The root `src/app/layout.tsx` still owns `<html lang="pt-BR">`/`<body>`; don't duplicate those in the group layout. Header/footer are presentational (props in, no `"use client"`); `SiteHeader` takes `patchVersion`/`updatedAt` and is null-safe. GOTCHA: moving/renaming/deleting a `page.tsx` leaves a STALE `.next/types/app/**` that makes `tsc` fail (`Cannot find module '.../page.js'`) — `rm -rf .next` then re-run `npm run typecheck` (dev/build regenerates the types). Format dates with UTC parts (not `toLocaleDateString`) to avoid server/client timezone drift.
- Theme (US-002): shadcn-style HSL tokens live in `src/app/globals.css` `:root` (space-separated `H S% L%`, consumed via `hsl(var(--token))` in tailwind.config.ts). Use semantic Tailwind classes — `bg-background`, `text-foreground`, `text-muted-foreground`, `bg-card`, `border-border`, `bg-primary`, `bg-secondary`, `bg-accent`, `bg-destructive` — NOT raw `text-gray-*`. Tier colors are `bg-tier-{s,a,b,c,x}` / `text-tier-*` / `border-tier-*`. Brand gradient is `bg-brand-gradient` (purple #9333EA -> cyan #5EEAD4); for gradient text use `bg-brand-gradient bg-clip-text text-transparent`. A `@layer base` already applies `bg-background text-foreground` to `body`, so pages don't need to re-set it.
- Catalog icons in the UI / next/image (US-015): champion/trait/item/augment `iconUrl`s are Community Dragon CDN URLs (`raw.communitydragon.org`, from ddragon.ts) and are NON-NULL strings in the schema. Render them with **`next/image`**, NOT `<img>` — `next/core-web-vitals` treats `@next/next/no-img-element` as a WARNING and the quality gate requires ZERO warnings, so a plain `<img>` fails lint. `next/image` needs the host whitelisted: `next.config.mjs` `images.remotePatterns` already includes `{ protocol: "https", hostname: "raw.communitydragon.org" }` (added in US-015) — without it next/image throws "hostname is not configured" at runtime. Use `fill` + a `relative` sized parent + `sizes` for square icons, or explicit `width`/`height` for fixed ones. US-042 layers lazy-load/Lighthouse/breakpoint tuning on top of this baseline.
- Presentational components (US-013/US-015): shared UI lives in `src/components/*` as plain server components (props in, NO `"use client"` unless interactivity is needed). `CompCard` (`src/components/comp-card.tsx`) takes `{ comp: CompCard, currentPatchId }`, derives badges via `getCompBadges` (never re-implement badge logic inline), and the WHOLE card is a `<Link href={`/comps/${slug}`} aria-label={`Ver comp ${name}`}>` so it's one keyboard-focusable target named by the comp. Tier-colored borders use the same full-class no-interpolation map as the page (`border-l-tier-{s,a,b,c,x}`). Import the row type from the query module (`import type { CompCard as CompCardData } from "@/server/queries/tierlist"`) — alias it to avoid colliding with the component name.
- Comp-detail page (US-017): the comp route is `src/app/(public)/comps/[slug]/page.tsx` (under the `(public)` group so it inherits header/footer). It is the SHARED page that US-018..US-022 extend — each of those stories renders its section (carries/item-priority, early/core/flex units, board, augments, guide) BELOW `<CompHeader/>` in this same file, reusing `getCompBySlug(slug)`'s `CompDetail` (already includes the full tree: units{champion,items{item}}, itemPriority{item}, augments{augment}, traits{trait}, both patch rels). Static+ISR pattern: `export const revalidate = 3600` + `export async function generateStaticParams()` (from `getPublishedCompSlugs()` in queries/comp.ts — all PUBLISHED slugs, not set-scoped) + data wrapped in a PER-SLUG `unstable_cache(fn, ["comp-detail", slug], { tags: [`comp:${slug}`, "tierlist"] })` created inside the component (tag is dynamic per slug). The `tierlist` tag is added alongside `comp:<slug>` so a patch change (US-039 revalidates `tierlist`) refreshes the badges here too; US-038's publish action revalidates `comp:<slug>`. NEXT 15 GOTCHA: the page's `params` is a `Promise<{slug:string}>` — `await` it. 404: `getCompBySlug` returns null for missing/draft/archived (it filters `status:PUBLISHED`) → call `notFound()` from `next/navigation` (it returns `never`, so TS narrows the comp to non-null after the guard). Header lives in presentational `src/components/comp-header.tsx` (props `{comp: CompDetail, currentPatchId}`): reuse `TIER_META[tier].chipClass` for the tier chip (dark `text-background`), `getCompBadges` for Novo/Atualizado (NEVER re-derive), and a local `DIFFICULTY_LABEL` map (EASY→Fácil, MEDIUM→Médio, HARD→Difícil) for the enum. `situational` is a SEPARATE boolean from `tier==="X"` — only append a "Situacional" suffix when `situational && tier !== "X"` or you get "Situacional · Situacional". BROWSER GOTCHA: `rm -rf .next` kills any dev server already running (its build artifacts vanish → 404/500); a stale prior server may still hold :3000 so a fresh `npm run dev` lands on :3001 — point Playwright at whatever port the log prints.
- Reusable icon tooltip (US-018): `src/components/icon-tooltip.tsx` `<IconTooltip src name size?=40 className?/>` is the shared way to show a catalog icon (item/champion/augment) with its name in a tooltip. It's a pure CSS tooltip (server component, no `"use client"`) that appears on hover AND keyboard-focus/touch: the wrapper `<span>` is `tabIndex={0} role="img" aria-label={name}` (so it's focusable and screen-reader-named), the inner `next/image` is decorative (`alt=""`), and a sibling tooltip `<span aria-hidden>` toggles opacity via `group-hover/tt:opacity-100 group-focus/tt:opacity-100` (named group `group/tt`). Native `title` was avoided (doesn't show on touch, and would double up with the CSS tooltip). Tooltip uses `bg-card text-card-foreground ring-1 ring-border` — NOT `bg-popover`, because `--popover` == `--background` (both `224 71% 4%`) so a popover tooltip is invisible on the dark page; `--card` (`222 47% 11%`) reads as elevated. Reuse this for ANY icon that needs its name on hover (US-021 augments, builder palettes) instead of re-implementing tooltips or using `<img title>`.
- Comp-detail sections (US-018): the comp page (`src/app/(public)/comps/[slug]/page.tsx`) renders each US-018..US-022 section as its own presentational component BELOW `<CompHeader/>`, inside a `<div className="mt-8 flex flex-col gap-8">`. US-018 added `src/components/comp-carries.tsx` exporting `<CompCarries units={comp.units}/>` (carries = `units.filter(isCarry)` sorted by `carryOrder` nulls-last then `order`; each shows champion icon+name + `★`×`starLevel` and its items in query order) and `<CompItemPriority items={comp.itemPriority}/>` (an `<ol>` of item icons with an aria-hidden right-chevron SVG BETWEEN items = the 'setas' order indicator). Section components take the NARROW `CompDetail["units"]`/`["itemPriority"]` slice, not the whole `comp` (only `CompHeader` needs the whole comp). Child lists are ALREADY ordered by the query (`orderBy: { order: "asc" }`) — don't re-sort item lists; only carries need a `carryOrder` sort since `units` is ordered by `order`. Every section should `return null` when its data is empty so the page degrades gracefully.
- Champion cost colors (US-019): TFT champion `cost` (1-7) is color-coded by convention (1 gray, 2 green, 3 blue, 4 purple, 5 gold). Render it via a FULL-CLASS lookup map (never interpolate — same Tailwind purge rule as tier colors): `src/components/comp-units.tsx` `COST_STYLE` uses default-palette classes `border-slate-400/70 text-slate-200` (1), `border-emerald-400/70 text-emerald-300` (2), `border-sky-400/70 text-sky-300` (3), `border-fuchsia-400/70 text-fuchsia-300` (4), `border-amber-400/70 text-amber-300` (5), with amber as the fallback for cost>5. If US-020 (board), US-025/US-026 (builder palette) need cost coloring, reuse this map/pattern (consider lifting it to `src/lib/*` if a client component needs it, since `comp-units.tsx` is a server component).
- Comp unit lists (US-019): `src/components/comp-units.tsx` `<CompUnits units={comp.units}/>` groups the comp's units by `role` into the AC's three fixed sections — 'Early Units' (EARLY), 'Core/Final' (CORE), 'Flex Units' (FLEX), in that order via a `ROLE_SECTIONS` map. It takes the narrow `CompDetail["units"]` slice (like the other comp-detail sections), preserves the query's `order` (units are already `orderBy order asc` — don't re-sort), omits empty role sections, and returns null when there are no units. Each `<UnitCard>` = `<IconTooltip size=56>` + name (`line-clamp-2 text-xs` centered) + a cost badge (◆ + cost, color-coded via COST_STYLE, `aria-label="Custo N"`) + `★`×`starLevel` (text-primary, `aria-label="N estrelas"`, only when starLevel set). Cards are `w-16 sm:w-20` in a `flex-wrap` ul so they reflow on mobile without h-scroll. Rendered below `<CompItemPriority/>` in the shared comp-detail page. Note the section labels are intentionally English TFT terms per the AC (the rest of the UI copy is pt-BR); the section wrapper heading is 'Unidades'.
- Hex board / percentage layout (US-020): `src/components/comp-board.tsx` `<CompBoard units={comp.units}/>` renders the comp's on-board CORE units on a 4×7 honeycomb. It's a `"use client"` component (the 'Mostrar nomes' toggle uses `useState`) — a client component with only `useState` does NOT make the page dynamic, so the comp route stays STATIC (unlike `useSearchParams`, which needs Suspense; check the dev overlay's "Static route"). Board indexing is 0-based (`boardRow` 0-3, `boardCol` 0-6) — matches the seed's `CORE_HEXES`; off-board units (null coords) are excluded via `byHex.get(`${row}-${col}`)`; component returns null when nothing is on-board. RESPONSIVE-NO-HSCROLL recipe (reuse for the US-025 BuilderBoard): each hex cell is `w-[13.333%]` (=100/7.5) `shrink-0 aspect-square`, odd rows get `pl-[6.667%]` (half a hex) for the offset, rows nest with inline `marginTop:-2.5%`; 7×13.333%+6.667% = 100% exactly, so it fits any container with zero horizontal scroll (verified at 360px). Hexes are drawn with a pointy-top `clipPath` polygon on LAYERED absolute divs (tile/ring layer `inset-[2px]` + image layer `inset-[3px]`, both clipped) so a colored ring frames a `next/image fill` icon — a clip-path clips box-shadow/`ring-*` too, so use an inset image layer for the "border", not a ring. Carry highlight = extra `bg-brand-gradient` glow hex + gradient ring + thicker image inset (accent/brand-gradient per AC). `import type { CompDetail }` into a client component is safe (types erased); passing the units array (with Date fields) as props from the server page serializes fine.
- Guide text + name highlighting (US-022): `src/components/comp-guide.tsx` `<CompGuide comp={comp}/>` is the comp-detail guide — 'Quando jogar' (single `whenToPlay` block) + 'Como jogar' (three fixed `<h3>` subsections 'Início de jogo'/'Meio jogo'/'Fim de jogo' = `earlyGame`/`midGame`/`lateGame`, NO 'Stage 2/3/4' labels) + optional 'Dicas' (`tips`). It's the ONE comp-detail section that takes the WHOLE `CompDetail` (not a narrow slice) because it needs both the guide scalars and the unit/item names. REUSABLE HIGHLIGHT RECIPE for wrapping catalog names inside free prose: collect the names (champions from `units`, items from `units.items` + `itemPriority`) into a Set, build a case-insensitive `RegExp` of the escaped names sorted LONGEST-FIRST (so 'Aurelion Sol' beats 'Sol'), fence each alternative with letter/number lookbehind+lookahead (`u` flag, `(?<![\p{L}\p{N}])...(?![\p{L}\p{N}])`) so a name isn't matched inside a larger word, then split the string and wrap each hit in `<mark className="bg-primary/15 text-primary">`. Do this in a SERVER component — Node/SSR supports `\p{L}` + lookbehind and it stays out of the client bundle. Optional guide fields (`string | null`) narrow cleanly with `PHASES.flatMap(p => (t = comp[p.key]?.trim()) ? [{...p, text: t}] : [])`; `GuideText` splits prose on `\n+` into `<p>` paragraphs for multi-line admin input.
- Client-side filtering of a static/ISR list (US-016): to add filters to a STATIC page WITHOUT making it dynamic, filter CLIENT-SIDE from the URL. The server page fetches ALL rows (cached, static) and renders `<Suspense fallback={<Bands.../>}><ClientFilters .../></Suspense>`. The `<Suspense>` boundary is MANDATORY around any client component that calls `useSearchParams` — without it the build errors ("should be wrapped in a suspense boundary") and/or the page bails to dynamic; WITH it the route stays "Static route" (check the dev overlay). Filter state lives ONLY in the URL: repeatable params for multi-select (`searchParams.getAll("tier")`, toggled by delete+append) + a `q` string for text; write via `router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false })` (shareable links + back-button for free; the fallback prerenders the unfiltered list so static HTML/SEO still has content). Combine categories with AND, matches within a category with OR. GOTCHA: NEVER import `@/server/queries/*` or `@/server/db` into a `"use client"` component — they pull the PrismaClient into the browser bundle; only `import type` from them is safe (types are erased). Put client-safe pure helpers/presentation in `src/lib/*` (e.g. `src/lib/tiers.ts` = `TIER_ORDER`, `groupByTier<T extends {tier:Tier}>`, `TIER_META`; its `import type { Tier }` is erased so no runtime Prisma dep) and have the server query module re-export/delegate to it so there's ONE source. Shared presentational components with NO server-only imports (e.g. `src/components/tier-bands.tsx`, which only renders markup + `CompCard`) render fine in BOTH the server fallback and the client tree. If a control block is rendered TWICE (desktop inline panel + mobile drawer, so both read/write the same URL state and never diverge), don't give its inputs a fixed `id` (that duplicates ids in the DOM) — use a wrapping `<label>` + `aria-label`. Mobile filters open in a bottom-sheet drawer (`role="dialog" aria-modal`, Escape closes, a "Ver resultados (n)" button closes it); toggle desktop-vs-drawer with `hidden sm:block` / `sm:hidden`. The server page fetches ALL rows (cached, static) and renders `<Suspense fallback={<Bands.../>}><ClientFilters .../></Suspense>`. The `<Suspense>` boundary is MANDATORY around any client component that calls `useSearchParams` — without it the build errors ("should be wrapped in a suspense boundary") and/or the page bails to dynamic; WITH it the route stays "Static route" (check the dev overlay). Filter state lives ONLY in the URL: repeatable params for multi-select (`searchParams.getAll("tier")`, toggled by delete+append) + a `q` string for text; write via `router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false })` (shareable links + back-button for free; the fallback prerenders the unfiltered list so static HTML/SEO still has content). Combine categories with AND, matches within a category with OR. GOTCHA: NEVER import `@/server/queries/*` or `@/server/db` into a `"use client"` component — they pull the PrismaClient into the browser bundle; only `import type` from them is safe (types are erased). Put client-safe pure helpers/presentation in `src/lib/*` (e.g. `src/lib/tiers.ts` = `TIER_ORDER`, `groupByTier<T extends {tier:Tier}>`, `TIER_META`; its `import type { Tier }` is erased so no runtime Prisma dep) and have the server query module re-export/delegate to it so there's ONE source. Shared presentational components with NO server-only imports (e.g. `src/components/tier-bands.tsx`, which only renders markup + `CompCard`) render fine in BOTH the server fallback and the client tree. If a control block is rendered TWICE (desktop inline panel + mobile drawer, so both read/write the same URL state and never diverge), don't give its inputs a fixed `id` (that duplicates ids in the DOM) — use a wrapping `<label>` + `aria-label`. Mobile filters open in a bottom-sheet drawer (`role="dialog" aria-modal`, Escape closes, a "Ver resultados (n)" button closes it); toggle desktop-vs-drawer with `hidden sm:block` / `sm:hidden`.
- Admin tier-list editor backend (US-046): the `/admin/tier-list` screen (US-047) reads `getAdminTierListComps()` (in `src/server/queries/admin.ts`) → `TierGroup<AdminTierListComp>[]` — the current set's comps of ALL statuses (lean `select` `id/name/slug/tier/status`), grouped via the shared `groupByTier` from `@/lib/tiers` (NOT a new grouper). It lives in `admin.ts` (all-statuses, uncached, set-scoped) NOT the public `tierlist.ts` (PUBLISHED-only, cached) — when an AC says "reuse tierlist.ts" but the clean home is elsewhere, reuse the shared `@/lib/tiers` helper and document the divergence. Retiering goes through `setCompTier(compId, tier)` in `src/actions/comps.ts`: `requireRole("EDITOR")` → validate with the new pure `isTier()` guard (in `@/lib/tiers`; accepts only S/A/B/C/X) → update ONLY `Comp.tier` → when PUBLISHED, `revalidateTag("tierlist")` + `revalidateTag("comp:"+slug)` (same as `updateComp`). Returns `CompActionResult` (`{ok:false,error}` for invalid tier / missing comp). Any server action taking an enum from the client must runtime-validate it even though the TS param is the enum (a `"use server"` fn is a public POST) — `isTier` is the reusable `Tier` guard.
- SEO / metadata (US-023): site-wide SEO constants live in `src/lib/site.ts` (client-safe, NO server imports): `SITE_URL` (from `NEXT_PUBLIC_SITE_URL`, default `https://metacomps.gg`, trailing slash stripped), `SITE_NAME`, `SITE_DESCRIPTION`, `SITE_LOCALE` (`pt_BR`), `absoluteUrl(path)`. The root `src/app/layout.tsx` `metadata` sets `metadataBase = new URL(SITE_URL)` (REQUIRED so every relative `alternates.canonical` / `openGraph.url` resolves to an absolute URL) + a title `template` `%s · MetaComps` + site-wide `openGraph`/`twitter` defaults that all pages inherit and can override. Per-page SEO uses `generateMetadata` (comp route) or a static `export const metadata` (homepage sets `alternates:{canonical:'/'}`). `app/sitemap.ts` + `app/robots.ts` are Next file conventions (default-export a function returning `MetadataRoute.Sitemap`/`MetadataRoute.Robots`); the sitemap lists ONLY PUBLISHED comps (reuse a status:PUBLISHED query — `getPublishedCompsForSitemap()`). Dynamic per-comp OG images use `opengraph-image.tsx` (`next/og` `ImageResponse`, 1200x630) — satori supports FLEXBOX-ONLY inline styles (no Tailwind/className, no CSS grid, no CSS vars → hardcode hex, e.g. tier colors mirror the globals.css tier tokens; every div with >1 child needs `display:flex`); `twitter-image.tsx` just re-exports `{ default, alt, size, contentType } from './opengraph-image'` so `twitter:image` is populated with the same image. File-based `opengraph-image`/`twitter-image` auto-populate `og:image`/`twitter:image` — don't set `openGraph.images` in `generateMetadata` or you override them. JSON-LD = a `<script type="application/ld+json" dangerouslySetInnerHTML={{__html: JSON.stringify(obj)}} />` in the page body. next/og default runtime is nodejs, so Prisma reads work in the image route.
- `unstable_cache` serializes its result to JSON (US-023 gotcha): `Date` fields come back as STRINGS on cache HITS (real `Date`s only on the first cache-MISS call), even though the Prisma-derived TS type still says `Date`. Calling `.toISOString()` (or any Date method) directly on such a field 500s on the 2nd request. Normalize with `new Date(value).toISOString()` (works for both `Date` and ISO string). This affects ANY value read through `getCompData`/`getTierListData` etc. — the existing UI never hit it because it doesn't call Date methods on cached data, but metadata/JSON-LD/sitemap-style code does.
- Builder (US-025): the public builder lives at `src/app/(public)/builder/page.tsx` (server RSC, `revalidate=3600`, champion list from `getBuilderChampions()` wrapped in `unstable_cache` tag `catalog`, canonical `/builder`) which ships `BuilderChampion[]` to the `"use client"` `src/components/builder/builder.tsx`. That top component owns ALL board state; `champion-palette.tsx` (own search+sort state) and `builder-board.tsx` (4x7 honeycomb) are its children. STATE MODEL to reuse in US-026/027/028: board = `PlacedUnit[]` (`{id,championId,row,col}` from `src/lib/builder.ts`; `id = crypto.randomUUID()` generated ONLY in event handlers, never in render, so SSR is safe); UNDO/REDO = a snapshot stack `history: PlacedUnit[][]` + `cursor`, `commit(next)` does `history.slice(0,cursor+1).concat([next])` (truncates the redo branch) — undo/redo just move the cursor; disable the buttons at `cursor===0` / `cursor===len-1`. INTERACTION: an "armed" palette champion (click-to-toggle, `aria-pressed`) places on the next hex click; with nothing armed, a hex click SELECTS a placed unit, and a second hex click MOVES it (swaps if occupied); a selected unit is removed via the toolbar button or Delete/Backspace; Escape disarms/deselects. Both click AND HTML5 drag/drop are wired — palette tiles + placed hexes are `draggable` and set `text/plain` = `CHAMPION_DND_PREFIX|UNIT_DND_PREFIX + id` (both in `src/lib/builder.ts`); hexes `preventDefault` on dragover and route the drop by prefix. The board REUSES the US-020 comp-board percentage geometry (`w-[13.333%]`, odd-row `pl-[6.667%]`, `marginTop:-2.5%`, `HEX_CLIP`) so it never h-scrolls (verified 1280 & 360). GOTCHA when browser-testing: firing two `.click()`s in ONE `browser_evaluate` won't chain (React hasn't re-rendered between them, so the 2nd handler sees stale state) — arm and place in SEPARATE evaluate/click calls. TFT champion cost coloring is now `costClass()` in the client-safe `src/lib/champion-cost.ts` (lifted out of `comp-units.tsx`, which now imports it) — reuse that single source for any cost badge (US-026/027 builder tooltips/panels).
- Synergy engine (US-024): `src/lib/synergy.ts` `computeSynergies(units, traits)` is a PURE, import-free, client-safe module (like `src/lib/tiers.ts` / `src/lib/site.ts`) so the `"use client"` builder (US-025/026 SynergyPanel) can call it directly — NEVER put engine logic behind a server-only import. Inputs: `SynergyUnit { championId, traits: string[] }` (board position is irrelevant to synergy counting, so it's intentionally NOT part of the input) + `TraitInfo { key, name, breakpoints: number[] }`. It's KEY-AGNOSTIC: `SynergyUnit.traits` and `TraitInfo.key` just need the SAME string key — the catalog links champions↔traits by NAME (`CatalogChampion.traitNames` ↔ `CatalogTrait.name`) while the DB uses ids (`ChampionTrait`); the caller picks one key space and the engine works with either. UNIQUE COUNTING rule (TFT): `countUnitsByTrait` dedupes placed units by `championId` (a 2nd copy of a champion adds nothing) and collapses a trait repeated on one champion via `new Set(unit.traits)` — reuse this helper, don't re-count traits inline. Breakpoints are normalized defensively (sort asc + de-dupe + drop non-positive); a trait activates once unique count ≥ `breakpoints[0]`; `ActiveTrait` returns `count` (the displayed number, e.g. "Mecha 4"), `activeBreakpoint` (threshold reached), `tier` (1-based style tier), `nextBreakpoint` (null when maxed), `maxed`. `[]`-breakpoint meta-traits never activate; traits on units but absent from the catalog are ignored. Output is pre-sorted for a panel: tier desc, count desc, name asc. When US-025/026 wires this up, map each placed champion to `{ championId: <its apiId/id>, traits: <its trait names/ids> }` and pass the current set's `CatalogTrait`s (or DB `Trait` rows with `breakpoints` cast from `Json`) as `TraitInfo[]`.
- Builder stars / team value / synergy panel (US-026): `PlacedUnit` (src/lib/builder.ts) now carries a REQUIRED `stars: number` (1–3) — every place site must set it (`placeChampion` → `stars:1`), and move/swap must preserve it (spread `{...u}`). Star edits go through the SAME `commit()` snapshot-history so they're undoable; the star toolbar control (`role=group aria-label="Nível de estrela"`, 1★/2★/3★ `aria-pressed`) only renders when a unit is selected. Two client-safe pure helpers live in builder.ts: `clampStars(n)` (1–3, rounds, NaN→1) and `teamGoldValue(units, costOf)` = Σ `cost × 3^(stars-1)` (TFT combine math: 2★ worth 3 copies, 3★ worth 9). The live synergy panel maps each placed unit → `SynergyUnit` via `championsById.get(id).traits.map(t => t.id)` and each `BuilderTraitInfo` → `TraitInfo` as `{ key: t.id, ... }` (trait id is the shared key), then calls `computeSynergies` in a `useMemo` keyed on `units` so it updates on every place/move/remove — STARS DO NOT change synergy counts (unique-champion counting). Trait breakpoints come from the new `getBuilderTraits(set?)` server query (see src/server/CLAUDE.md). Presentational panel: `src/components/builder/synergy-panel.tsx`. When US-028 encodes the board, include `stars` (already on `PlacedUnit`) beside championId/row/col.
- Builder items + augments (US-027): `PlacedUnit` (src/lib/builder.ts) now also carries `items: string[]` (equipped item ids, slot order, up to `MAX_ITEMS=3`); board augments are a SEPARATE board-level `string[]` in `builder.tsx` (comp-wide, NOT per-unit and NOT part of the unit undo/redo history). Pure client-safe helpers in builder.ts: `addUnitItem(items,id)` (appends, returns unchanged COPY when at `MAX_ITEMS` so callers can length-compare to skip a no-op snapshot), `removeUnitItemAt(items,i)`, `toggleAugment(augs,id)` (add/remove, caps at `MAX_AUGMENTS=3`) — all covered by builder.test.ts. Item EDITS go through `commit()` (undo/redo-able); AUGMENT selection uses plain `useState` (ephemeral, not historied). Components in `src/components/builder/`: `<ItemPanel>` renders only when a unit is selected — 5 tabs by `ItemType` (Craftáveis=COMPLETED / Radiantes=RADIANT / Artefatos=ARTIFACT / Componentes=COMPONENT / Outros=EMBLEM|SUPPORT|OTHER) + text search (tab AND search), grid disables when the unit is full; `<AugmentPicker>` is board-level (n/3, chosen slots removable, grid `aria-pressed`, remaining disabled at 3). Items are click-to-equip AND draggable (`ITEM_DND_PREFIX`) — `builder-board.tsx` routes an item drop onto a hex to `onDropItem` (equip on that hex's unit) and overlays up to 3 item icons on each unit's hex. DATA: `getBuilderItems(set?)`/`getBuilderAugments(set?)` in `src/server/queries/catalog.ts` (plain mapped objects, cache-safe) — the builder page bundles all FOUR ({champions,traits,items,augments}) under one `unstable_cache(['builder-catalog'],{tags:['catalog']})`. ALL four catalog queries key by the same `id`: champion/trait ids feed synergy; item ids live in `PlacedUnit.items`; augment ids in the board augment selection — US-028's share code must encode BOTH `items` and the augment selection alongside championId/row/col/stars.
- Open a comp in the builder (US-029): `src/components/open-in-builder.tsx` `<OpenInBuilder comp={comp}/>` is a plain SERVER component (no `"use client"`) that turns a `CompDetail` into a builder share code and renders a `next/link` `<Link href={`/builder/${code}`}>`. KEY: `encodeBoard` (`src/lib/board-code.ts`) is PURE + client-safe and its base64url codec uses `btoa`/`TextEncoder` which are Node globals too, so the code is generated at RENDER time on the server — no client JS, no round-trip. `boardFromComp` maps `comp.units` filtered to ON-BOARD units (`boardRow !== null && boardCol !== null` = the CORE units; EARLY/FLEX are off-board with null coords) → `SharedUnit {championId, row:boardRow, col:boardCol, stars: starLevel ?? 1, items: items.map(i=>i.itemId)}`, and `comp.augments` → `augmentIds`. ID SPACE ALIGNS: `CompUnit.championId`/`CompUnitItem.itemId`/`CompAugment.augmentId` are the DB `Champion.id`/`Item.id`/`Augment.id`, i.e. the SAME keys `getBuilder{Champions,Items,Augments}` put on the builder catalog — so `/builder/[code]`'s sanitization keeps them all (no phantom drops). Reuse this "encode on the server, render a Link" trick anywhere you need to seed the ephemeral builder from server data.
- Builder share code + URL mirroring (US-028): the board is shared by encoding its WHOLE state into a URL-safe code in `src/lib/board-code.ts` (pure, client-safe like `tiers.ts`/`synergy.ts` — only imports types/helpers from `builder.ts`). `encodeBoard({units: Omit<PlacedUnit,"id">[], augments})` → compact versioned JSON `{v:1,u:[[championId,row,col,stars,items[]]],a:[augmentIds]}` → `JSON.stringify` → UTF-8 (`TextEncoder`) → `btoa` → base64url (`+/`→`-_`, strip `=`); `decodeBoard(code)` reverses and is DEFENSIVE (returns `null` on malformed/non-JSON/wrong-version input; drops out-of-bounds hexes; `clampStars`; dedupes hexes last-wins; caps items/augments) so a crafted code can't crash or place phantom/overlapping units. `btoa/atob/TextEncoder/TextDecoder` are globals in BOTH the browser and Node (tsx test), so the codec is portable and unit-testable. The ephemeral local `PlacedUnit.id` is NOT encoded; on reload, `toPlacedUnits()` assigns DETERMINISTIC ids `u0,u1,…` (index-based) — SSR-safe, unlike `crypto.randomUUID()` which must never run in render (differs server vs client → hydration mismatch). The `/builder/[code]` route (dynamic, server-rendered on demand — NO generateStaticParams; keep `/builder` itself Static) decodes the param and SANITIZES the decoded units/items/augments against the current catalog (drop unknown ids) before seeding `<Builder initialUnits/initialAugments>`; set `robots:{index:false,follow:true}` + `canonical:"/builder"` (codes are infinite/ephemeral). To MIRROR state into the URL without going dynamic or triggering a Next navigation, use `window.history.replaceState(window.history.state, "", path)` in a `useEffect` (NOT `router.replace` — that re-renders/refetches; passing the existing `history.state` preserves Next's router state) — the page stays "Static route". "Copiar link" = `navigator.clipboard.writeText(origin+path)` in a `try/catch` (clipboard rejects in insecure contexts / when denied — the address bar already reflects the board as a fallback). The builder catalog `unstable_cache` bundle moved to `src/server/queries/builder-catalog.ts` (`getBuilderCatalog()`) so `/builder` and `/builder/[code]` share one cache entry — reuse it, don't re-wrap the four queries.
- Admin auth / Auth.js (US-030): auth is **NextAuth v5** (`next-auth@beta`) with a **Credentials** provider (no OAuth) + JWT sessions. SPLIT-CONFIG is mandatory because `src/middleware.ts` runs on the **Edge** runtime and must NOT bundle Prisma/bcrypt: `src/auth.config.ts` is the edge-safe base (`pages.signIn:"/admin/login"`, `session.strategy:"jwt"`, `jwt`/`session` callbacks that copy `user.id`/`user.role` onto token→session, `providers:[]`); `src/auth.ts` spreads it and adds the `Credentials` provider (`authorize` = `db.user.findUnique` + `bcrypt.compare(passwordHash)`) and exports `{ handlers, auth, signIn, signOut }` + **`requireRole(minRole="EDITOR")`** (server-only guard: `auth()` → `redirect("/admin/login")` if unauth, `redirect("/admin/login?error=forbidden")` if role rank < required; ADMIN=2 > EDITOR=1). Route handler `src/app/api/auth/[...nextauth]/route.ts` = `export const { GET, POST } = handlers`. **NEVER import `@/auth` (Prisma+bcrypt) from middleware or a client component** — middleware imports `@/auth.config` only. `src/middleware.ts` `matcher:["/admin/:path*"]` (public routes are OUTSIDE the matcher, so `/`,`/comps/*`,`/builder*` stay reachable with no session); it redirects unauth `/admin/**`→`/admin/login?callbackUrl=…` and authed→`/admin` from the login page. TS AUGMENTATION GOTCHA (`src/types/next-auth.d.ts`): augment `Session`/`User` via `declare module "next-auth"` BUT augment `JWT` via `declare module "@auth/core/jwt"` — `next-auth/jwt` is a pure `export *` re-export so augmenting it doesn't merge into the interface the callbacks' `token` uses (symptom: `if(token.id)` narrows `unknown`→`{}` → TS2322). ENV (documented in `.env.example`, real values in gitignored `.env`): `AUTH_SECRET` (`openssl rand -base64 32`), `AUTH_TRUST_HOST=true` (non-Vercel dev), `ADMIN_EMAIL`/`ADMIN_PASSWORD`. `prisma/seed.ts` `seedAdminUser()` upserts the ADMIN curator (bcryptjs hash, keyed on unique email — idempotent). Login form (`src/components/admin/login-form.tsx`, `"use client"`) uses `signIn("credentials",{redirect:false})` from **`next-auth/react`** (React 18.3 has NO `useActionState`, so client `signIn`/`signOut` beat server-action + `useFormState`); LogoutButton uses `signOut({callbackUrl:"/admin/login"})`. `/admin/login` is PUBLIC (US-031's `(admin)` protected layout must NOT wrap it or you get a redirect loop); `/admin` is the minimal protected landing US-031 replaces. Admin pages that call `auth()`/`requireRole()` read cookies ⇒ auto-dynamic (no `force-dynamic` needed). US-045 UPDATE: the Credentials field is now **`identifier`** (was `email`) so a plain username logs in — `signIn("credentials", { identifier, password })`, provider `credentials: { identifier: { type: "text" }, ... }`; `authorize` normalizes `credentials.identifier` (trim+lowercase) then `db.user.findUnique({ where: { email } })` (User's only unique col is still `email`, so usernames live there and MUST be seeded lowercased). `prisma/seed.ts` now also runs `seedCuratorUser()` — a SEPARATE fn (never touches ADMIN) that upserts a role EDITOR user (default identifier/pass `tftlab`/`tftlab`, env `CURATOR_EMAIL`/`CURATOR_PASSWORD`). EDITOR already has full panel access (`requireRole` default = EDITOR), so no permission change was needed.
- Admin shell / route group (US-031): protected admin pages live in a `(admin)` ROUTE GROUP nested INSIDE `admin/` — i.e. `src/app/admin/(admin)/{layout,page}.tsx` (parens = no URL segment, so `(admin)/page.tsx` serves `/admin`). `/admin/login` stays at `src/app/admin/login/page.tsx`, a SIBLING of the `(admin)/` group so the protected layout NEVER wraps it (no redirect loop). ONE `admin` folder only — do NOT also create `src/app/(admin)/admin/` at top level (two folders → the `/admin` segment collides). US-033..US-039 admin pages go UNDER `src/app/admin/(admin)/` (e.g. `(admin)/comps/page.tsx` → `/admin/comps`) so they inherit the shell + guard. The group layout is `export const dynamic = "force-dynamic"` + async and calls `await requireRole("EDITOR")` at the top = the SINGLE guard for the whole subtree (child pages don't need to re-guard; they read live data). It renders the chrome: gradient 'MetaComps Admin' wordmark, `<AdminNav>` (`src/components/admin/admin-nav.tsx`, `"use client"`, active-link via `usePathname`, `flex-wrap` so 4 links reflow at 360px), signed-in email + role badge + `<LogoutButton>` (US-030). metadata `robots:{index:false,follow:false}`. Admin dashboard/list DATA comes from `getAdminDashboardStats()` in `src/server/queries/admin.ts` — the ANTI-pattern of public queries: NOT `unstable_cache`'d (force-dynamic ⇒ counts must be live) and set-scoped `db.comp.count` per status. When deleting/moving a `page.tsx` (like the old US-030 `admin/page.tsx`), `rm -rf .next` before typecheck or a stale `.next/types` fails the build. GOTCHA: to verify a live count in the browser, insert a temp row (e.g. a DRAFT `Comp` with `patchIntroducedId`=current), screenshot, then delete it.
- CatalogPicker (US-032): `src/components/admin/catalog-picker.tsx` `<CatalogPicker>` is the REUSABLE admin autocomplete for every catalog relation — import it in the comp-form stories (US-034..037), don't re-implement per-field pickers. It's a CONTROLLED `"use client"` combobox over a normalized `CatalogOption[]` (`{id,name,iconUrl,meta?}`) — table-agnostic, so map Champion/Item/Trait/Augment rows to that shape at the call site (meta = the secondary line, e.g. `Custo N` / item-family / breakpoints / augment tier). SINGLE vs MULTIPLE is a discriminated union on `multiple`: single = `value:string|null` + `onChange(string|null)`; multiple = `multiple + value:string[]` + `onChange(string[])`; it returns DB row ids. Icons use next/image (host already whitelisted); accessible (role=combobox+listbox+option, ↑/↓/Enter/Esc/Backspace-on-empty); selections are removable chips; multiple hides already-selected; results capped MAX_RESULTS=50 with a 'Refine a busca…' hint. GOTCHA: any array DERIVED from `props.value` inside a component (like `selectedIds`) must be `useMemo`'d or `react-hooks/exhaustive-deps` warns on downstream memos and FAILS the zero-warning lint gate. It's mounted/demoed at `/admin/catalog` (`src/app/admin/(admin)/catalog/page.tsx` + client `catalog-explorer.tsx`), which loads the cached `getBuilderCatalog()` bundle (tag `catalog`) and fills the previously-dead dashboard/nav 'Catálogo' link. Reuse `getBuilderCatalog()` (NOT four separate queries) whenever an admin page needs the whole catalog.
- Admin comps list (US-033): the curator's comp management table lives at `src/app/admin/(admin)/comps/page.tsx` (under `(admin)` → inherits the force-dynamic guard/shell; the dashboard StatCards + AdminNav already deep-link here). It reads `getAdminComps(status?)` from `src/server/queries/admin.ts` (lean `Prisma.validator` `select`, set-scoped, NOT `unstable_cache`'d — admin is force-dynamic, counts/rows must be live; `orderBy updatedAt desc`; exported row type `AdminCompRow`). The `?status=draft|published|archived` query token (the SAME tokens the dashboard StatCards link with) maps to the `CompStatus` enum via a `STATUS_BY_PARAM` record — reuse those exact tokens when linking to filtered views. `searchParams` is a `Promise` in Next 15 — `await` it. Row `Editar` links → `/admin/comps/[id]` and the `Nova comp` button → `/admin/comps/new` (both 404 until US-034 builds the form routes — that's fine, the AC only needs the links present). RESPONSIVE ROW GOTCHA: in a `flex flex-wrap items-center` row with `shrink-0` metadata chips (tier/status/patch/date/action), a `flex-1` name div gets squeezed to 0 width on a 360px viewport and the name VANISHES — give the primary text `basis-full sm:basis-0 sm:flex-1` so it takes its own full line on mobile while staying inline on desktop. Reuse `TIER_META[tier].chipClass` (+ `text-background`) for the tier chip like the public pages. US-034..039 admin comp pages/actions build on this list (edit form, publish/archive, patch flow).
- Server actions / admin forms (US-034): mutations live in `src/actions/*` (write-side sibling of `src/server/queries/*`), each file `"use server"`. See `src/actions/CLAUDE.md` for the full convention set. Key rules to REUSE in US-035..039: (1) every exported action calls `await requireRole("EDITOR")` FIRST — a `"use server"` fn is a public endpoint, re-guard even though the page is guarded; (2) return a discriminated `{ ok: true; ... } | { ok: false; error }` result (NOT throw) so the client form shows inline validation; (3) the client form is `"use client"` + `useState` (React 18.3 has NO `useActionState`) — it `import { createComp, type CompFormInput } from "@/actions/comps"` (a `"use server"` file may export types too), awaits the action, then `router.push(...)` + `router.refresh()` on success (mirrors login-form). NEVER import `@/server/db`/`@/server/queries/*` into a client component; importing a server ACTION into a client component is the sanctioned client→Prisma bridge. (4) Stable+unique slug = `slugify(base)` (`@/lib/slug`, pure/client-safe so the form previews the same slug the action writes — shares the seed's algorithm) then loop `-2/-3/…` on a `findUnique({where:{slug}})` collision; `resolveUniqueSlug(base, excludeId?)` lets update keep its own slug. Empty optional text → `null` (`nullifyBlank`), not `""`. (5) A comp's `set` follows `patchIntroduced.set` (fetch the patch = also validates it) — no set field on the form. (6) NO `revalidateTag` in US-034 (admin list is force-dynamic, re-reads on nav); public-page ISR revalidation on publish is US-038. Form-value CONTROLLED-INPUT gotcha: convert nullable DB fields (`playstyle`/guide/`patchUpdatedId`) to `""`/`null` for the inputs in `initialValues`, and auto-derive slug from name only until the curator edits the slug field (a `slugTouched` flag, seeded `true` in edit mode). Routes: static `new` segment wins over dynamic `[id]` at the same level, so `/admin/comps/new` never resolves as `[id]="new"`.
- Comp composition editor (US-035): the traits + off-board EARLY/FLEX unit editor lives in `src/components/admin/comp-composition-form.tsx` (`"use client"`), rendered BELOW `<CompForm>` on the EDIT page ONLY (`.../comps/[id]/page.tsx`) — a comp needs an id first, and `new` redirects to `[id]` after create so the curator fills composition there. REUSABLE CatalogPicker patterns: (a) to use `<CatalogPicker>` as a pure "ADD" control that leaves you to render the chosen rows yourself (needed when each row carries extra data like a trait level), pass SINGLE mode with `value={null}` always + `options` pre-filtered to exclude already-chosen ids — it clears its query/closes on select and shows no chip, so `onChange={(id)=>id && add(id)}` just appends to your own state; (b) for a plain ordered set with no per-item data, MULTIPLE mode is the whole editor (add/remove chips, selection order = list order) — the two EARLY/FLEX lists are just `<CatalogPicker multiple value={ids} onChange={setIds} options={championOptions}/>`. Map the builder catalog to `CatalogOption` with `useMemo` (champion `meta: "Custo N"`, trait `meta: "Ativa em a / b"` from breakpoints). Server action `updateCompComposition` (in `src/actions/comps.ts`) REPLACE-ALL in a `db.$transaction`: `deleteMany`+`createMany` for `CompTrait` (order=index) and for CompUnits scoped to `role: { in: [UnitRole.EARLY, UnitRole.FLEX] }` (order=index across early then flex) — MUST scope the unit delete to EARLY/FLEX so CORE board units (stars/carry/coords/`CompUnitItem`) survive for US-037's builder (verified). Validate every id against the comp's OWN `set` (not the current set) via `db.trait.count`/`db.champion.count` `id:{in}` === deduped length, and require integer `level>=1`. Read side: `getAdminCompComposition(id)` in `src/server/queries/admin.ts`; the page fetches `getBuilderChampions(comp.set)`/`getBuilderTraits(comp.set)` (comp's set, not `getCurrentSet()`).
- Playwright MCP EACCES workaround (US-035, admin browser tests): with auth live (US-030), admin browser checks STILL use `http://localhost:3000` (NOT host.docker.internal); log in at `/admin/login` with `ADMIN_EMAIL`/`ADMIN_PASSWORD` from `.env` (defaults `admin@metacomps.gg`/`admin1234`). The MCP throws `EACCES: permission denied, mkdir '/workspace/screenshots'` on EVERY `browser_navigate`/`browser_click`/`browser_fill_form` (it eagerly creates a root-owned output dir we can't mkdir) — but THE ACTION STILL HAPPENS. Ignore the error and follow each nav/click with `browser_snapshot` (returns inline, writes no file) to read the resulting page; `browser_take_screenshot` works with a RELATIVE `filename` (lands in repo root — `rm` before staging). To browser-test admin composition/edit flows without corrupting the 3 seed comps, seed a throwaway `slug:"temp-us035"` DRAFT comp via a repo-root `tsx` script (include a CORE unit+item to prove it survives a save), drive the UI, verify with another `tsx` DB read, then `deleteMany({where:{slug}})`. Env NOTE (US-036): the embedded Postgres (:5432) + a `next dev` on :3000 were ALREADY running this session — check `ps aux | grep -E "postgres|next-server"` and `curl -s -o /dev/null -w "%{http_code}" localhost:3000` before spinning up your own; a `tsx -e "db.comp.count()"` one-liner confirms DB connectivity.
- Comp priority editor (US-036): item + augment-category priority editor in `src/components/admin/comp-priority-form.tsx` (`"use client"`), rendered BELOW `<CompCompositionForm>` on the EDIT page only. Follows the US-035 pattern exactly. ITEM PRIORITY = SINGLE-mode `<CatalogPicker value={null}>` add control (options exclude already-chosen, `meta` = pt-BR ItemType label) + ordered `<ol>` (icon/name/↑↓/×). AUGMENT PRIORITY = an ordered `<ol>` of the chosen categories (labels Economia/Itens/Combate — REUSE the same labels as public `comp-augments.tsx` for parity) with ↑↓/× reorder, plus `+ <Category>` buttons for not-yet-chosen ones; `Comp.augmentPriority` is a SUBSET/ordering of the 3 `AugmentCategory` enum values (can be empty, NOT fixed length 3). Server action `updateCompPriority(compId, {itemIds, augmentPriority})` (`src/actions/comps.ts`) = ONE `db.$transaction`: `deleteMany`+`createMany` `CompItemPriority` (order=index) + `db.comp.update` overwriting the scalar `augmentPriority` column. It touches ONLY those two things — per-unit `CompUnitItem` and the recommended-augment `CompAugment` list are the builder's (US-037) and MUST survive (verified). Validate `itemIds` vs the comp's OWN set (`db.item.count id:{in}` === deduped length) and each category vs the enum (dedupe keeping first order). Read side `getAdminCompPriority(id)`→`{itemIds, augmentPriority}` in `src/server/queries/admin.ts`; the `[id]` page adds `getBuilderItems(comp.set)` to its fetch. GENERAL: `AugmentCategory` (like `ItemType`) can be `import type`'d into a `"use client"` component (erased) and its labels/order pinned in a local `Record<AugmentCategory,string>` / `readonly AugmentCategory[]` literal.
- Admin final board = REUSE the public `<Builder>` (US-037): the admin's board editor is NOT a fork — it's the same `src/components/builder/builder.tsx` with an OPTIONAL `onSave?(units, augments)` prop. When `onSave` is present the builder switches to "admin save mode": adds a per-selected-unit carry toggle (`Marcar carry`/`★ Carry`, undoable via the SAME `commit()` history) + a `Salvar board` button + a save-status line, AND stops mirroring state into the URL / hides `Copiar link` (the admin persists to DB, doesn't share a code). To add an admin-only field to a placed unit, put it on the shared `PlacedUnit` (US-037 added REQUIRED `isCarry: boolean` — update the 3 construction sites: builder.tsx `placeChampion`, board-code.ts `toPlacedUnits`, builder.test.ts helper) and EXCLUDE it from the share code by widening `SharedUnit = Omit<PlacedUnit,'id'|'isCarry'>` so US-028 encode/decode + its tests stay unchanged. A server component can't pass a closure to a client prop, so the client bridge is a thin `"use client"` wrapper (`src/components/admin/admin-builder.tsx`) that holds `compId`, imports the server action, and passes `onSave={(u,a)=>saveCompBoard(compId,…)}` + `router.refresh()` on success — keeps the generic Builder decoupled from Prisma/the DB. The `[id]/build` page seeds `initialUnits`/`initialAugments` from `getAdminCompBoard` (deterministic `u0,u1,…` ids, SSR-safe) after sanitizing vs the comp.set catalog. PARITY is automatic: saving the CORE `CompUnit`s + their `CompUnitItem`s + `CompAugment`s writes exactly what the public comp page reads (board, carries, per-unit items, recommended augments) — verified end-to-end.
- Publish lifecycle + ISR revalidation (US-038): a comp's moderation `status` is owned EXCLUSIVELY by three server actions in `src/actions/comps.ts` — `publishComp`/`unpublishComp`/`archiveComp` — NOT by the base `<CompForm>` (the `status` field was removed from `CompFormInput`; `createComp` always makes a `DRAFT`, `updateComp` never touches `status`). This closes the FR-20 bypass AND avoids the stale-client-`status`-after-`router.refresh()` write-back bug. `publishComp` enforces FR-20 via `collectPublishBlockers(comp)` → list of missing requirements (name, tier, `compTrait.count≥1`, `compUnit isCarry ≥1`, `compUnit role:CORE + non-null boardRow/boardCol ≥1` = non-empty final board); non-empty ⇒ `{ok:false, error:"Não é possível publicar. Faltam: …"}`. All three actions call a shared `revalidateComp(slug)` = `revalidateTag("tierlist") + revalidateTag("comp:"+slug)` (from `next/cache`) so `/` and `/comps/[slug]` refresh immediately (unpublish/archive make the comp drop off the list + its page 404 via the cached `getCompBySlug` refetch returning null). `updateComp` ALSO revalidates when `existing.status==="PUBLISHED"` (edit a live comp's guide/tier → public reflects it; a tier change re-buckets it). VERIFIED GOTCHA: `revalidateTag` regenerates the `unstable_cache` entry from the LIVE DB, so it also cleans up a phantom comp deleted straight from the DB — direct DB writes never revalidate, only these actions do (so a browser cleanup that `deleteMany`s a comp won't refresh the tier list; that's fine for a DRAFT/ARCHIVED throwaway that was never on it). UI = `"use client"` `<CompStatusControls comp={{id,slug,status}}>` on the edit page: context-aware buttons (Publicar/Despublicar/Arquivar/Voltar para rascunho), inline blocker error, `status` in local `useState` (source of truth — `router.refresh()` re-runs the force-dynamic page but does NOT reinit client state), flipped optimistically on success.
- Patch flow / current patch (US-039): `src/actions/patches.ts` owns patches. `createPatch({version,releasedAt,set})` validates version/set non-empty + a `yyyy-mm-dd` date (`parseIsoDate` → UTC-midnight `Date`, timezone-stable, matches the UI's UTC `formatDate`) and rejects a duplicate `version` with a friendly message (don't let `@@unique(version)` throw a raw P2002); it does NOT revalidate (a new patch touches no public page). `setCurrentPatch(patchId)` = the core action: read the OUTGOING `currentPatchId` from `SiteConfig`, and when it exists AND differs from the incoming id, `upsert` a `CompTierSnapshot` (key `compId_patchId`, idempotent) pinned to the OUTGOING patch id for every PUBLISHED comp of the OUTGOING set (freezes that patch's tier history), then `upsert` `SiteConfig` (id:1, `currentSet` follows the patch's `set`) — snapshots + config in ONE `$transaction`. Finish with a SINGLE `revalidateTag("tierlist")`: the tier list `/` AND every comp page carry the `tierlist` tag and both read `currentPatchId` for the Novo/Atualizado badges, so one revalidate re-derives ALL badges from the new state (VERIFIED end-to-end: switching current 17.2→17.3 dropped the Novo/Atualizado badges off the cards + header flipped to 17.3; switching back to 17.2 restored both badges — route stayed "Static route"). Return `snapshotCount` so the UI can report "N comps carimbadas". Read side `getAdminPatchesData()` in `src/server/queries/admin.ts` (`{patches:{id,version,set,releasedAt,compsIntroduced,isCurrent}[], currentPatchId, currentSet, publishedCompCount}`; `_count.compsIntroduced`, `isCurrent = id===currentPatchId`; not-cached — admin is force-dynamic). Page `src/app/admin/(admin)/patches/page.tsx` (under `(admin)` → inherits force-dynamic guard/shell; fills the nav "Patches" link) pre-formats `releasedAt` → string before passing rows to the `"use client"` `<PatchList>` (avoids Date-in-client-prop concerns). `<PatchForm currentSet>` (create) + `<PatchList patches currentPatchId publishedCompCount>` (per-row "Definir como atual"; `current` in local `useState` is the source of truth for which row shows the "Patch atual" badge, flipped optimistically — `router.refresh()` doesn't reinit client state) in `src/components/admin/`. NOTE: `CompTierSnapshot` has `onDelete:Cascade` on `patch`, so deleting a patch cascades its snapshots — handy for test cleanup (delete the throwaway patch to drop its snapshots).
- Draft preview / rendering a comp outside the public route (US-040): to show a comp EXACTLY as it appears published from a non-public context, reuse the public detail machinery — the (now exported) `compDetailInclude` + `CompDetail` type from `src/server/queries/comp.ts` and the `src/components/comp-*` section components (`CompHeader`/`CompCarries`/`CompItemPriority`/`CompBoard`/`CompUnits`/`CompAugments`/`CompGuide` + `OpenInBuilder`) — DON'T rebuild the layout. The only difference from `getCompBySlug` is dropping the `status:"PUBLISHED"` filter: `getAdminCompForPreview(id)` = `db.comp.findUnique({where:{id}, include: compDetailInclude})` (not cached — admin is force-dynamic). The preview route lives at `src/app/admin/(admin)/preview/[id]/page.tsx` UNDER the `(admin)` group, so it's auth-gated for free (the group layout's `requireRole`) and inherits `robots:noindex,nofollow` — still set `robots` on the page when an AC explicitly requires noindex. Drafts are ALREADY excluded from every public surface (tier list `/`, `sitemap.xml`, `/comps/[slug]`) by the `status:"PUBLISHED"` filters in `src/server/queries/*`; a preview feature needs NO changes there — verify by flipping a comp to DRAFT in the DB (temp `tsx` script), curling those 3 surfaces, then flipping it back.
- Embedded-postgres GOTCHA (browser-test env): NEVER `rm pgdata/postmaster.pid` while Postgres is running — Postgres watches that file and SELF-TERMINATES within seconds when it vanishes (presents as a suddenly-unreachable DB). On a "lock file already exists" error, first `pgrep -f "bin/postgres -D /tmp/pgscratch"`: if a live postmaster owns it the pid file is NOT stale — only remove it after `pkill`-ing that process, then relaunch `node /tmp/pgscratch/start-pg.js`. Poll the `run_in_background` task's `/tmp/claude-*/tasks/<id>.output` file (grep for `PG_READY` / dev-server `Ready`) in a bounded loop rather than a fixed sleep. When `rm -rf .next` invalidates the running dev server, a stale server may still hold :3000 so the fresh `npm run dev` lands on :3001 — read the port from the task output, don't assume 3000.
- Analytics + error monitoring (US-041): both are DEPENDENCY-FREE, provider-agnostic, PRODUCTION-GATED and privacy-first — do NOT reach for `@sentry/nextjs` ('ex: Sentry' in the AC = an example; its webpack-plugin/OpenTelemetry deps break the minimal mandate; the layer is Sentry-ready via ONE env var). Client-safe pure-ish libs live in `src/lib/analytics.ts` + `src/lib/error-monitoring.ts` (like `tiers.ts`/`synergy.ts` — NO server imports, so `"use client"` code imports them directly). ANALYTICS: cookieless Plausible via a `window.plausible` command-QUEUE stub (`ensurePlausibleQueue` so calls buffer until the script loads); `track(name, props)` is a NO-OP unless `NEXT_PUBLIC_ANALYTICS_DOMAIN` is set AND the visitor consented (localStorage key `metacomps.analytics-consent`). The event-name map `ANALYTICS_EVENTS` (pageview/`filter_apply`/`comp_open`/`builder_open`) is the SINGLE source — reuse it, never inline event strings. `<AnalyticsProvider/>` (`"use client"`, in ROOT `src/app/layout.tsx`) owns consent state, renders `<ConsentBanner>` only when enabled+undecided, and loads the provider `<Script>` + `<PageviewTracker>` (uses `usePathname`, NOT `useSearchParams`, so NO Suspense needed and pages stay Static; query/filter changes don't create pageviews) ONLY after consent. Provider returns null entirely when the domain is unset (safe dev/preview default — verified 0 scripts/banner in HTML). HOW TO RECORD AN EVENT: from a client component call `track()` directly (tier-list-filters `toggleValue` → `filter_apply`); from a SERVER page mount drop `<TrackEvent name props/>` (fires once via a `useRef` guard — StrictMode-safe — and does NOT make the page dynamic; used for `comp_open`); for a click that must emit an event, split the clickable into a tiny `"use client"` component (`<BuilderOpenLink>` — `OpenInBuilder` still ENCODES the share code on the SERVER, only the anchor became client). ERROR MONITORING: `captureError(err,{source,digest})` → `toCapturedError` (normalizes Error/string/unknown) → POSTs via `navigator.sendBeacon` (fetch `keepalive` fallback) ONLY in production with `NEXT_PUBLIC_SENTRY_DSN`|`NEXT_PUBLIC_ERROR_REPORTING_URL` set; else `console.error` (never phones home in dev/CI). Wire-up = App Router `src/app/error.tsx` + `src/app/global-error.tsx` (both `"use client"`, report on mount, pt-BR retry UI) + `<ErrorMonitor/>` in the root layout (window `error`/`unhandledrejection` listeners for non-render errors). All four `NEXT_PUBLIC_*` vars documented in `.env.example`. To BROWSER-TEST the enabled path without a real endpoint: temporarily set the domain + point `NEXT_PUBLIC_ANALYTICS_SRC` at a throwaway `public/_stub.js` that flushes `window.plausible.q` into a global array, restart dev (NEXT_PUBLIC vars inline at COMPILE — a `.env` change needs a full restart, not just HMR), assert on the captured events, then revert `.env` (gitignored) + `rm` the stub/screenshot before staging.
- Image perf + no-horizontal-scroll (US-042): `next.config.mjs` `images` tunes perf on top of the CDN whitelist — `formats:['image/avif','image/webp']` (next/image content-negotiates AVIF→WebP→original; verified the optimizer returns `Content-Type: image/avif`), `minimumCacheTTL:2678400` (31d; catalog icons are immutable per set → optimized variants get `Cache-Control: max-age=2678400`), and pinned `imageSizes`/`deviceSizes` (every icon is 14–72 CSS px, so no oversized srcset variants). ALL icons render via `next/image` (NO raw `<img>` anywhere — it lazy-loads by default; only add `priority` for a real above-the-fold LCP image, which we have none of) and every `fill` icon MUST carry an explicit `sizes` (e.g. `sizes="64px"`) or Lighthouse flags "properly size images". NO-HSCROLL GOTCHA: the pure-CSS `IconTooltip` (`src/components/icon-tooltip.tsx`) is `absolute left-1/2 -translate-x-1/2 opacity-0`; near the right edge that invisible tooltip pokes past the viewport and adds phantom horizontal scroll (only bites at some widths — e.g. comp page at 768px, +21px). FIX = `overflow-x-clip` on the public `<main>` (`src/app/(public)/layout.tsx`): `clip` (NOT `hidden`) contains the X axis WITHOUT computing `overflow-y` to `auto`, so it creates NO scroll container — the `sticky` header keeps pinning, upward tooltips still show, and a `fixed` bottom-sheet drawer inside main is NOT clipped (plain `overflow:clip` doesn't make main a containing block for fixed descendants; all verified). To VERIFY responsiveness: measure `document.documentElement.scrollWidth - clientWidth` (target 0) at 360/768/1280 on `/`, `/comps/[slug]`, `/builder` via `browser_evaluate`. LIGHTHOUSE in this env: no `lighthouse` CLI/Chrome installed, but a Playwright chromium lives at `/ms-playwright/chromium-<ver>/chrome-linux64/chrome` — install `lighthouse`+`chrome-launcher` OUTSIDE the repo (`/tmp/lhscratch`, like embedded-postgres) and run mobile audits against a PRODUCTION build (`npm run build` → `npm start` on :3000, NOT dev — dev is unoptimized and scores poorly) with `chromePath` set to that chromium + flags `--headless=new --no-sandbox --disable-dev-shm-usage`; default lighthouse preset already = mobile (moto-g + 4x-CPU/slow-4g throttling). Scored `/`=99, `/comps/[slug]`=100 (both CLS 0). The build output is the perf receipt: public pages are `○ Static`/`● SSG` with ~117–119 kB First Load JS.
- Builder hex visual style (US-044): the builder board (`src/components/builder/builder-board.tsx`) draws each hex as a TWO-TONE FRAME (tftacademy style) — an outer RIM span (`inset-0`, clipped) with a state-colored top-lit GRADIENT + an inner WELL span (`inset-[7.5%]`, clipped, dark `bg-[#0a0e18]`) that seats the champion `next/image` (same 7.5% inset) — the rim showing through the inset gap IS the "border" (clip-path clips `ring`/`box-shadow`, so you CANNOT use them for a hex outline). The clip is `BUILDER_HEX_CLIP` (`src/lib/builder.ts`) = `polygon(50% 0%, 95% 25%, 95% 75%, 50% 100%, 5% 75%, 5% 25%)`: SAME vertical geometry as the shared `HEX_CLIP` (peaks 0/100%, shoulders 25/75%) so the 4×7 honeycomb tiling (`w-[13.333%]` + odd-row `pl-[6.667%]` + `marginTop:-2.5%`) is unchanged, but the 5%/95% horizontal insets leave a clean column gap so each hex reads as a distinct tile. Rim colors are FULL class strings in an if/else (no `bg-${x}` interpolation — Tailwind purge): empty `from-slate-600/70 to-slate-800/60`, armed `from-primary/70 to-primary/30` (cyan), occupied `from-slate-400/85 to-slate-600/75`, carry `from-amber-300 to-amber-600`, selected cyan brand `from-[#8af2e8] via-primary to-[#0e7490]`. Selected/carry ALSO get a glow via `style={{filter:"drop-shadow(...)"}}` — `drop-shadow` FOLLOWS the clip-path silhouette (the one shadow that works on a clipped hex). Hex overlays (stars/carry-badge/items/name) are positioned as % of the button box, so with the narrower hex keep them inside the shape (stars `top-[7%]`, items `bottom-[19%] w-[22%] px-[13%]`, name pill `bottom-[10%] inset-x-[6%]`). SCOPE: this is builder-only — `comp-board.tsx` has its OWN local `HEX_CLIP` const (does NOT import from builder.ts), so restyling the builder never touches the comp-detail board. The builder is a `"use client"` component with the board owned by `builder.tsx`; a visual-only change to the presentational `Hex` doesn't affect place/move/remove/stars/items/augments/synergy (all in the parent). BROWSER-TEST GOTCHA: an armed palette champion STAYS armed after placing (place doesn't disarm), so a later hex click PLACES it again (replacing any occupant) instead of selecting — disarm (click the armed palette btn / Escape) before selecting a placed unit; and firing arm+place in ONE `browser_evaluate` won't work (React hasn't re-rendered, so the hex handler sees stale `armedChampionId`) — use separate evaluate calls.
- Tailwind content globs MUST include `src/lib` (US-047): `tailwind.config.ts` `content` originally listed only `./src/pages`, `./src/components`, `./src/app`. But client-safe presentational helpers in `src/lib/*` hold LITERAL Tailwind class strings — `TIER_META` in `src/lib/tiers.ts` (`chipClass: "bg-tier-s"`, …) and cost colors in `src/lib/champion-cost.ts`. A class that appears ONLY in an un-scanned file is silently DROPPED from the build, so the S/A/B/C/X tier CHIP backgrounds (`bg-tier-*`) were transparent app-wide (public tier list, comp cards, comp header) — a latent bug since US-016 moved `TIER_META` into `src/lib`. `border-l-tier-s` survived only because it ALSO appears literally in `src/components/comp-card.tsx`. FIX: added `"./src/lib/**/*.{js,ts,jsx,tsx,mdx}"` to `content`. RULE: any `src/**` dir that can contain literal utility strings must be in `content`; when you move class-bearing constants into `src/lib`, confirm the glob covers it. To DIAGNOSE a "class in className but computed style is default/transparent" case, check the loaded stylesheet in the browser (`[...document.styleSheets].some(s => [...s.cssRules].some(r => r.selectorText?.includes('bg-tier-s')))`) — if the RULE is missing (not just overridden), it's a content-scan/purge problem, not a specificity one. A `content` change needs a dev restart + `rm -rf .next` (config changes don't reliably HMR).

---

## Mon Jun 30 2026 16:12 - US-001 (Scaffold do projeto Next.js + Tailwind)
- The Next.js + Tailwind scaffold was already present and correct from the init commit; this iteration verified it against the acceptance criteria rather than re-creating it.
- Verified:
  - App Router project under `src/app` with TypeScript + ESLint (`.eslintrc.json` extends next/core-web-vitals).
  - `src/app/globals.css` has the `@tailwind base/components/utilities` directives; Tailwind/PostCSS/autoprefixer configured.
  - package.json scripts present: dev, build, start, lint, typecheck.
  - `npm run typecheck` -> exit 0; `npm run lint` -> "No ESLint warnings or errors".
  - Dev server boots ("Ready"), `curl localhost:3000` -> 200.
  - Browser (Playwright at localhost:3000): homepage renders heading "MetaComps" + paragraph "Tier lists de composições de TFT."; only console error is a favicon 404 (harmless).
- Files changed: prd.json (US-001 passes=true + notes), progress.txt. No source changes needed.
- **Learnings for future iterations:**
  - See Codebase Patterns above — especially that browser checks use localhost:3000 directly, and that screenshot artifacts must be removed before `git add`.
  - Nothing is committed by these iterations (per CLAUDE.md, only `git add` to stage); the init commit already tracks the scaffold files.
---

## Mon Jun 30 2026 16:30 - US-002 (Aplicar tema de cores do tftlab.com.br)
- Implemented the tftlab dark slate + cyan theme as shadcn-style design tokens.
- Files changed:
  - `src/app/globals.css`: added `:root` HSL tokens (--background 224 71% 4%, --foreground 213 31% 91%, --primary/--ring 180 100% 42.5%, --secondary 175 60% 20%, --muted/--border/--input 217.2 32.6% 17.5%, --accent, --card, --popover, --destructive, foregrounds, --radius 0.5rem) plus tier vars --tier-{s,a,b,c,x}; added `@layer base` applying `bg-background text-foreground` to body and `border-border` to all.
  - `tailwind.config.ts`: mapped colors (background, foreground, card, popover, primary, secondary, muted, accent, destructive, border, input, ring, tier.{s,a,b,c,x}) to `hsl(var(--...))`, added borderRadius scale from --radius, and `backgroundImage["brand-gradient"]` = linear-gradient(#9333EA -> #5EEAD4). Set `darkMode: "class"`.
  - `src/app/page.tsx`: swapped `text-gray-500` for `text-muted-foreground`; heading now uses `bg-brand-gradient bg-clip-text text-transparent` to demonstrate the gradient token.
- Quality: `npm run typecheck` -> exit 0; `npm run lint` -> no warnings/errors.
- Browser verify (Playwright, localhost:3000, 1280x720): computed body background = rgb(3,7,17) (dark slate), body text = rgb(225,231,239) (light); h1 backgroundImage = linear-gradient(rgb(147,51,234), rgb(94,234,212)); tagline color = rgb(148,163,184) (muted-foreground); CSS vars read back exactly (--background "224 71% 4%", --primary "180 100% 42.5%", --tier-s "0 84% 60%", --radius "0.5rem"). 0 console errors. Screenshot showed dark page with purple->cyan "MetaComps" wordmark.
- **Learnings for future iterations:**
  - See the new Codebase Patterns line about the theme: use semantic classes (bg-background/text-foreground/text-muted-foreground/bg-card/border-border/bg-tier-*) instead of raw gray utilities so future UI inherits the tftlab identity automatically.
  - HSL tokens are stored as bare `H S% L%` (no `hsl()` wrapper) because tailwind wraps them via `hsl(var(--token))`; opacity modifiers like `bg-primary/50` work because of this.
  - `darkMode: "class"` is set but the dark palette is the default in :root, so no `dark` class toggling is needed yet (the whole site is dark).
  - Card token chosen slightly lighter than background (222 47% 11%) so cards read as elevated against the 224 71% 4% page; tier colors: S=red, A=orange, B=amber, C=green, X=slate-gray (situational).
---

## Mon Jun 30 2026 16:22 - US-003 (Configurar Prisma + Postgres + PrismaClient singleton)
- Wired Prisma to Postgres with a hot-reload-safe PrismaClient singleton.
- Key decision: pinned Prisma to v6 (6.19.3) instead of latest (7.8.0). Prisma 7 rejects `url = env("DATABASE_URL")` in the schema datasource ("The datasource property `url` is no longer supported in schema files") and mandates `prisma.config.ts` + a driver adapter. That directly contradicts this story's AC and would complicate every later schema/migrate/seed story (US-004..US-010, US-012). Prisma 6 keeps the classic, well-documented workflow the PRD assumes.
- Files changed:
  - `prisma/schema.prisma` (new): `generator client { provider = "prisma-client-js" }` + `datasource db { provider = "postgresql"; url = env("DATABASE_URL") }`.
  - `.env.example` (new): documents `DATABASE_URL` (postgresql://...). Local `.env` created from it for tooling but is gitignored, so not staged.
  - `src/server/db.ts` (new): exports `db` = PrismaClient singleton, cached on `globalThis` when `NODE_ENV !== "production"`, with dev query logging.
  - `package.json`: added `@prisma/client` (dependencies) and `prisma` (devDependencies); added `postinstall: prisma generate`. `package-lock.json` updated.
- Quality: `./node_modules/.bin/prisma generate` -> "Generated Prisma Client (v6.19.3)" with no error; `npm run typecheck` -> exit 0; `npm run lint` -> no warnings/errors. Backend-only story — no browser verification in the AC.
- **Learnings for future iterations:**
  - See the new Prisma line in Codebase Patterns. Most important: stay on Prisma 6; run `prisma generate` after schema edits; import DB as `{ db } from "@/server/db"`.
  - npm here runs with allow-scripts restrictions, so package install scripts (incl. @prisma/client postinstall) don't auto-run — run `./node_modules/.bin/prisma generate` manually after installing/changing the schema.
  - `prisma` CLI commands print an "Update available 6 -> 7" banner; that's expected noise, not an error — ignore it.
  - The generated client lands in `node_modules/@prisma/client` (gitignored), so nothing client-generated is staged; only the schema/env example/db.ts/package files are.
---

## Mon Jun 30 2026 16:25 - US-004 (Schema: modelos de catalogo Champion/Item/Trait/Augment)
- Added the four Data Dragon catalog models + their join table to `prisma/schema.prisma`, generated and applied the first migration.
- Schema details:
  - `enum ItemType { COMPONENT, COMPLETED, ARTIFACT, RADIANT, EMBLEM, SUPPORT, OTHER }`.
  - `Champion(apiId,name,cost Int,iconUrl,set)`, `Item(apiId,name,iconUrl,type ItemType,set)`, `Trait(apiId,name,iconUrl,set,breakpoints Json)`, `Augment(apiId,name,iconUrl,set,tier String?)`. All ids are `String @id @default(cuid())`; each has `createdAt`/`updatedAt`.
  - `@@unique([apiId, set])` + `@@index([set])` on all four catalog models (apiId is the Data Dragon natural key, scoped per set).
  - Join `ChampionTrait(championId,traitId)` with `@@unique([championId,traitId])`, `@@index([traitId])`, and `onDelete: Cascade` on both FKs. Reverse relations `Champion.traits` / `Trait.champions`.
- Migration: `prisma/migrations/20260630232443_catalog_models/` (+ `migration_lock.toml` provider=postgresql). Generated & applied with `prisma migrate dev --name catalog_models`; `prisma migrate status` -> "Database schema is up to date!".
- Files changed: `prisma/schema.prisma`, new `prisma/migrations/**`, `prd.json` (US-004 passes=true), `progress.txt`. No app/source code touched (backend-only story; no browser verification in AC).
- Quality: `prisma validate` OK, `prisma generate` OK (v6.19.3), `npm run typecheck` exit 0, `npm run lint` no warnings/errors. Runtime smoke test against the live DB confirmed: composite-key upserts, champion<->trait include, Json breakpoints, ItemType enum, and cascade delete on join rows.
- **Learnings for future iterations:**
  - See the new Codebase Patterns line "Local Postgres for migrations" — the env has no DB, so you MUST spin up embedded-postgres (with LANG=C + --locale=C) before any migrate/seed story. The start script at /tmp/pgscratch/start-pg.js is reusable.
  - Catalog ids are cuid strings; later models that reference catalog (CompUnit->Champion, CompTrait->Trait, CompUnitItem/CompItemPriority->Item, CompAugment->Augment) should use `String` FK fields. Add the REVERSE relation fields to these catalog models in the story that introduces each child model (Prisma requires both sides), not here.
  - Augment.tier is `String?` (TFT augment rarity is named: Silver/Gold/Prismatic). Distinct from the `AugmentCategory` enum (ECON/ITEMS/COMBAT) used for augmentPriority in later stories.
  - `prisma format` rewrote schema.prisma alignment — run it after editing so the diff stays clean.
  - Migration timestamps come from the machine clock; they're fine to commit. Don't hand-edit applied migration SQL — make schema changes and add a NEW migration instead.
---

## Mon Jun 30 2026 16:28 - US-005 (Schema: modelos Patch e SiteConfig)
- Added the `Patch` and singleton `SiteConfig` models to `prisma/schema.prisma`; generated & applied the migration.
- Schema details:
  - `Patch(id cuid, version String @unique, set String, releasedAt DateTime, createdAt DateTime @default(now()))` + `@@index([set])` + reverse relation `siteConfigs SiteConfig[]`.
  - `SiteConfig(id Int @id @default(1), currentSet String, currentPatchId String?, currentPatch Patch? @relation(...), updatedAt DateTime @updatedAt)`. Singleton row keyed on id=1; `currentPatch` is OPTIONAL (config may exist before a patch is seeded). FK emits `ON DELETE SET NULL`.
- Migration `prisma/migrations/20260630232742_patch_and_siteconfig/` generated & applied with `prisma migrate dev --name patch_and_siteconfig`; `prisma migrate status` -> "Database schema is up to date!" (2 migrations).
- Files changed: `prisma/schema.prisma`, new `prisma/migrations/20260630232742_patch_and_siteconfig/**`, `prd.json` (US-005 passes=true), `progress.txt`. No app/source code touched (backend-only story; no browser verification in AC).
- Quality: `prisma format` + `prisma validate` OK, `prisma generate` OK (v6.19.3), `npm run typecheck` exit 0, `npm run lint` no warnings/errors. Runtime smoke (temp .mjs run from the project tree, then removed) confirmed: `siteConfig.upsert({where:{id:1}})` returns id=1, `include: { currentPatch: true }` returns the linked patch, duplicate `Patch.version` rejected by the unique index, `updatedAt` auto-populated.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Patch/SiteConfig" line. Most important: read/write config via `db.siteConfig.upsert({ where: { id: 1 } })`; and when US-006/US-007 add Comp/CompTierSnapshot, remember to add their back-relation fields onto `Patch` too.
  - GOTCHA on the embedded Postgres: it may STILL be running from a prior iteration (the postmaster persists in /tmp/pgscratch/pgdata). `node start-pg.js` then fails with `lock file "postmaster.pid" already exists` — that's not a real failure. Check `pgrep -a postgres` first; if PID in postmaster.pid is alive, the server is up and you can run `prisma migrate dev` straight away (verify with `prisma migrate status`). Only re-run start-pg.js if no postgres process exists.
  - Bare `import { PrismaClient } from "@prisma/client"` only resolves when the script sits inside the project tree (node resolves node_modules upward from the file). A smoke script in /tmp/pgscratch fails with ERR_MODULE_NOT_FOUND — copy it into the repo root to run, then `rm` it before staging.
---

## Mon Jun 30 2026 16:31 - US-006 (Schema: modelo Comp + enums + relacao com Patch)
- Added the `Comp` model and its four enums to `prisma/schema.prisma`; wired the two Patch relations and generated/applied the migration.
- Schema details:
  - Enums: `Tier(S,A,B,C,X)`, `CompStatus(DRAFT,PUBLISHED,ARCHIVED)`, `Difficulty(EASY,MEDIUM,HARD)`, `AugmentCategory(ECON,ITEMS,COMBAT)`.
  - `Comp`: id cuid, `slug @unique`, name, set, `tier Tier`, `situational Boolean @default(false)`, `status CompStatus @default(DRAFT)`, `playstyle String?`, `difficulty Difficulty @default(MEDIUM)`, guide fields `whenToPlay?/earlyGame?/midGame?/lateGame?/tips?`, `augmentPriority AugmentCategory[]`, `createdAt @default(now())`, `updatedAt @updatedAt`, `publishedAt DateTime?`.
  - Two FKs to `Patch` require NAMED relations: `patchIntroduced` (required, `@relation("PatchIntroduced")`, FK ON DELETE RESTRICT) and `patchUpdated` (optional, `@relation("PatchUpdated")`, FK ON DELETE SET NULL). Added back-relations `Patch.compsIntroduced`/`compsUpdated`.
  - Indices `@@index([set, status, tier])` and `@@index([status])` (+ the implicit unique slug index).
- Migration `prisma/migrations/20260630233017_comp_model/` generated & applied with `prisma migrate dev --name comp_model`; `prisma migrate status` -> "Database schema is up to date!" (3 migrations).
- Files changed: `prisma/schema.prisma`, new `prisma/migrations/20260630233017_comp_model/**`, `prd.json` (US-006 passes=true + notes), `progress.txt`. No app/source code touched (backend-only story; no browser verification in AC).
- Quality: `prisma format` + `prisma validate` OK, `prisma generate` OK (v6.19.3), `npm run typecheck` exit 0, `npm run lint` no warnings/errors. Runtime smoke (temp .mjs in repo root, then removed) confirmed: full create with guide + `augmentPriority ["ECON","ITEMS","COMBAT"]` + both patches; default row (status DRAFT, difficulty MEDIUM, situational false, augmentPriority [], patchUpdatedId null); duplicate-slug rejection (P2002); and `Patch.compsIntroduced/compsUpdated` reverse-include.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Comp model" line. Most important: the two Patch FKs MUST be named relations or `prisma validate` fails with an ambiguity error; `augmentPriority` is a Postgres enum array that needs no `@default` (empty array is the implicit default).
  - Embedded Postgres was already running from a prior iteration (pgrep showed the postmaster on :5432) — no need to re-spin; just confirmed `prisma migrate status` was clean before migrating.
  - When US-007 adds Comp child join tables, add their back-relation fields onto `Comp` (and a `CompTierSnapshot[]` back-relation onto `Patch`), or the migration step will fail validation.
---

## Mon Jun 30 2026 16:34 - US-007 (Schema: tabelas de juncao da Comp)
- Added the six Comp join models + `enum UnitRole` to `prisma/schema.prisma`; wired all back-relations and generated/applied the migration.
- Schema details:
  - `enum UnitRole { EARLY, CORE, FLEX }`.
  - `CompTrait(compId, traitId, level, order)` @@unique([compId, traitId]) + @@index([traitId]).
  - `CompUnit(compId, championId, role UnitRole, isCarry @default(false), carryOrder?, starLevel?, boardRow?, boardCol?, order)` @@unique([compId, boardRow, boardCol]) + @@index([compId]) + @@index([championId]); relation `items CompUnitItem[]`.
  - `CompUnitItem(compUnitId, itemId, order)` and `CompItemPriority(compId, itemId, order)` (the latter @@unique([compId, itemId])).
  - `CompAugment(compId, augmentId, order)` @@unique([compId, augmentId]) and `CompTierSnapshot(compId, patchId, tier Tier)` @@unique([compId, patchId]).
  - `onDelete: Cascade` on EVERY FK (Comp/CompUnit children required by AC; catalog/patch-side FKs also Cascade to match the existing ChampionTrait join pattern).
  - Back-relations added: `Comp.{traits,units,itemPriority,augments,tierSnapshots}`, `CompUnit.items`, `Champion.compUnits`, `Trait.compTraits`, `Item.{compUnitItems,itemPriorities}`, `Augment.compAugments`, `Patch.tierSnapshots`.
- Migration `prisma/migrations/20260630233322_comp_join_tables/` generated & applied with `prisma migrate dev --name comp_join_tables`; `prisma migrate status` -> "Database schema is up to date!" (4 migrations).
- Files changed: `prisma/schema.prisma`, new `prisma/migrations/20260630233322_comp_join_tables/**`, `prd.json` (US-007 passes=true + notes), `progress.txt`. No app/source code touched (backend-only story; no browser verification in AC).
- Quality: `prisma format` + `prisma validate` OK, `prisma migrate dev` applied + `prisma generate` OK (v6.19.3), `npm run typecheck` exit 0, `npm run lint` no warnings/errors. Runtime smoke (temp .mjs in repo root, then removed) confirmed: full nested create (traits/units/unit-items/itemPriority/augments/snapshots all = 1), dup-hex rejection (P2002 on @@unique([compId,boardRow,boardCol])), multiple null-board units coexist, CompUnit->items cascade on unit delete, and full Comp-children cascade (traits/units/itemPriority/augments/snapshots all -> 0) on comp delete.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Comp join tables" line. Most important: the `@@unique([compId,boardRow,boardCol])` relies on Postgres NULL-distinctness so off-board EARLY/FLEX units (null coords) don't collide — keep boardRow/boardCol nullable.
  - Embedded Postgres was already running on :5432 from a prior iteration (pgrep showed the postmaster); `prisma migrate status` was clean before migrating, so no re-spin needed.
  - US-009/US-010 seed stories can now write the full comp tree in one nested `db.comp.create({ data: { traits: {create}, units: {create: { items: {create} }}, itemPriority, augments, tierSnapshots } })`.
---

## Mon Jun 30 2026 16:37 - US-008 (Schema: modelo User para o admin)
- Added `enum UserRole { ADMIN, EDITOR }` and `model User` to `prisma/schema.prisma`; generated & applied the migration.
- Schema details:
  - `User(id cuid, email String @unique, name String?, role UserRole @default(EDITOR), passwordHash String?, createdAt DateTime @default(now()))`.
  - User is standalone (no relations yet). `passwordHash` is optional so US-030's Auth.js can provision OAuth/credential users without a local password.
- Migration `prisma/migrations/20260630233538_user_model/` generated & applied with `prisma migrate dev --name user_model`; `prisma migrate status` -> "Database schema is up to date!" (5 migrations). SQL creates the `UserRole` enum, `User` table, and `User_email_key` unique index.
- Files changed: `prisma/schema.prisma`, new `prisma/migrations/20260630233538_user_model/**`, `prd.json` (US-008 passes=true + notes), `progress.txt`. No app/source code touched (backend-only story; no browser verification in AC).
- Quality: `prisma format` + `prisma validate` OK, `prisma migrate dev` applied + `prisma generate` OK (v6.19.3), `npm run typecheck` exit 0, `npm run lint` no warnings/errors. Runtime smoke (temp .mjs in repo root, then removed) confirmed: default row (role EDITOR, name/passwordHash null, createdAt auto-set), explicit ADMIN create, and duplicate-email rejection (P2002).
- **Learnings for future iterations:**
  - See the new Codebase Patterns "User model" line. Most important: `passwordHash` is intentionally nullable for US-030's Auth.js; `role` defaults to EDITOR and gates admin access (ADMIN vs EDITOR); there is no public sign-up.
  - Embedded Postgres was already running on :5432 from a prior iteration (pgrep showed the postmaster) — no re-spin needed; confirmed `prisma migrate status` clean after migrating.
  - US-030 (Auth.js) will likely add an adapter (sessions/accounts) or `requireRole()` helper around this model; keep User free of TFT-domain relations.
---

## Mon Jun 30 2026 16:52 - US-009 (Seed: importar catalogo do Data Dragon, idempotente)
- Implemented the catalog importer + seed. `src/server/ddragon.ts` fetches and normalizes the current set's catalog; `prisma/seed.ts` upserts it into the DB idempotently and links champions to traits.
- Source decision: used **Community Dragon** (`raw.communitydragon.org/latest/cdragon/tft/en_us.json`) instead of classic Data Dragon. The classic ddragon TFT files (`tft-champion.json`/`tft-trait.json`) expose only id/name/cost/image — they carry NO trait breakpoints and NO champion<->trait associations, both of which this story's AC explicitly requires ("Trait.breakpoints e Champion.cost preenchidos ... necessarios ao motor de sinergias"). CDragon's export has all of it. File/function keep the `ddragon` name per the PRD's file-path AC; documented in src/server/CLAUDE.md.
- `ddragon.ts` details: dynamically resolves the current set as the highest mainline mutator `TFTSet<number>` -> `TFTSet17` (no hardcoded number). `getCatalog()` returns normalized, de-duped, classified `champions/traits/items/augments` + `set` token; pure transform, no DB writes. Champions filtered to playable (has >=1 trait, cost 1-7). Trait `breakpoints` = sorted distinct `minUnits`. Items classified into `ItemType` by apiName keyword (Emblem/Radiant/Artifact/Support) + composition (>=2 components = COMPLETED, referenced-in-a-composition = COMPONENT, else OTHER); junk (Consumable/MarketOffering/nameless/etc.) filtered out. Augments scoped to the current set token. `.tex` asset paths rewritten to CDN PNGs via `assetUrl()`.
- `seed.ts` details: upserts each entity on `@@unique([apiId,set])` (`where:{apiId_set:{apiId,set}}`) in bounded-concurrency batches; populates `ChampionTrait`. Wired `prisma.seed = "tsx prisma/seed.ts"` + `db:seed` script + `tsx` devDep.
- Seeded set TFTSet17: **63 champions, 44 traits, 360 items, 53 augments, 133 championTraits** (all > 0). Verified Champion.cost has 0 missing; Trait.breakpoints filled for 42/44 (the 2 empties — "Choose Trait", "God-Blessed" — are meta-traits with genuinely no unit breakpoints). A champion icon URL HEAD-resolved to HTTP 200 image/png.
- IDEMPOTENCY proven: seeded twice, counts identical (133/133 championTraits, no drift).
- Files changed: NEW `src/server/ddragon.ts`, `prisma/seed.ts`, `prisma/CLAUDE.md`, `src/server/CLAUDE.md`; `package.json` (prisma.seed + db:seed + tsx) + `package-lock.json`; `prd.json` (US-009 passes=true); `progress.txt`.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors". Backend-only story — no "Verify in browser" in the AC.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Catalog seed / Data Dragon" line. THE key gotchas: (1) source is Community Dragon, not ddragon; (2) run the seed with `npm run db:seed`, never `prisma db seed` directly (tsx not on PATH -> ENOENT); (3) the Stargazer duplicate-trait-name trap — resolve ChampionTrait by a deterministic canonical apiId, not by display name, or re-seeds drift.
  - Node here is v24, so global `fetch` is available in the seed (no node-fetch dep). tsx works despite esbuild's blocked postinstall (the `@esbuild/linux-x64` binary ships in the optional dep).
  - To run an ad-hoc TS smoke script against the catalog/DB, drop a `_foo.ts` in the REPO ROOT (so relative imports `./src/server/...` resolve), run with `./node_modules/.bin/tsx _foo.ts`, then `rm` it before staging. `tsx -e '<code>'` defaults to CJS and rejects top-level await — use a file, not `-e`.
  - US-010 (next) can build the full comp tree with nested `db.comp.create`, stamp `set = getCatalog().set` (or read it back), and reuse the seeded catalog ids (look up Champion/Item/Trait/Augment by `apiId_set`).
---

## Mon Jun 30 2026 17:10 - US-010 (Seed: patch atual, SiteConfig e comps de exemplo)
- Extended `prisma/seed.ts` so a single `npm run db:seed` imports the catalog (US-009) AND seeds the patch/SiteConfig/sample comps. Refactored the old `main()` body into `seedCatalog()` (returns `{ set, setNumber }`); added `seedSampleData(set, setNumber)`; `main()` now calls both.
- What was seeded:
  - **Patches**: previous `17.1` (releasedAt 2026-06-01) + current `17.2` (releasedAt 2026-06-15), upserted on `version`.
  - **SiteConfig**: upserted on `id:1` with `currentSet=TFTSet17` + `currentPatchId`=current patch.
  - **3 PUBLISHED comps in distinct tiers (S/A/B)**, each built from REAL seeded catalog rows queried back from the DB (champions ordered by apiId, items `type:"COMPLETED"`, augments) — no hardcoded apiIds, so it follows whatever set the catalog loaded. Per comp: a disjoint 12-champion window sorted by cost → 3 EARLY (off-board, null coords), 7 CORE (on the 4x7 hex board), 2 FLEX; the 2 most expensive CORE units are carries (`isCarry`, star 3, 3 items each); 4 CompTrait (level = unit-count per trait among the comp's units), 4-item CompItemPriority, 3 CompAugment, `augmentPriority` array (rotated per comp), board positions, and full guide (whenToPlay + earlyGame/midGame/lateGame + tips).
  - **Badges**: comp #1 `n-o-v-a-blitzcrank` (S) introduced in current patch ⇒ NOVO; comp #2 `meeple-jhin` (A) introduced in previous + `patchUpdated`=current ⇒ ATUALIZADO; comp #3 `dark-star-master-yi` (B) introduced in previous, no update ⇒ none. (Badge derivation matches the US-011 rule: isNew = introduced==current; isUpdated = updated==current && !isNew.)
- Idempotency: comps have no natural key besides `slug`, so each is `deleteMany({where:{slug}})` (cascades children) then a single nested `db.comp.create`; slugs deduped per run via `uniqueSlug`. Proven by seeding TWICE — identical output (3 comps, same slugs, no drift). releasedAt/publishedAt use fixed ISO dates so timestamps don't drift across runs.
- Files changed: `prisma/seed.ts` (extended), `prisma/CLAUDE.md` (US-010 seed conventions), `prd.json` (US-010 passes=true + notes), `progress.txt`.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm run db:seed` completes with no error (twice). Verified via a throwaway tsx script (removed before staging): SiteConfig.currentPatch=17.2, 2 patches, 3 PUBLISHED comps with roles {EARLY:3,CORE:7,FLEX:2}, onBoard=7, 2 carries × 3 items, 4 traits, 4 itemPriority, 3 augments, complete guide, and the NOVO/ATUALIZADO/none badge states. Backend-only — no "Verify in browser" in the AC.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Sample data seed (US-010)" line. Most important: sample comps reference catalog rows QUERIED from the DB (never hardcode apiIds); comp idempotency is delete-by-slug + nested create (no upsert key); wire badges by setting patchIntroduced/patchUpdated relative to the current patch.
  - The first comp's slug came out `n-o-v-a-blitzcrank` because the top trait "N.O.V.A." slugifies the dots to hyphens — harmless, but a reminder that slugify collapses punctuation. Names are derived as `${topTrait.name} ${primaryCarry.name}` from live data, so exact slugs depend on the loaded set; downstream stories should look comps up by querying PUBLISHED comps, not by assuming a fixed slug.
  - US-012 (next, read queries) can now rely on real data: `getPublishedComps(set)` should read `SiteConfig.currentSet`/`currentPatchId` and return PUBLISHED comps with traits/carries; `getCompBySlug(slug)` should include units{items}, itemPriority, augments, board and guide. Drafts/archived must be excluded (all 3 seeded comps are PUBLISHED, so add a DRAFT later if you need to prove exclusion).
---

## Mon Jun 30 2026 17:20 - US-011 (Service de badges Novo/Atualizado com testes)
- Implemented the badge-derivation service and its unit tests; introduced the repo's first test runner.
- `src/server/services/badges.ts` (new): `getCompBadges(comp, currentPatchId): { isNew, isUpdated }`.
  - `isNew = comp.patchIntroducedId === currentPatchId`; `isUpdated = comp.patchUpdatedId === currentPatchId && !isNew` — matches the AC verbatim, Novo takes precedence over Atualizado.
  - Input is a minimal structural `BadgeComp { patchIntroducedId: string; patchUpdatedId: string | null }`, so a full Prisma `Comp` row satisfies it without a cast.
  - Guards `currentPatchId == null` (SiteConfig.currentPatchId is nullable) by returning both-false early; this also prevents a `null === null` false-positive on `isUpdated` for comps whose `patchUpdatedId` is null.
- `src/server/services/badges.test.ts` (new): 7 cases covering every AC branch — nova, atualizada, nenhuma (two variants: same earlier patch, and null patchUpdated), atualizada+nova (Novo precedence), and null/undefined currentPatchId.
- Test infra (first in repo): added `"test": "tsx --test \"src/**/*.test.ts\""` to package.json. Uses Node 24's built-in test runner through `tsx` (already a devDep) — no new dependency added. Tests use `node:test` + `node:assert/strict`.
- Files changed: NEW `src/server/services/badges.ts`, `src/server/services/badges.test.ts`; `package.json` (test script); `prd.json` (US-011 passes=true + notes); `progress.txt`.
- Quality: `npm test` -> 7 pass / 0 fail; `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors". Backend-only story — no "Verify in browser" in the AC.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns lines ("Unit tests" and "Badges service"). Key points: run tests with `npm test`; KEEP the glob quoted in the script; co-locate `*.test.ts` next to the code and remember they go through typecheck + lint too.
  - No test framework existed before this story — future test stories (US-024 synergy engine, US-028 board-code) just add more `*.test.ts` files and `npm test` picks them up automatically.
  - This story needed NO live Postgres — it's a pure function, so no embedded-pg spin-up was required (unlike the schema/seed stories).
  - UI badge stories (US-015, US-017) should import `getCompBadges` and pass `SiteConfig.currentPatchId`; don't re-derive the badge logic inline.
---

## Mon Jun 30 2026 17:40 - US-012 (Queries de leitura: tier list e comp por slug)
- Added the public read-query layer under `src/server/queries/` (new dir). Three modules, all using the `{ db }` PrismaClient singleton:
  - `config.ts`: `getSiteConfig()` -> singleton SiteConfig (id=1) with `currentPatch` included; `getCurrentSet()` -> `currentSet` or null. Single source of truth for current set/patch. Type `SiteConfigWithPatch`.
  - `tierlist.ts`: `getPublishedComps(set?)` -> PUBLISHED comps of the set (defaults to `SiteConfig.currentSet`; `[]` if unconfigured), ordered tier(S,A,B,C,X)/publishedAt desc/name, shaped as lean `CompCard` (main traits ordered + carries only [`units: { where: { isCarry: true } }`], champion id/name/iconUrl/cost). `groupCompsByTier(comps)` buckets into the fixed 5 bands (`TIER_ORDER`). Drafts/archived filtered by `status: "PUBLISHED"`.
  - `comp.ts`: `getCompBySlug(slug)` -> `findFirst({ slug, status: "PUBLISHED" })`, returns `CompDetail | null`. `CompDetail` includes the full tree: patchIntroduced/patchUpdated, traits(trait), units(champion, items(item)), itemPriority(item), augments(augment) — every child list ordered by `order`. Guide + board coords are Comp/CompUnit scalars (come for free). Null return lets US-017 render a 404 for missing/draft/archived.
- Row types exported via `Prisma.CompGetPayload<{ include: typeof theInclude }>` where each include is `Prisma.validator<Prisma.CompInclude>()({...})` (keeps the literal `orderBy` types so GetPayload resolves precisely — a plain object/`satisfies` widens `"asc"` to `string` and breaks it). UI components (US-014/015/017+) import `CompCard`/`CompDetail` instead of redefining shapes.
- Files changed: NEW `src/server/queries/config.ts`, `src/server/queries/tierlist.ts`, `src/server/queries/comp.ts`; `src/server/CLAUDE.md` (added queries/ section); `prd.json` (US-012 passes=true + notes); `progress.txt`.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors". Live-DB smoke (temp `_smoke.ts`, removed after; inserted then deleted a DRAFT + an ARCHIVED comp to prove exclusion): `getPublishedComps()` -> 3 PUBLISHED in S/A/B order, 0 non-PUBLISHED leaked; card[0] = 4 traits / 2 carries / badges {isNew:true}; explicit set arg -> 3; unknown set -> 0; groups S:1 A:1 B:1 C:0 X:0; `getCompBySlug(published)` -> full comp (12 units, 7 onBoard, 4 itemPriority, 3 augments, carry 3 items ordered 0,1,2, both patch rels, guide present); `getCompBySlug` -> null for nonexistent, draft AND archived slugs. Backend-only story — no "Verify in browser" in the AC.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Public read queries (US-012)" line — the 4 rules (always PUBLISHED-filter, set defaults to SiteConfig, `Prisma.validator` for include types, `findFirst` for slug+status) apply to every future public query.
  - Embedded Postgres was already running on :5432 from prior iterations (pgrep showed the postmaster) — no re-spin needed; the 3 US-010 seed comps were present, so no re-seed either.
  - US-014 (tier-list page) can call `groupCompsByTier(await getPublishedComps())` and render each band (empty state per band since bands are always present). US-015 (CompCard) consumes `CompCard`; US-017 (comp detail) consumes `CompDetail` + `getCompBySlug` (return null => `notFound()`), and both pass `(await getSiteConfig())?.currentPatchId` to `getCompBadges`.
---

## Mon Jun 30 2026 17:55 - US-013 (Layout publico: header com aba Builder + footer)
- Added the `(public)` route group and its shared layout with header + footer; moved the homepage into the group.
- Files changed:
  - NEW `src/app/(public)/layout.tsx`: async server component; `getSiteConfig()` -> `<SiteHeader patchVersion updatedAt>` + `<main class="flex-1">{children}</main>` + `<SiteFooter>`, inside a `min-h-screen flex-col` shell so the footer sits at the bottom.
  - NEW `src/components/site-header.tsx`: sticky `top-0` header (backdrop-blur). Brand "MetaComps" (bg-brand-gradient bg-clip-text text-transparent) links to `/`; `<nav aria-label="Navegação principal">` with Tierlist (`/`) + Builder (`/builder`); right-aligned patch block "Patch <version>" (text-primary) + "Última atualização: DD/MM/YYYY". Container is `flex-wrap justify-between` so the patch block wraps below-right on narrow screens. Date formatted from UTC parts (no locale/timezone drift). Both patch fields null-guarded.
  - NEW `src/components/site-footer.tsx`: Riot Games non-affiliation disclaimer (required for TFT fan sites).
  - MOVED `src/app/page.tsx` -> `src/app/(public)/page.tsx` (route group parens keep the URL at `/`); DELETED the old top-level page to avoid a duplicate-`/`-route build error.
- Quality: `npm run typecheck` exit 0 (after `rm -rf .next` to clear the stale generated types — see gotcha below); `npm run lint` "No ESLint warnings or errors".
- Browser verify (Playwright, dev server localhost:3000):
  - Desktop 1280x720: snapshot shows `banner` (brand + Tierlist/Builder nav + "Patch 17.2" / "Última atualização: 30/06/2026"), `main` (homepage), `contentinfo` (footer disclaimer). Screenshot = dark slate page, purple->cyan wordmark, cyan patch number.
  - Mobile 360x720: same structure; header usable, patch block wraps neatly below-right; NO horizontal scroll (evaluate: scrollWidth==clientWidth==360, overflow=false).
  - Only console error is the expected favicon 404 (harmless).
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Public layout (US-013)" line. Most important: put ALL public pages under `src/app/(public)/` so they inherit the header/footer; the group `layout.tsx` fetches SiteConfig; the root `layout.tsx` keeps `<html>/<body>`.
  - GOTCHA (cost me the first typecheck): after moving/deleting a `page.tsx`, `.next/types/app/page.ts` still points at the old file and `tsc` fails with `Cannot find module '.../page.js'`. Fix = `rm -rf .next` (it's a gitignored build cache) then re-run typecheck; dev/build regenerates correct types. This will recur in every story that adds/moves route files.
  - Dev server was started with `npm run dev` (background) and left running on :3000 for subsequent UI stories (like embedded-pg on :5432, it persists across iterations — check `curl -s -o /dev/null -w "%{http_code}" localhost:3000` == 200 before re-starting).
  - The Builder link points at `/builder` which doesn't exist yet (US-025) — that's fine, the nav just needs the links; the page arrives later.
---

## Mon Jun 30 2026 18:15 - US-014 (Pagina da tier list: faixas S/A/B/C/X)
- Replaced the placeholder Home with the tier-list page at `src/app/(public)/page.tsx` (route `/`).
- Implementation:
  - Renders the 5 fixed bands S, A, B, C, and Situacional (the X band — chip shows "X", heading "Situacional") in order, from `groupCompsByTier(await getPublishedComps())`.
  - Each band is a `<section aria-label={label}>` with: a tier chip (`bg-tier-{s,a,b,c,x}` + dark `text-background`) showing the letter, the band heading, and a 4px tier-colored left border (`border-l-4` + `border-l-tier-{s,a,b,c,x}`). Class names are FULL literals in a `TIER_META` map so Tailwind's JIT keeps them (interpolated `bg-tier-${tier}` would be purged).
  - Per-band empty state: "Nenhuma comp <Label> neste patch." (matches the AC example, e.g. "Nenhuma comp C Tier neste patch.").
  - Comps currently render as minimal name chips — US-015 swaps in the full `<CompCard>` (badges, trait/carry icons, link to `/comps/[slug]`) into these bands.
  - ISR: `export const revalidate = 3600` + data wrapped in `unstable_cache(fn, ["tierlist-groups"], { tags: ["tierlist"] })`. US-038's publish flow calls `revalidateTag("tierlist")`. No request-time APIs => route stays static.
- Files changed: `src/app/(public)/page.tsx` (rewritten), `prd.json` (US-014 passes=true + notes), `progress.txt`. No query/schema changes — reused `getPublishedComps`/`groupCompsByTier`/`TIER_ORDER` from US-012 as-is.
- Quality: `rm -rf .next` then `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors".
- Browser verify (Playwright, dev server localhost:3000, dev overlay showed "Static route"):
  - Desktop 1280x720: 5 bands in order; S=N.O.V.A. Blitzcrank, A=Meeple Jhin, B=Dark Star Master Yi, C+X empty states. Computed chip bg colors are distinct per tier (S red 239,67,67 / A orange 249,116,21 / B amber 231,176,8 / C green 33,196,93 / X slate 118,135,162), each left border matches its chip, all chip text is dark (3,7,17). No horizontal scroll (scrollWidth==clientWidth==1280).
  - Mobile 360x720: bands stack vertically (chip+label row on top, comps below); no horizontal scroll (360==360).
  - Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "ISR + tag revalidation (US-014)" line. Key points: keep pages static by pairing `export const revalidate` with `unstable_cache({ tags })`; never interpolate Tailwind tier classes (write them out); check the dev overlay's Static/Dynamic indicator.
  - The dev server + embedded Postgres were both already running from prior iterations and stayed up; the 3 US-010 seed comps (N.O.V.A. Blitzcrank/Meeple Jhin/Dark Star Master Yi in S/A/B) are what render — no re-seed needed. C and X bands are legitimately empty (the seed only makes S/A/B), which is exactly what proves the empty-state UI.
  - US-015 (next) builds the `CompCard` component and drops it into these bands (`comps.map` currently emits a name `<li>` — replace with `<CompCard comp={comp} currentPatchId={...} />`). It needs `SiteConfig.currentPatchId` for badges, so the page will also fetch `getSiteConfig()` (cache it under the same `tierlist` tag or read via the layout) and pass it down.
---

## Mon Jun 30 2026 18:40 - US-015 (Componente CompCard com badges)
- Built the `CompCard` component and dropped it into the tier-list bands (replacing the US-014 placeholder name chips).
- Files changed:
  - NEW `src/components/comp-card.tsx`: presentational server component. Props `{ comp: CompCard (from queries/tierlist), currentPatchId }`. The whole card is a `<Link href={`/comps/${comp.slug}`} aria-label={`Ver comp ${comp.name}`}>` (single keyboard-focusable target, accessible name = comp name, focus-visible ring). Renders: name (h3), carry champions (`comp.units` — already carries-only from the query — icon + name), and main trait icons with active level (`comp.traits`, ordered). `bg-muted/40` background + 4px tier-colored LEFT border via a full-class `TIER_BORDER` map (`border-l-tier-{s,a,b,c,x}`). Badges from `getCompBadges(comp, currentPatchId)`: Novo = `bg-primary text-primary-foreground` (cyan), Atualizado = `bg-secondary text-secondary-foreground` (teal); Novo precedence is in the service. Icons via `next/image` (fill+sizes for the 40px carry squares, 14x14 for traits).
  - `next.config.mjs`: added `images.remotePatterns` for `raw.communitydragon.org` — required so `next/image` will render the CDN icons (else it throws for an unconfigured host). US-042 owns the fuller image/perf story on top.
  - `src/app/(public)/page.tsx`: renamed the cached `getTierGroups` -> `getTierListData`, which now `Promise.all`s `groupCompsByTier(getPublishedComps())` AND `getSiteConfig()` and returns `{ groups, currentPatchId }` (both under the `tierlist` unstable_cache tag so US-038/US-039 revalidation refreshes badges too). The band map now renders `<CompCard comp currentPatchId/>`.
- Quality: `rm -rf .next` then `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors".
- Browser verify (Playwright, dev server localhost:3000, dev overlay "Static route"):
  - Desktop 1280x720: 3 cards — S=N.O.V.A. Blitzcrank (NOVO), A=Meeple Jhin (ATUALIZADO), B=Dark Star Master Yi (no badge); a11y snapshot shows each card as `link "Ver comp <name>"` -> `/comps/<slug>`, trait `img`s with alt=trait name. Computed: 18 icons all loaded (0 broken, champion naturalWidth 40), card bg rgba(30,41,59,0.4)=muted/40, left border rgb(239,67,67)@4px (tier-s), Novo bg rgb(0,217,217)=primary/cyan with dark text, Atualizado bg rgb(20,82,77)=secondary/teal with light text, no h-scroll (1280==1280).
  - Mobile 360x720: 3 cards stack, no h-scroll (360==360).
  - Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - See the new Codebase Patterns lines ("Catalog icons in the UI / next/image", "Presentational components"). Most important: use `next/image` (NOT `<img>`, which lint-warns and fails the zero-warning gate) and the `raw.communitydragon.org` remotePattern is now in next.config; import query row types (aliased) instead of redefining shapes; derive badges via `getCompBadges`, never inline.
  - The tier-list page's cached fetch now also carries `currentPatchId` under the `tierlist` tag — future badge-dependent pages should tag their patch reads similarly so patch changes revalidate them.
  - Dev server + embedded Postgres were already running from prior iterations; I did `rm -rf .next` (US-013 gotcha) which 500'd the running dev server, so I restarted `npm run dev` — if a page suddenly 500s after clearing `.next`, just restart dev.
  - US-017 (comp detail) should reuse this exact pattern: `getCompBySlug` -> `notFound()` on null, pass `(await getSiteConfig())?.currentPatchId` to `getCompBadges`, render icons via next/image.
---

## Mon Jun 30 2026 17:35 - US-016 (Filtros: tier, sinergia, campeao, busca)
- Added client-side filtering to the tier list while keeping the page STATIC/ISR. The server keeps shipping all PUBLISHED comps (unchanged `tierlist` unstable_cache tag); a `"use client"` component filters them from the URL.
- Files changed:
  - NEW `src/lib/tiers.ts`: client-safe `TIER_ORDER`, `groupByTier<T extends { tier: Tier }>`, `TierGroup<T>`, and `TIER_META` (label/chip/border full classes). `import type { Tier }` only, so no runtime Prisma dependency -> safe to import from client components. Single source of truth for tier grouping/presentation.
  - NEW `src/components/tier-bands.tsx`: shared presentational bands renderer (extracted from page.tsx). Props `{ groups, currentPatchId, hideEmpty? }`. `hideEmpty=false` (default) keeps the US-014 per-band empty states (default view); `hideEmpty=true` drops empty bands (filtered view). Renders `CompCard` per comp. No server-only imports, so it works in BOTH the server Suspense fallback and the client filter tree.
  - NEW `src/components/tier-list-filters.tsx` (`"use client"`): owns filter state<->URL and rendering. Reads `searchParams.getAll("tier"|"trait"|"champion")` (repeatable multi-select) + `q` (text); writes via `router.replace(qs ? \`${pathname}?${qs}\` : pathname, { scroll: false })`. AND across categories, OR within. Trait/champion options derived from the comps present (deduped by name, sorted). Search matches comp name + trait names + carry names. Desktop: inline panel (`hidden sm:block`). Mobile: a "Filtros" button (with active-count badge) opens a bottom-sheet drawer (`role="dialog" aria-modal`, Escape closes, "Ver resultados (n)" closes). `FilterControls` is rendered in BOTH the desktop panel and the drawer — since all state is in the URL they never diverge; the search field uses a wrapping `<label>` (NOT a fixed id) to avoid a duplicate id across the two instances. No-results state ("Nenhuma comp encontrada com os filtros selecionados" + Limpar filtros) shows only when a filter is active and nothing matches.
  - `src/app/(public)/page.tsx`: now fetches the FLAT comp list (+ currentPatchId) and renders `<Suspense fallback={<TierBands groups={groupByTier(comps)} .../>}><TierListFilters comps currentPatchId/></Suspense>`. The Suspense boundary is required for `useSearchParams` and keeps the route static (dev overlay: "Static route").
  - `src/server/queries/tierlist.ts`: refactored to import `groupByTier`/`TIER_ORDER` from `@/lib/tiers` and re-export them; `groupCompsByTier` is now a thin delegate. Removes the duplicated grouping/order and keeps the query module's public API.
  - `src/server/CLAUDE.md`: noted the grouping now lives in `src/lib/tiers.ts`.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass.
- Browser verify (Playwright, dev server localhost:3000):
  - Desktop 1280x720 ("Static route" confirmed): tier=A -> only A band; tier=A + champion=Blitzcrank -> no-results empty state + Limpar; trait=Challenger -> S+B bands (both have Challenger), A hidden; `?q=jhin` shared-link pre-fills the search box and shows only Meeple Jhin (search matches carry name); typing "dark" updates URL to `?q=dark` and filters to Dark Star Master Yi; no h-scroll (1280==1280); 0 console errors.
  - Mobile 360x720: inline panel hidden, "Filtros" button shown; drawer opens (heading + search + 20 toggle buttons [5 tier + 9 trait + 6 champion] + "Ver resultados"); selecting S in the drawer -> `?tier=S` and "Ver resultados (1)"; "Ver resultados" closes the drawer and the results show only the S band; "Filtros" button gains its active-count badge; no h-scroll (360==360).
  - Screenshots captured then removed before staging (repo-root artifacts).
- **Learnings for future iterations:**
  - See the new Codebase Patterns line "Client-side filtering of a static/ISR list (US-016)". Most important: `useSearchParams` MUST be inside `<Suspense>` to keep the page static; NEVER import `@/server/queries/*`/`@/server/db` into a client component (Prisma leaks into the bundle) — only `import type`; put client-safe helpers in `src/lib/*` and delegate from the server module.
  - The dev server was already running from prior iterations and hot-reloaded every change (no `rm -rf .next` needed since no route file was moved/deleted — page.tsx was edited in place). Embedded Postgres was already up with the 3 US-010 seed comps.
  - Filter option lists are derived from the comps actually present (only offer a trait/champion that some comp has) — with the current seed that's 9 traits + 6 carries; as more comps are published the lists grow automatically.
  - US-018..US-022 (comp detail sections) are next; they consume `getCompBySlug`/`CompDetail` and should reuse the client-safe-lib + shared-presentational split whenever a piece needs both server render and client interactivity.
---

## Mon Jun 30 2026 17:50 - US-017 (Pagina de comp: cabecalho e metadados)
- Created the comp-detail route + header. This page is the SHARED comp page that US-018..US-022 will extend (their sections render below <CompHeader/>).
- Files changed:
  - NEW `src/app/(public)/comps/[slug]/page.tsx`: `generateStaticParams` (from new `getPublishedCompSlugs()`), `export const revalidate = 3600`, per-slug `unstable_cache(fn, ["comp-detail", slug], { tags: [`comp:${slug}`, "tierlist"] })`, awaits the Next-15 `params` Promise, `notFound()` when `getCompBySlug` returns null (missing/draft/archived). Renders `<article><CompHeader/></article>`.
  - NEW `src/components/comp-header.tsx`: presentational header — tier chip (reuses `TIER_META[tier].chipClass`, `text-background`), name h1, Novo/Atualizado via `getCompBadges`, `S Tier`/`Situacional` label (`· Situacional` suffix only when `situational && tier!=="X"`), playstyle + difficulty (`DIFFICULTY_LABEL` EASY/MEDIUM/HARD -> Fácil/Médio/Difícil), traits with level (next/image icon + name + level, e.g. "N.O.V.A. 3").
  - `src/server/queries/comp.ts`: added `getPublishedCompSlugs()` (all PUBLISHED slugs, not set-scoped) for `generateStaticParams`/sitemap.
  - `src/server/CLAUDE.md`: documented `getPublishedCompSlugs`.
  - `prd.json` (US-017 passes=true + notes), `progress.txt` (this entry + Codebase Patterns line).
- Quality: `rm -rf .next` then `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass.
- Browser (Playwright): a stale prior-iteration dev server held :3000 (and `rm -rf .next` broke its artifacts → 404/500), so the fresh server ran on :3001 — killed the stale one and verified on :3001.
  - `/comps/n-o-v-a-blitzcrank` (S, NOVO): Tier S chip, h1 "N.O.V.A. Blitzcrank", NOVO badge, "S Tier", Estilo de jogo: Fast 8, Dificuldade: Difícil, traits N.O.V.A. 3 / Anima 2 / Challenger 2 / Conduit 2; 4 trait icons loaded (0 broken, naturalWidth 32); dev overlay "Static route".
  - `/comps/meeple-jhin` (A, ATUALIZADO): ATUALIZADO badge, Dificuldade: Médio, Slow Roll, traits Meeple 3 / Sniper 3 / Anima 2 / Rogue 2.
  - `/comps/does-not-exist` => HTTP 404 (notFound).
  - Only console error is the expected favicon 404; no horizontal scroll at 1280 (1280==1280) or 360 (360==360).
- **Learnings for future iterations:**
  - See the new "Comp-detail page (US-017)" Codebase Patterns line. Key points: US-018..US-022 add sections to THIS page below <CompHeader/> using the same `CompDetail` from `getCompBySlug`; the ISR tag is per-slug (`comp:<slug>`) + `tierlist`; `params` is a Promise in Next 15; `notFound()` narrows the null-check.
  - Don't re-implement badge/difficulty/tier presentation — reuse `getCompBadges`, `TIER_META`, and the `DIFFICULTY_LABEL` map in comp-header.tsx.
  - `rm -rf .next` (needed to clear stale route types after adding a page) also breaks any RUNNING dev server — restart it, and check the port it actually binds (a leftover server may hold :3000, pushing the new one to :3001).
---

## Mon Jun 30 2026 18:30 - US-018 (Pagina de comp: carries e prioridade de itens)
- Added the carries + item-priority sections to the shared comp-detail page, rendered below <CompHeader/> (US-019..US-022 add their sections into the same wrapper).
- Files changed:
  - NEW `src/components/icon-tooltip.tsx`: reusable `<IconTooltip src name size?=40 className?/>` — a pure-CSS tooltip icon (server component). Wrapper is `tabIndex={0} role="img" aria-label={name}` and focusable; the tooltip `<span aria-hidden>` toggles via `group-hover/tt` + `group-focus/tt` so it shows on hover AND keyboard-focus/touch (native `title` doesn't show on touch). Inner `next/image` is decorative (`alt=""`). Tooltip uses `bg-card text-card-foreground ring-border` because `--popover`==`--background` on this theme (invisible on dark), while `--card` reads as elevated.
  - NEW `src/components/comp-carries.tsx`: `<CompCarries units={comp.units}/>` (carries filtered by `isCarry`, sorted `carryOrder` nulls-last then `order`; each row = champion IconTooltip[48] + name + `★`×`starLevel` + its items in query order via IconTooltip; "Sem itens recomendados" fallback) and `<CompItemPriority items={comp.itemPriority}/>` (an `<ol>` of item IconTooltips separated by an aria-hidden right-chevron `PriorityArrow` SVG = the "setas" order indicator). Both take the narrow `CompDetail["units"]`/`["itemPriority"]` slice; both `return null` when empty.
  - `src/app/(public)/comps/[slug]/page.tsx`: imports the two components and renders them inside a new `<div className="mt-8 flex flex-col gap-8">` below <CompHeader/>; updated the file docstring.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass. (No route file moved, so no `rm -rf .next` needed — the running dev server hot-reloaded.)
- Browser verify (Playwright, dev server on :3001 — a stale prior server holds :3000):
  - Desktop 1280x720 — `/comps/n-o-v-a-blitzcrank`: Carries region = Bard (★★★; Adaptive Helm / Archangel's Staff / Bloodthirster) + Blitzcrank (★★★; Blue Buff / Bramble Vest / Deathblade); "Prioridade de itens" = 4 items with 3 chevron arrows between them; a11y tree shows each icon as `img "<name>"` (role=img + aria-label). Evaluate: 16 icon `<img>`s all loaded (0 broken, naturalWidth 32); hovering an item sets its tooltip opacity to 1 (confirmed "Blue Buff" tooltip visible in the screenshot); page stays "Static route"; no h-scroll (1280==1280).
  - Mobile 360x720: carries stack (champion row on top, items wrap below the name), item-priority row fits; both sections present; no h-scroll (360==360).
  - Only console error is the expected favicon 404. Screenshots captured (us018-desktop/mobile.png) then removed before staging.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns lines ("Reusable icon tooltip (US-018)", "Comp-detail sections (US-018)"). Most important: reuse `<IconTooltip>` for any name-on-hover icon (US-021 augments, builder) instead of `<img title>`; add each comp-detail section as its own presentational component taking the narrow `CompDetail[...]` slice, below <CompHeader/> in the same `flex-col gap-8` wrapper.
  - Query-provided child lists (units.items, itemPriority, augments, traits) are ALREADY `orderBy: { order: "asc" }` — render them as-is; only carries need a `carryOrder` re-sort because `comp.units` is ordered by `order`, not carryOrder.
  - `--popover` and `--background` are the SAME HSL on this theme, so a `bg-popover` overlay/tooltip is invisible on the dark page — use `bg-card` (elevated) for any floating surface.
  - The dev server + embedded Postgres were already up from prior iterations; the 3 US-010 seed comps (each with 2 carries × 3 items + 4-item priority) render both sections, so no re-seed was needed.
---

## Mon Jun 30 2026 18:50 - US-019 (Pagina de comp: unidades early/core/flex)
- Added the early/core/flex unit lists to the shared comp-detail page, rendered below the carries + item-priority sections (US-020..US-022 add board/augments/guide into the same wrapper).
- Files changed:
  - NEW `src/components/comp-units.tsx`: `<CompUnits units={comp.units}/>` (presentational, no `"use client"`). Groups `comp.units` by `role` into the three AC sections in fixed order — 'Early Units' (EARLY), 'Core/Final' (CORE), 'Flex Units' (FLEX) — via a `ROLE_SECTIONS` map; preserves the query's `order` (units are `orderBy order asc`, no re-sort); omits empty role sections; returns null when there are no units. Wrapper heading is 'Unidades'. Each `<UnitCard>` = shared `<IconTooltip size=56>` (name-on-hover/focus, next/image) + name (`line-clamp-2 text-xs` centered) + a cost badge (◆ + `champion.cost`, `aria-label="Custo N"`) color-coded by TFT cost tier via a full-class `COST_STYLE` map (1 slate / 2 emerald / 3 sky / 4 fuchsia / 5 amber; amber fallback for cost>5 — NO interpolation so Tailwind keeps the classes) + `★`×`starLevel` in `text-primary` (`aria-label="N estrelas"`) shown only WHEN starLevel is set. Cards are `w-16 sm:w-20` in a `flex-wrap` ul so they reflow on mobile.
  - `src/app/(public)/comps/[slug]/page.tsx`: import + render `<CompUnits units={comp.units} />` below `<CompItemPriority/>` inside the existing `mt-8 flex flex-col gap-8` wrapper; updated the file docstring (US-019 now rendered here; US-020..US-022 remain).
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass. (No route file moved, so no `rm -rf .next` needed — the running dev server hot-reloaded.)
- Browser verify (Playwright, dev server on :3001 — a stale prior server holds :3000):
  - Desktop 1280x720 — `/comps/n-o-v-a-blitzcrank`: 'Unidades' region with three subsections (h3 headings 'Early Units' / 'Core/Final' / 'Flex Units' in order). Early Units = Aatrox/Briar/Caitlyn (◆1, ★); Core/Final = Bel'Veth (◆2, ★★), Aurora/Diana (◆3, ★★), Aurelion Sol/Corki (◆4, ★★), Bard/Blitzcrank (◆5, ★★★); Flex Units = Cho'Gath (◆1, ★★), Akali (◆2, ★★). Evaluate: 12 unit icons all loaded (0 broken, naturalWidth 64); page stays "Static route"; no h-scroll (1280==1280); 0 console errors.
  - Mobile 360x720: the three sections render, cards wrap into a legible grid (cost colors distinct per tier, stars cyan), no h-scroll (360==360).
  - Only console message was the expected/harmless one (0 errors, 0 warnings at error/warning level). Screenshots captured (us019-desktop/mobile.png) then removed before staging.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns lines ("Champion cost colors (US-019)", "Comp unit lists (US-019)"). Most important: cost colors use a FULL-CLASS lookup map (default Tailwind palette) — never interpolate; the unit-list component follows the same narrow-slice + return-null-when-empty pattern as the other comp-detail sections.
  - The AC fixes the section labels as English TFT terms ('Early Units'/'Core/Final'/'Flex Units') even though the rest of the UI is pt-BR — kept them verbatim so browser verification matches the AC.
  - `comp-units.tsx` is a server component; if US-020 (board) or the builder (US-025/026) need the cost-color map on the client, lift `COST_STYLE` into `src/lib/*` (client-safe) rather than duplicating it.
  - Dev server + embedded Postgres were already up from prior iterations; the 3 US-010 seed comps (12 units each: 3 EARLY / 7 CORE / 2 FLEX) render all three sections, so no re-seed was needed.
---

## Mon Jun 30 2026 19:10 - US-020 (Pagina de comp: posicionamento no tabuleiro)
- Added the board-positioning section to the shared comp-detail page, rendered below <CompItemPriority/> and above <CompUnits/> inside the existing `mt-8 flex flex-col gap-8` wrapper.
- Files changed:
  - NEW `src/components/comp-board.tsx`: `"use client"` `<CompBoard units={comp.units}/>`. Builds a `byHex` Map from units with non-null `boardRow`/`boardCol` (0-indexed 4×7), returns null when nothing is on-board. Renders a 4-row × 7-col honeycomb: each hex is `w-[13.333%] shrink-0 aspect-square`; odd rows `pl-[6.667%]` (half-hex offset); rows nest via inline `marginTop:-2.5%`. Hexes are pointy-top `clipPath` polygons applied to layered absolute divs (tile/ring `inset-[2px]` + `next/image fill` icon `inset-[3px]`), so the ring shows as a hex border. Empty hexes = `bg-muted/30` grid backdrop. Carry (`isCarry`) highlight = extra `bg-brand-gradient` glow hex behind + gradient ring + thicker `inset-[4px]` image → prominent purple→cyan ring vs `bg-slate-600` on others. 'Mostrar nomes' `<button aria-pressed>` toggles a name label overlaid at each hex bottom (text-shadow for legibility; reads 'Ocultar nomes' when on). Board wrapper `mx-auto w-full max-w-md` in a rounded bordered `bg-card/40` panel.
  - `src/app/(public)/comps/[slug]/page.tsx`: import + render `<CompBoard units={comp.units} />`; updated the file docstring (US-020 now rendered; US-021..US-022 remain).
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass. (No route file moved, so no `rm -rf .next` — the running dev server hot-reloaded.)
- Browser verify (Playwright; dev server on :3001 — a stale prior server holds :3000):
  - Desktop 1280x720 — `/comps/n-o-v-a-blitzcrank`: 'Posicionamento' region with 7 CORE units at the correct hexes (row0 Bel'Veth/Aurora/Diana, row1 Aurelion Sol/Corki, row3 Bard/Blitzcrank — matches the seed's CORE_HEXES). Evaluate: 7 board icons loaded (0 broken, naturalWidth 64), exactly 2 gradient/carry hexes (Bard+Blitzcrank), board inner width 448px, page stays "Static route", no h-scroll (1280==1280). Clicking 'Mostrar nomes' → button becomes 'Ocultar nomes' (aria-pressed true) and all 7 champion names overlay their hexes.
  - Mobile 360x720: board fits 16–344px within the viewport, 7 icons loaded (0 broken), names truncate gracefully on the small hexes, no h-scroll (360==360).
  - 0 console errors. Screenshots (us020-desktop/mobile.png) captured then removed before staging.
- **Learnings for future iterations:**
  - See the new Codebase Patterns line "Hex board / percentage layout (US-020)". Most important: a client component with only `useState` keeps the page STATIC (only `useSearchParams` forces Suspense/dynamic); the `w-[13.333%]` + `pl-[6.667%]` percentage recipe guarantees zero h-scroll at any width and is directly reusable for the US-025 BuilderBoard; clip-path clips `ring-*`/box-shadow, so frame hex icons with an inset image layer, not a ring.
  - Board coords are 0-indexed and match the seed's `CORE_HEXES` (row 0/1 front, row 3 back with carries) — the empty row 2 renders as a faint hex row, which is expected.
  - Dev server + embedded Postgres were already up from prior iterations; the 3 US-010 seed comps (7 CORE on-board units each, 2 carries) render the board, so no re-seed needed.
---

## Mon Jun 30 2026 19:25 - US-021 (Pagina de comp: augments e prioridade)
- Added the augments section to the shared comp-detail page, rendered below <CompUnits/> inside the existing `mt-8 flex flex-col gap-8` wrapper.
- Files changed:
  - NEW `src/components/comp-augments.tsx`: presentational (no `"use client"`) `<CompAugments augments={comp.augments} augmentPriority={comp.augmentPriority}/>`. Takes the narrow `CompDetail['augments']`/`['augmentPriority']` slices like the other sections. Renders an 'Augments' `<section aria-labelledby>` with TWO parts: (1) 'Prioridade de augments' — an `<ol>` of the comp's `augmentPriority` categories IN ORDER as teal `bg-secondary`/`text-secondary-foreground` chips, separated by the SAME aria-hidden right-chevron SVG used by `CompItemPriority` (n-1 arrows = the ECON>ITEMS>COMBAT priority indicator); categories mapped to pt-BR labels via a `CATEGORY_LABEL: Record<AugmentCategory,string>` (ECON→Economia, ITEMS→Itens, COMBAT→Combate). `AugmentCategory` type derived as `CompDetail['augmentPriority'][number]` (no direct Prisma enum import needed). (2) recommended augments list — each augment via the shared `<IconTooltip src name/>` (US-018) so the augment NAME shows on hover/focus/touch. Returns null only when BOTH `augments` and `augmentPriority` are empty.
  - `src/app/(public)/comps/[slug]/page.tsx`: import + render `<CompAugments .../>` after `<CompUnits/>`; updated the file docstring (US-021 now rendered; only US-022 guide remains).
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass. (No route file moved, so no `rm -rf .next` — the running dev server hot-reloaded.)
- Browser verify (Playwright; dev server on :3001 — a stale prior server holds :3000):
  - Desktop 1280x720 — `/comps/n-o-v-a-blitzcrank`: 'Augments' region with 'Prioridade de augments' rendering ECONOMIA › ITENS › COMBATE (2 chevron separators) + 3 recommended augments (Stellar Combo / Ahri's Boon / Anima Commander). Evaluate: all 3 augment icons loaded (0 broken, naturalWidth 48), tooltip names present in the a11y tree, no h-scroll (1280==1280). Only console error is the expected favicon 404.
  - Mobile 360x720: section renders, 3 icons loaded (0 broken), no h-scroll (360==360).
  - DATA-DRIVEN confirmed: `/comps/meeple-jhin` shows a DIFFERENT priority order (COMBATE › ITENS › ECONOMIA) and different augments (Apotheotic Forge / Divine Amendment / Meep King), proving the indicator reads `comp.augmentPriority` rather than a hardcoded order.
  - Screenshot (us021-augments-desktop.png) captured then removed before staging.
- **Learnings for future iterations:**
  - Reused the existing "Comp-detail sections (US-018)" pattern verbatim — no new Codebase Pattern needed. The augment-priority chevron is a copy of `CompItemPriority`'s `PriorityArrow` (kept local to avoid coupling the two files); if a third section wants it, lift it to a shared presentational primitive.
  - `Comp.augmentPriority` is a scalar `AugmentCategory[]` on the comp, so it comes back on `CompDetail` for free (no include needed) — derive the enum union as `CompDetail['augmentPriority'][number]` in a client-safe way instead of importing the Prisma enum.
  - The US-010 seed rotates `augmentPriority` per comp (comp #1 [ECON,ITEMS,COMBAT], comp #2 rotated), which is why the two comps show different orders — handy for verifying the indicator is data-driven.
  - Dev server + embedded Postgres were already up from prior iterations; the 3 US-010 seed comps each have 3 CompAugment rows + a rotated augmentPriority, so no re-seed needed.
---

## Mon Jun 30 2026 18:05 - US-022 (Pagina de comp: guia 'Quando jogar' e 'Como jogar')
- Added the guide section to the shared comp-detail page — the LAST section on `/comps/[slug]` and the final US-018..US-022 slice.
- Files changed:
  - NEW `src/components/comp-guide.tsx`: presentational (no `"use client"`) `<CompGuide comp={comp}/>`. Takes the WHOLE `CompDetail` (like `CompHeader`) because it needs the guide scalars AND the unit/item names for highlighting. Returns a FRAGMENT of up to three `<section>`s (so each inherits the page wrapper's `gap-8` uniformly); returns null when the comp has no guide content.
    - THEME 1 'Quando jogar' = a single block (`comp.whenToPlay`).
    - THEME 2 'Como jogar' = three `<h3>` subsections in fixed order via a `PHASES` map — 'Início de jogo' (`earlyGame`), 'Meio jogo' (`midGame`), 'Fim de jogo' (`lateGame`). Each phase renders only when its field is non-empty; `PHASES.flatMap(p => text ? [{...p, text}] : [])` type-narrows `string | null` → `string`. NO 'Stage 2/3/4' labels anywhere (the three subsections replace them; verified `/Stage\s*[234]/` = 0 matches).
    - Optional 'Dicas' block (`comp.tips`) in a bordered `bg-muted/40` card.
    - TEXT HIGHLIGHT of unit/item names: `collectNames(comp)` gathers champion names (`comp.units`) + item names (unit `items` + `itemPriority`) into a Set; `highlightNames(text, names)` builds a case-insensitive `RegExp` of the escaped names sorted LONGEST-FIRST (so 'Aurelion Sol' beats 'Sol') with letter/number lookbehind+lookahead boundaries (`u` flag + `\p{L}`/`\p{N}`) so a name isn't matched inside a larger word, then splits the string and wraps each hit in `<mark className="bg-primary/15 text-primary">`. `GuideText` splits prose on newlines into `<p>` paragraphs (readable multi-line; the seed text is single-line so one `<p>`). Trait names (e.g. 'N.O.V.A.') are intentionally NOT highlighted — the AC scopes highlight to units/items.
  - `src/app/(public)/comps/[slug]/page.tsx`: import + render `<CompGuide comp={comp}/>` after `<CompAugments/>`; updated the file docstring (all US-017..US-022 sections now rendered here).
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 7/7 pass. (No route file moved, so no `rm -rf .next` — the running dev server hot-reloaded.)
- Browser verify (Playwright; dev server on :3001 — a stale prior server holds :3000):
  - Desktop 1280x720 — `/comps/n-o-v-a-blitzcrank`: 'Quando jogar' region (single block), 'Como jogar' region with h3 subheads `['Início de jogo','Meio jogo','Fim de jogo']` in order, 'Dicas' region. Evaluate: 11 `<mark>` highlights on champion names (Blitzcrank, Aatrox, Briar, Caitlyn, Bel'Veth, Aurora, Diana); 0 'Stage 2/3/4' labels; page stays 'Static route'; no h-scroll (1280==1280). Only console error is the expected favicon 404.
  - Mobile 360x720: all three guide sections render, highlights show as cyan inline text, no h-scroll (360==360). Screenshot (us022-guide-mobile.png) captured then removed before staging.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Guide text + name highlighting (US-022)" line. Key reusable bit: to highlight catalog names inside free prose, escape + sort-longest-first + lookaround-boundary regex → `<mark>`; run it in a SERVER component (Node/SSR supports `\p{L}` + lookbehind, and it stays out of the client bundle). The admin comp form (US-034) writes these guide fields; the highlight adapts automatically since it reads names off the comp.
  - Reused the "Comp-detail sections" pattern: narrow-slice-or-whole-comp, `return null` when empty, presentational. This is the last comp-detail section — US-023 (SEO) is next and is a backend/metadata story on the same route.
---

## Mon Jun 30 2026 18:40 - US-023 (SEO: metadata, sitemap, robots e OG)
- Implemented the whole SEO layer for the public site. Backend/metadata story (AC only requires "Typecheck passes", no browser step) but runtime-verified anyway against the live seeded DB.
- Files changed:
  - NEW `src/lib/site.ts`: client-safe SEO constants — `SITE_URL` (reads `NEXT_PUBLIC_SITE_URL`, default `https://metacomps.gg`, trailing slash stripped), `SITE_NAME`, `SITE_DESCRIPTION`, `SITE_LOCALE` (`pt_BR`), `absoluteUrl(path)`. No server-only imports so it's safe anywhere.
  - `src/app/layout.tsx`: expanded root `metadata` — `metadataBase = new URL(SITE_URL)` (resolves all relative canonical/OG URLs to absolute), title `template` `%s · MetaComps` + default title, `applicationName`, and site-wide `openGraph` (type website) + `twitter` (summary_large_image) defaults inherited by every page.
  - `src/app/(public)/comps/[slug]/page.tsx`: added `generateMetadata` (title `${name} — ${tierLabel}`, description = `whenToPlay` clamped to 160 chars else a synthesized tier/patch/carries/traits summary, `alternates.canonical`, `openGraph` type=article with url/publishedTime/modifiedTime + title carrying tier+set+patch, matching `twitter` card; null comp → `robots:{index:false,follow:false}`) and an Article JSON-LD `<script>` in the page body. Helpers: `compPatchVersion`, `clamp`, `buildCompDescription`, `toIso`.
  - `src/app/(public)/page.tsx`: static `export const metadata = { alternates:{canonical:'/'} }`.
  - NEW `src/app/sitemap.ts` (`revalidate = 3600`): homepage + one entry per PUBLISHED comp; NEW NEW `src/app/robots.ts`: allow all, disallow `/admin`, sitemap + host.
  - `src/server/queries/comp.ts`: NEW `getPublishedCompsForSitemap()` → `{ slug, updatedAt }[]` (status:PUBLISHED, ordered by updatedAt desc) + `SitemapComp` type.
  - NEW `src/app/(public)/comps/[slug]/opengraph-image.tsx`: `next/og` `ImageResponse` 1200x630 (flexbox-only inline styles) — wordmark + patch, tier badge in tier color (hex mirrors globals.css tier tokens) + comp name, footer set/carries. NEW `twitter-image.tsx` re-exports it (`export { default, alt, size, contentType } from "./opengraph-image"`).
  - `.env.example`: documented `NEXT_PUBLIC_SITE_URL`.
- BUG found & fixed during runtime verify: `getCompData` reads through `unstable_cache`, which JSON-serializes results → `Date` fields (publishedAt/updatedAt) are STRINGS on cache HITS (real Dates only on the first cache-MISS). The first `.toISOString()` call worked (miss), the 2nd request 500'd ("toISOString is not a function"). Fixed with `toIso(v) = new Date(v).toISOString()`. Added a Codebase Patterns line so future cached-data consumers avoid it.
- Quality: `rm -rf .next` then `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 7/7 pass.
- Runtime verify (dev :3000, live seeded DB): `/robots.txt` (Allow /, Disallow /admin, Host + Sitemap = metacomps.gg); `/sitemap.xml` (homepage + exactly the 3 PUBLISHED comps — dark-star-master-yi/meeple-jhin/n-o-v-a-blitzcrank — each with `<lastmod>`, no drafts); `/comps/n-o-v-a-blitzcrank` HTML has `<title>N.O.V.A. Blitzcrank — S Tier · MetaComps</title>`, `<link rel="canonical" .../n-o-v-a-blitzcrank>`, meta description, og:title (`… — S Tier · TFT TFTSet17 (patch 17.2)`) / og:description / og:url / og:type=article / og:image (1200x630) / og:image:alt, twitter:card=summary_large_image + twitter:title/description/image, and the Article JSON-LD with correct datePublished/dateModified; the og-image and twitter-image URLs both return HTTP 200 `image/png`, valid PNG magic bytes, 1200x630, non-blank (~31 KB). Verified the page returns 200 across repeated (cache-hit) requests after the toIso fix.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns lines: "SEO / metadata (US-023)" and the `unstable_cache` Date-serialization gotcha. Most reusable: satori (`ImageResponse`) is flexbox-only inline styles + hardcoded hex (no Tailwind/CSS-vars); file-based `opengraph-image`/`twitter-image` auto-fill the image meta tags (don't also set `openGraph.images`); and `new Date(v).toISOString()` any date read through a cache.
  - US-025 (`/builder`) and future public routes should set their own `alternates.canonical` (the root layout does NOT set a global canonical, on purpose, so non-comp routes don't all claim `/`). When a route becomes real, add it to `app/sitemap.ts`.
  - US-030+ admin: `/admin` is already `Disallow`ed in robots.txt; keep admin pages `noindex` too when they land.
---

## Mon Jun 30 2026 - US-024 (Builder: motor de sinergias — funcao pura + testes)
- Implemented the synergy engine as a pure, client-safe module (no imports), plus a full unit-test suite.
- Files changed:
  - `src/lib/synergy.ts` (new): `computeSynergies(units, traits) -> ActiveTrait[]` and the exported helper `countUnitsByTrait(units) -> Map<string,number>`. Types `SynergyUnit { championId, traits: string[] }`, `TraitInfo { key, name, breakpoints: number[] }`, `ActiveTrait { key, name, count, breakpoints, activeBreakpoint, tier, nextBreakpoint, maxed }`.
  - `src/lib/synergy.test.ts` (new): 17 node:test cases.
  - `prd.json` (US-024 passes=true + notes), `progress.txt`.
- Design:
  - PURE + client-safe (lives with tiers.ts/site.ts) so the US-025/026 `"use client"` builder can call it directly. No `db`/Prisma import.
  - KEY-AGNOSTIC: `SynergyUnit.traits` and `TraitInfo.key` share whatever string key the caller uses — catalog by trait NAME, DB by trait id — so the engine is decoupled from the data source (mirrors the badges service's structural-input approach).
  - Board POSITION is deliberately omitted from `SynergyUnit`: TFT trait counts don't depend on where a unit sits, so "positioned units" reduce to `{ championId, traits }` for synergy purposes.
  - UNIQUE COUNTING: dedupe by `championId` (duplicate copy contributes nothing) + `new Set` over each champion's traits (repeated trait counts once). Both satisfy AC "nao duplica a mesma unidade".
  - BREAKPOINTS: normalized (sorted, distinct, positive) defensively; active when count >= first bp; `tier` = highest bp index reached; `nextBreakpoint` null at max. `[]`-breakpoint meta-traits never activate; unknown traits ignored.
  - Output pre-sorted for a synergy panel: tier desc, count desc, name asc.
- Quality: `npm test` -> 24 pass / 0 fail (17 synergy + 7 badges); `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors". Backend/pure-function story — no browser verification in AC.
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Synergy engine (US-024)" line. Most important for US-025/026: import `computeSynergies` from `@/lib/synergy` into the client builder; feed it placed champions mapped to `{ championId, traits }` + the set's trait breakpoints as `TraitInfo[]`.
  - `Trait.breakpoints` is a Prisma `Json` column; when building `TraitInfo[]` from DB rows, cast it (e.g. `bp as number[]`). The catalog's `CatalogTrait.breakpoints` is already `number[]`.
  - Reuse `countUnitsByTrait` for the team unit-count/cost display in US-026 rather than re-deriving unique-unit logic.
---

## Mon Jun 30 2026 - US-025 (Builder publico: tabuleiro + paleta de campeoes)
- Built the public builder at `/builder`: a 4x7 hex board (BuilderBoard) + a champion palette (ChampionPalette) with click/drag placement, move, remove, name toggle, clear and undo/redo.
- Files changed:
  - NEW `src/server/queries/catalog.ts`: `getBuilderChampions(set?)` -> `BuilderChampion[]` (plain serializable rows: id/apiId/name/cost/iconUrl + traits[{id,name,iconUrl}]); set defaults to `getCurrentSet()`, ordered cost-then-name, traits sorted by name.
  - NEW `src/lib/builder.ts` (client-safe, no imports): board dims (4x7), `HEX_CLIP`, `PlacedUnit` type, `hexKey`/`unitsByHex`, and the `CHAMPION_DND_PREFIX`/`UNIT_DND_PREFIX` drag payload prefixes.
  - NEW `src/lib/champion-cost.ts` (client-safe): `costClass(cost)` — the TFT cost-color map LIFTED out of `comp-units.tsx` so the palette and the US-019 unit list share one source. Refactored `src/components/comp-units.tsx` to import it (identical classes; comp page re-verified HTTP 200).
  - NEW `src/components/builder/champion-palette.tsx` (`"use client"`): search box (matches champion name OR trait names) + sort select (Custo/Nome/Origem/Classe; origem=first trait, classe=last trait, since the schema has no origin/class flag); champion tiles are `draggable`, show cost badge, arm on click (`aria-pressed`).
  - NEW `src/components/builder/builder-board.tsx` (`"use client"`): 4x7 honeycomb reusing the US-020 comp-board percentage layout (no h-scroll). Each hex is a `<button>` (aria-label "<name> — linha R, coluna C" / "Hex vazio ...") that routes clicks to the parent and accepts champion/unit drops; placed units are draggable between hexes.
  - NEW `src/components/builder/builder.tsx` (`"use client"`): owns all board state + undo/redo snapshot history + toolbar (Nomes/Desfazer/Refazer/Remover/Limpar + unit count + aria-live hint). Keyboard: Delete removes, Escape disarms/deselects, Ctrl/Cmd+Z undo, Ctrl+Shift+Z / Ctrl+Y redo (ignored while a form field is focused).
  - NEW `src/app/(public)/builder/page.tsx`: server RSC, `revalidate=3600`, champion list via `unstable_cache(..., { tags: ["catalog"] })`, metadata title "Builder" + canonical "/builder". Ships champions to `<Builder>`.
  - `src/server/CLAUDE.md` (+catalog.ts note), `prd.json` (US-025 passes=true + notes), `progress.txt` (+Codebase Patterns "Builder (US-025)").
- Quality: `rm -rf .next` then `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 24/24 pass.
- Browser verify (Playwright, localhost:3000; screenshots taken with relative filenames then removed):
  - DESKTOP 1280x720 — page is "Static route" (no request-time APIs; the client builder uses only `useState`, no `useSearchParams`, so no Suspense needed). 28 empty hexes + 63-champion palette sorted by cost. Flows verified: arm Aatrox (hint + aria-pressed) -> click (1,1) => "1 unidade"; arm+place Jhin (2,3) => "2 unidades"; disarm -> select Aatrox (Remover enabled, hint "... selecionado") -> click empty hex => moved to (4,7); Desfazer => Aatrox back to (1,1) + Refazer enabled; Refazer => (4,7); select + Remover => Aatrox gone ("1 unidade", Remover re-disabled); Nomes => button "Ocultar nomes" + Jhin name overlay; palette search "jhin" => 1 result; search "stargazer" => 6 champs, NONE name-matching (proves trait search); sort Nome => alphabetical, Origem => trait-grouped order; Limpar => "0 unidades" + Undo still enabled (clear is undoable). No h-scroll (1280==1280). Only console error is the expected favicon 404.
  - MOBILE 360x720 — board + palette stack vertically, toolbar wraps to two rows, hex board fits, placed Aurora at (2,4). No h-scroll (360==360).
- **Learnings for future iterations:**
  - See the new Codebase Patterns "Builder (US-025)" line — most reusable for US-026/027/028: the `PlacedUnit[]` + snapshot-history undo/redo model, the arm/select/drag interaction, generating unit ids with `crypto.randomUUID()` only in handlers, and the client-safe `src/lib/builder.ts` + `src/lib/champion-cost.ts`.
  - Browser-test GOTCHA: two synchronous `.click()`s in one `browser_evaluate` don't chain (React batches state; the 2nd handler reads stale state). Arm and place in SEPARATE calls, or use the MCP `browser_click` tool per action.
  - MCP `browser_click`/`browser_navigate` throw `EACCES mkdir /workspace/screenshots` when auto-capturing, but the ACTION still executes — verify the result with `browser_snapshot`/`browser_evaluate`. `browser_take_screenshot` with a RELATIVE `filename` works and lands in the repo root (remove before staging).
  - US-026 needs a `getBuilderTraits()` query (trait `key`/`name`/`breakpoints`, casting `Trait.breakpoints` Json -> `number[]`) to feed `computeSynergies`; add per-unit fields (`starLevel`, later `items`/augments) to `PlacedUnit` in `src/lib/builder.ts` and thread them through `commit`.
---

## Mon Jun 30 2026 - US-026 (Builder: estrelas, custo do time e painel de sinergias)
- Added per-unit star levels (1–3), a live synergy panel driven by src/lib/synergy.ts, and a team-value counter to the public builder.
- Files changed:
  - `src/lib/builder.ts`: `PlacedUnit` gained required `stars: number`; added `MIN_STARS`/`MAX_STARS`, pure `clampStars()` and `teamGoldValue(units, costOf)` (Σ cost×3^(stars-1)).
  - `src/server/queries/catalog.ts`: new `getBuilderTraits(set?)` → `BuilderTraitInfo[]` ({id,name,iconUrl,breakpoints}); `Trait.breakpoints` Json coerced via `toBreakpoints` (positive numbers, else []). Keyed by trait id (same id on champion traits).
  - `src/app/(public)/builder/page.tsx`: caches `{champions, traits}` in ONE `unstable_cache(['builder-catalog'],{tags:['catalog']})`; passes `traits` to `<Builder>`.
  - `src/components/builder/builder.tsx`: new `traits` prop; memoized `traitsById`/`traitInfos`/`activeTraits` (computeSynergies) + `teamValue`; `placeChampion` sets `stars:1`; new `setUnitStars` (undoable); star toolbar control when a unit is selected; 'Valor: ◆N' next to unit count; renders `<SynergyPanel>` below the board.
  - `src/components/builder/builder-board.tsx`: `★`×stars overlay per hex; hex aria-label includes star count (imports `clampStars`).
  - NEW `src/components/builder/synergy-panel.tsx`: presentational active-trait list (icon + name + nível + count/next, tier-colored badge, empty state).
  - NEW `src/lib/builder.test.ts`: unit tests for `clampStars` + `teamGoldValue`.
  - `src/server/CLAUDE.md`: documented `getBuilderTraits`.
- Quality: rm -rf .next; `npm run typecheck` exit 0; `npm run lint` 'No ESLint warnings or errors'; `npm test` 27/27 pass.
- Browser (Playwright, localhost:3001 — a stale prior server holds :3000): page stays 'Static route'. Placed Veigar(1)/Poppy(1)/Gnar(2) => '3 unidades, Valor: ◆4', synergy panel 'Meeple (nível 1) 3 / 5'. Selected Veigar => star group appears (1★ pressed); set 3★ => 'Valor: ◆12' (9+1+2), hex label 'Veigar, 3 estrelas', overlay '★★★', synergy UNCHANGED (stars don't affect trait count). Removed Gnar => '2 unidades, Valor: ◆10', Meeple<3 => '0 ativas' empty state, Veigar kept 3★. No h-scroll at 1280 (1280==1280) or 360 (360==360; star control + synergy panel + palette all stack). Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - See the new 'Builder stars / team value / synergy panel (US-026)' Codebase Patterns line. Key: `PlacedUnit.stars` is now REQUIRED — every place site sets it and move/swap preserves it; star edits go through `commit()` so they're undoable; stars scale team value but NOT synergy counts.
  - The synergy engine key space here is the trait `id` (both `BuilderChampion.traits[].id` and `BuilderTraitInfo.id`), not the trait name — pick one consistently.
  - Browser-driving gotcha (confirmed): placing with an armed champion does NOT auto-disarm, so a follow-up hex click places another copy — press Escape or click the palette tile again to disarm before selecting a placed unit. And reading `aria-pressed` in the SAME `browser_evaluate` that clicks returns the stale (pre-render) value — read it in a separate call.
---

## Tue Jun 30 2026 - US-027 (Builder: itens e augments nas unidades)
- Completed and verified the builder item panel + augment picker. The implementation was already written and committed in a prior "init" commit but the story was never verified/marked done (prd passes:false, no progress entry) — this iteration validated it end to end against the acceptance criteria and finished the bookkeeping.
- What was verified:
  - **DATA** — `src/server/queries/catalog.ts` adds `getBuilderItems(set?)` (BuilderItem: {id,apiId,name,iconUrl,type ItemType}) and `getBuilderAugments(set?)` (BuilderAugment: {id,apiId,name,iconUrl,tier}), both plain/cache-safe. Builder page bundles {champions,traits,items,augments} under one `unstable_cache(['builder-catalog'],{tags:['catalog']})`.
  - **STATE** — `src/lib/builder.ts`: `PlacedUnit.items: string[]` (cap MAX_ITEMS=3), pure helpers `addUnitItem`/`removeUnitItemAt`/`toggleAugment` (cap MAX_AUGMENTS=3), `ITEM_DND_PREFIX`. Board augments are a board-level `useState` (not per-unit, not historied); item edits go through `commit()` so they're undo/redo-able.
  - **UI** — `src/components/builder/item-panel.tsx` (5 ItemType tabs + search, 3 removable equipped slots, click-to-equip, draggable items, grid disabled when full), `augment-picker.tsx` (board-level, n/3, 3 removable slots, toggle grid, remaining disabled at cap), `builder-board.tsx` overlays up to 3 item icons per hex + routes item drops to `onDropItem`, `builder.tsx` wires `equipItem`/`removeUnitItem`/`handleDropItem`/`handleToggleAugment`.
  - **TESTS** — `src/lib/builder.test.ts` covers `addUnitItem`/`removeUnitItemAt`/`toggleAugment`.
- Files (already committed): src/server/queries/catalog.ts, src/app/(public)/builder/page.tsx, src/lib/builder.ts, src/lib/builder.test.ts, src/components/builder/{builder,builder-board,item-panel,augment-picker}.tsx. This iteration only edits prd.json + progress.txt.
- Quality gates: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 30/30 pass.
- Browser (Playwright, localhost:3000 — dev server already running, DB seeded TFTSet17: 360 items across all tab types, 53 augments):
  - DESKTOP 1280x720 — page "Static route". ItemPanel is hidden until a unit is selected (placeholder "Selecione uma unidade… (até 3)"). Armed Aatrox → placed at (2,3) → disarmed → selected the hex → ItemPanel appears (header "Itens — Aatrox", 5 tabs, search). Equipped Adaptive Helm/Bloodthirster/Blue Buff → 3/3, cap message shown, all 51 grid items disabled, hex renders 4 imgs (champion + 3 items). Removed Adaptive Helm → 2/3, grid re-enabled, hex 3 imgs. Radiantes tab → exactly 10 RADIANT items; search "radiant" within Radiantes → 2 items (tab AND search). AugmentPicker: selected Ahri's Boon/Anima Commander/Apotheotic Forge → 3/3, aria-pressed on the 3, remaining 50 disabled. Screenshot captured then removed.
  - MOBILE 360x720 — no horizontal scroll (scrollWidth==clientWidth==360); both ItemPanel and AugmentPicker visible with all 5 tabs.
  - Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - See the new "Builder items + augments (US-027)" Codebase Patterns line. Most important for US-028: the share code must encode BOTH `PlacedUnit.items` and the board-level augment selection (alongside championId/row/col/stars).
  - Browser gotcha (reconfirmed): the Playwright MCP's auto-screenshot after click/navigate throws `EACCES mkdir /workspace/screenshots`, but the ACTION still executes — verify the result with a follow-up `browser_evaluate`/`browser_snapshot`. `browser_take_screenshot` with a RELATIVE filename works fine (lands in repo root; rm it before staging). `input.fill()` (browser_type) does NOT hit the mkdir error.
  - This env already had Postgres running on :5432 (prior iteration) + dev server on :3000 + DB seeded — no need to re-spin. Confirm with `pgrep -a postgres` and `curl localhost:3000` before assuming they're down.
---

## Tue Jun 30 2026 - US-028 (Builder: compartilhar via codigo na URL)
- Implemented share-by-link for the builder: the whole board (units w/ position, stars, items + board augments) is encoded into a URL-safe code, the `/builder/[code]` route reloads it, and the URL mirrors edits live with a "Copiar link" button. Fully ephemeral — no server/DB writes (only catalog reads).
- Files changed:
  - **NEW `src/lib/board-code.ts`** — `encodeBoard(SharedBoard)`/`decodeBoard(code)` + `toPlacedUnits()`. `SharedBoard = { units: Omit<PlacedUnit,"id">[], augments: string[] }`. Codec: compact versioned JSON `{v:1,u:[[championId,row,col,stars,items[]]],a:[augmentIds]}` → `TextEncoder` → `btoa` → base64url (portable browser+Node). `decodeBoard` is defensive (null on malformed/wrong-version, drops out-of-bounds hexes, clamps stars, dedupes hexes last-wins, caps items/augments). `toPlacedUnits` assigns deterministic ids `u0,u1,…` (SSR-safe).
  - **NEW `src/lib/board-code.test.ts`** — 9 node:test cases (round-trip full/empty, URL-safe charset, null on garbage/non-JSON/wrong-version, bounds+clamp, hex last-wins, deterministic ids).
  - **NEW `src/server/queries/builder-catalog.ts`** — `getBuilderCatalog()` = the `unstable_cache(["builder-catalog"],{tags:["catalog"]})` bundle, extracted from builder/page.tsx so both builder routes share one cache entry.
  - **NEW `src/app/(public)/builder/[code]/page.tsx`** — decodes the param, sanitizes against the current catalog (drops unknown champion/item/augment ids), seeds `<Builder initialUnits/initialAugments>`; `robots:{index:false}` + `canonical:/builder`. Dynamic route (server-rendered on demand).
  - **EDIT `src/app/(public)/builder/page.tsx`** — now imports the shared `getBuilderCatalog()` instead of an inline `unstable_cache`.
  - **EDIT `src/components/builder/builder.tsx`** — optional `initialUnits`/`initialAugments` props seed history[0]/selectedAugments; `shareCode` useMemo; a `useEffect` mirrors state into the URL path via `window.history.replaceState(window.history.state,"",path)` (stays Static, no navigation); "Copiar link" button (`navigator.clipboard.writeText` in try/catch) + 1.5s "Link copiado!" toast.
  - CLAUDE.md: added the builder-catalog.ts note to `src/server/CLAUDE.md`.
- Quality gates: `rm -rf .next` then `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 38/38 pass; `npm run build` OK — route table confirms `/builder` = Static (○), `/builder/[code]` = Dynamic (ƒ, server-rendered on demand).
- Browser (Playwright, localhost:3001 — a prior-iteration dev server held :3000; DB live from prior iterations):
  - Placed Blitzcrank at r2c4 → URL became `/builder/<code>` decoding to `{v:1,u:[[id,1,3,1,[]]],a:[]}` (state reflected on edit). Set 3 stars → code stars 1→3. Equipped Adaptive Helm → code items updated. Selected Anima Commander augment → code `a:[augId]`.
  - "Copiar link" wrote the exact full URL to the clipboard (verified via a clipboard stub since the headless context blocks the real API with NotAllowedError) and toggled the button to "Link copiado!".
  - Opened the share URL FRESH (full reload): board reconstructed EXACTLY — Blitzcrank r2c4 "3 estrelas", 1 unidade, Valor 45 (=5×3² confirms stars restored), item icon overlaid on the hex, Augments 1/3, synergy panel recomputed (2 active traits). `robots` meta "noindex, follow", canonical `https://metacomps.gg/builder`.
  - Malformed code `/builder/this-is-not-a-valid-code` → empty board, no error overlay, URL self-heals to `/builder`.
  - No h-scroll at 1280 or 360 (scrollWidth==innerWidth); 0 console errors (only the expected favicon 404).
- **Learnings for future iterations:**
  - See the new "Builder share code + URL mirroring (US-028)" Codebase Patterns line. Most important for US-029 ("Abrir no Builder"): reuse `encodeBoard` to build a code from a comp's CORE units (map each to `{championId, row: boardRow, col: boardCol, stars: starLevel ?? 1, items: unit item ids}`) + augments, then link to `/builder/${code}` — the decode/sanitize path already handles everything.
  - URL mirroring MUST use `window.history.replaceState` (not `router.replace`) to keep the page Static and avoid a refetch/navigation; pass `window.history.state` through so Next's router state survives. This is the Next-supported pattern for client-only URL updates.
  - `crypto.randomUUID()` still must never run in render — decoded units get deterministic index ids from `toPlacedUnits`; only NEW placements (in event handlers) use `crypto.randomUUID()`. The two id spaces (`u0…` vs uuid) never collide.
  - Clipboard is blocked in the Playwright headless context (NotAllowedError) — to verify a copy handler, stub `navigator.clipboard.writeText` and assert the captured string; the real API works on localhost (a secure context) in a normal browser.
---

## Mon Jun 30 2026 - US-029 (Pagina de comp: botao 'Abrir no Builder')
- Added an "Abrir no Builder" button to the comp-detail page that encodes the comp's board into a builder share code and links to /builder/[code].
- Files changed:
  - `src/components/open-in-builder.tsx` (NEW): server component `<OpenInBuilder comp={comp}/>`. `boardFromComp(comp: CompDetail)` builds a `SharedBoard` from the on-board units (`boardRow !== null && boardCol !== null` = CORE units) mapped to `SharedUnit {championId, row: boardRow, col: boardCol, stars: starLevel ?? 1, items: items.map(i => i.itemId)}` + `comp.augments.map(a => a.augmentId)`, then `encodeBoard(...)` (US-028, pure/client-safe, runs in Node) produces the code at render time. Renders a `next/link` `<Link href={`/builder/${code}`}>` styled as a primary button (bg-primary) with a grid icon — NO client JS needed.
  - `src/app/(public)/comps/[slug]/page.tsx`: import `OpenInBuilder`; render it in a `mt-6` wrapper between `<CompHeader/>` and the `mt-8` sections div.
  - `prd.json` (US-029 passes=true + notes), `progress.txt` (this entry + a new Codebase Patterns line).
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 38/38 pass.
- Browser (Playwright, localhost:3000):
  - DESKTOP 1280x720 `/comps/n-o-v-a-blitzcrank`: button renders as a link to `/builder/<code>`; page stays "Static route"; only console error is favicon 404. The code decodes to 7 on-board units (5 cores @2*, 2 carries @3* with 3 items each) + 3 augments.
  - Following the link (fresh load of `/builder/<code>`): board reconstructed exactly — 7 unit icons + 10 item-overlay icons on the board, **Valor = ◆138** (= cores 2*3+3*3+3*3+4*3+4*3=48 + carries 5*9+5*9=90), **Sinergias 4 ativas** (Challenger 2/3, Conduit 2/3, Party Animal 1, Space Groove 1/3), **Augments 3/3**; 0 console errors; no h-scroll (1280==1280); "Static route".
  - MOBILE 360x720 `/comps/meeple-jhin`: button present + within viewport; no h-scroll (360==360).
- **Learnings for future iterations:**
  - See the new "Open a comp in the builder (US-029)" Codebase Patterns line. Most important: `encodeBoard` runs on the SERVER (btoa/TextEncoder are Node globals), so "open in builder" is a plain server-rendered `<Link>` with the code baked in — no client component required.
  - The comp's `CompUnit.championId` / `CompUnitItem.itemId` / `CompAugment.augmentId` already ARE the DB catalog ids the builder catalog keys on, so the code round-trips through `/builder/[code]`'s catalog sanitization with zero drops — no id translation needed.
  - `unit.boardRow`/`boardCol` are `number | null`; the `!== null` filter both narrows the types for the `as number` cast and correctly excludes off-board EARLY/FLEX units (the builder has no bench). Verified team value 138 matched exactly, proving every unit + star + item survived the encode→decode→sanitize→render pipeline.
---

## Mon Jun 30 2026 - US-030 (Auth do admin: Auth.js + login + middleware)
- Implemented admin authentication with Auth.js (NextAuth v5, Credentials provider + JWT sessions) and route protection via middleware.
- New/changed files:
  - `src/auth.config.ts` (new) — edge-safe base config (signIn page, jwt session strategy, jwt/session callbacks carrying user.id/role, empty providers). NO Prisma/bcrypt so the Edge middleware can import it.
  - `src/auth.ts` (new) — spreads authConfig + adds the Credentials provider (email/password → `db.user.findUnique` + `bcrypt.compare`); exports `{ handlers, auth, signIn, signOut }` and the server-only `requireRole(minRole="EDITOR")` guard.
  - `src/app/api/auth/[...nextauth]/route.ts` (new) — `export const { GET, POST } = handlers`.
  - `src/middleware.ts` (new) — matcher `/admin/:path*`; imports only `@/auth.config`. Redirects unauth `/admin/**` → `/admin/login?callbackUrl=…`, authed → `/admin` off the login page.
  - `src/types/next-auth.d.ts` (new) — Session/User augmentation via `next-auth`, JWT via `@auth/core/jwt`.
  - `src/components/admin/login-form.tsx` + `logout-button.tsx` (new, `"use client"`) — use `signIn`/`signOut` from `next-auth/react`.
  - `src/app/admin/login/page.tsx` (new, public) + `src/app/admin/page.tsx` (new, protected via `requireRole`).
  - `prisma/seed.ts` — added `seedAdminUser()` (bcryptjs hash, upsert on email, idempotent). Seeded the running DB with the ADMIN curator.
  - `.env.example` (+ local `.env`) — documented `AUTH_SECRET`, `AUTH_TRUST_HOST`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`.
  - `package.json`/lock — added `next-auth@beta`, `bcryptjs`, `@types/bcryptjs`.
- Quality: `rm -rf .next`; `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 38/38 pass.
- Browser (Playwright, localhost:3000, 1280x720):
  - `/admin` logged out → 302 to `/admin/login?callbackUrl=%2Fadmin`; login card renders with the dark theme + brand-gradient wordmark + cyan Entrar button.
  - Login `admin@metacomps.gg` / `admin1234` → lands on `/admin` showing "Autenticado como admin@metacomps.gg ADMIN" + Sair.
  - Visiting `/admin/login` while authed → redirected to `/admin`.
  - "Sair" → back to `/admin/login` (session cleared).
  - Wrong password → stays on `/admin/login` with inline "E-mail ou senha inválidos.", no auth granted.
  - curl cross-check: `/`, `/builder`, `/comps/[slug]` all 200 without a session; `/admin` and `/admin/comps/foo` redirect. `/api/auth/providers` lists the credentials provider.
  - Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - See the new Codebase Patterns line "Admin auth / Auth.js (US-030)". Key gotchas: SPLIT the config (edge-safe `auth.config.ts` for middleware vs Node `auth.ts` with the DB), and augment `JWT` via `@auth/core/jwt` (NOT `next-auth/jwt`, which is a pure re-export — augmenting it silently no-ops and `token.id` narrows `unknown`→`{}`).
  - React is pinned to 18.3.1, so `useActionState` does NOT exist — use client `signIn`/`signOut` from `next-auth/react` (no `SessionProvider` needed for those two) instead of server-action + `useFormState`.
  - US-031 builds the `(admin)` route group with a force-dynamic layout that validates the session — it must NOT wrap `/admin/login` (that page is intentionally public) or logging in redirect-loops. Reuse `requireRole()` for role gating; call `getSiteConfig()` for the dashboard's current-patch/draft-count widgets.
  - The seed now needs the DB reachable AND creates the admin user at the end of `main()`; to (re)create just the admin without re-fetching the whole catalog, run a tiny repo-root tsx script that upserts `db.user` (then `rm` it), as done this iteration.
---

## Tue Jun 30 2026 20:30 - US-031 (Layout e dashboard do admin)
- Built the protected admin shell + dashboard.
- Structure decision: nested `(admin)` route group inside the existing `admin/` folder.
  - `src/app/admin/(admin)/layout.tsx` (NEW): `export const dynamic = "force-dynamic"`; async server component calling `await requireRole("EDITOR")` (the single guard for the subtree); renders the admin chrome — gradient "MetaComps Admin" wordmark, `<AdminNav>`, signed-in email + role badge + `<LogoutButton>`; metadata robots noindex.
  - `src/app/admin/(admin)/page.tsx` (NEW): dashboard reading `getAdminDashboardStats()` — shows "Set atual", a "Patch atual" panel (version + releasedAt via UTC parts), three count StatCards (Rascunhos cyan-emphasized / Publicadas / Arquivadas, each linking to /admin/comps?status=…), and "Ir para" quick links (Comps/Catálogo/Patches).
  - `src/app/admin/page.tsx` (DELETED): the US-030 minimal landing; `(admin)/page.tsx` now owns `/admin` (two pages at `/admin` would conflict).
  - `src/components/admin/admin-nav.tsx` (NEW, "use client"): primary nav with active-section highlight via `usePathname`; `flex-wrap` so the 4 links reflow on mobile.
  - `src/server/queries/admin.ts` (NEW): `getAdminDashboardStats()` → `{draftCount, publishedCount, archivedCount, currentSet, currentPatch}`. NOT unstable_cache'd (force-dynamic ⇒ live counts); set-scoped `db.comp.count` per status via Promise.all + SiteConfig.
- Files changed: added `src/app/admin/(admin)/layout.tsx`, `src/app/admin/(admin)/page.tsx`, `src/components/admin/admin-nav.tsx`, `src/server/queries/admin.ts`; deleted `src/app/admin/page.tsx`; updated `src/server/CLAUDE.md`, `prd.json`, `progress.txt`.
- Quality: `rm -rf .next` then `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass.
- Browser verify (Playwright, localhost:3000 with embedded Postgres + seed):
  - Auth guard: curl `/admin` (unauth) → 302 `/admin/login?callbackUrl=%2Fadmin`; `/` and `/admin/login` stay 200.
  - Logged in admin@metacomps.gg/ADMIN → `/admin` dashboard: header (brand + nav w/ Dashboard active, email + ADMIN badge, Sair), Patch atual 17.2 lançado em 15/06/2026, Set atual TFTSet17, counts.
  - Draft count proven LIVE: inserted a temp DRAFT comp → Rascunhos=1 Publicadas=3 Arquivadas=0; removed it afterwards (back to 0).
  - DESKTOP 1280x720: no h-scroll (1280==1280). MOBILE 360x720: nav wraps to 2 rows, cards stack, no h-scroll (360==360) — needed `flex-wrap` on the nav (was 365px, 5px overflow, fixed).
  - Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - See the new "Admin shell / route group (US-031)" Codebase Pattern. Key: put all US-033..US-039 admin pages under `src/app/admin/(admin)/` so they inherit the force-dynamic layout guard; keep `/admin/login` a sibling of the group (never inside it).
  - Route-group name literally IS `(admin)` (folder `src/app/admin/(admin)/`); it reads redundantly but honors the AC "route group (admin)" and keeps `login` public without a top-level `admin` folder collision.
  - Admin data reads should use `src/server/queries/admin.ts` (uncached, live), NOT the public `unstable_cache`'d queries — the whole point of force-dynamic is fresh counts.
  - `rm -rf .next` after deleting/moving a page or the dev server's build vanishes (stale `.next/types` also fails typecheck).
---

## Tue Jun 30 2026 20:41 - US-032 (Componente CatalogPicker autocomplete com icones)
- Built the reusable admin catalog autocomplete and mounted it at /admin/catalog.
- Files changed:
  - NEW `src/components/admin/catalog-picker.tsx`: `<CatalogPicker>` — controlled `"use client"` combobox over a normalized `CatalogOption[]` (`{id,name,iconUrl,meta?}`). Discriminated union on `multiple` gives single (`value:string|null`) and multiple (`value:string[]`) modes, both returning DB row ids via `onChange`. Name search (case-insensitive), next/image icons, accessible combobox (role=combobox/listbox/option, aria-expanded/controls/activedescendant, ↑/↓/Enter/Esc/Backspace-on-empty), removable selection chips, MAX_RESULTS=50 cap + "Refine a busca…" hint.
  - NEW `src/components/admin/catalog-explorer.tsx`: `"use client"` demo host that wires one single-select (Carry principal) + four multi-select pickers (Campeões/Itens/Traits/Augments) and shows the returned ids/counts.
  - NEW `src/app/admin/(admin)/catalog/page.tsx`: server page under the `(admin)` guard; loads the cached `getBuilderCatalog()` bundle, maps each family to `CatalogOption[]` (meta = `Custo N` / pt-BR item family / trait breakpoints / augment tier), renders `<CatalogExplorer>`. Fills the previously-dead dashboard/nav "Catálogo" link.
  - `prd.json` (US-032 passes=true + notes), `progress.txt` (this entry + new Codebase Pattern).
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 38/38 pass.
- Browser (Playwright, logged in as admin@metacomps.gg / admin1234):
  - DESKTOP 1280x720 — single-select: typing "blitz" filtered to "Blitzcrank Custo 5", selecting it showed a chip + real id (`cmr1ank0p...`) + closed the dropdown. Multi-select Itens: "blade"→click Deathblade (chip, count 1, dropdown stayed open, selected hidden from list); "guinsoo"→Enter added Guinsoo's Rageblade (count 2, keyboard selection works); remove-× dropped back to 1. 51/53 icons loaded; no h-scroll (1280==1280); 0 console errors.
  - MOBILE 360x720 — pickers stack legibly; no h-scroll (360==360).
- **Learnings for future iterations:**
  - See the new "CatalogPicker (US-032)" Codebase Pattern. Import `<CatalogPicker>` for EVERY catalog relation in US-034..037 (single for carry/patch, multiple for trait/unit/item/augment lists) rather than building per-field pickers.
  - GOTCHA (lint gate): an array derived from `props.value` inside a component (`selectedIds`) MUST be `useMemo`'d — otherwise `react-hooks/exhaustive-deps` warns on the downstream `selectedSet` memo and the zero-warning lint gate fails. First fix compiled but WARNED; wrapping in `useMemo([props.multiple, props.value])` cleared it.
  - 2 of 53 catalog icons are broken (dead CDN URLs for junk "Outro" seed items 2-cost Pyke/Zoe that reuse champion-square asset paths) — a US-009 seed-classification artifact, NOT a picker bug. If a later story tightens the seed's item filter, those disappear.
  - The Playwright MCP auto-screenshot after each action throws `EACCES mkdir /workspace/screenshots`, but the ACTION still succeeds — ignore the error and re-`browser_snapshot` to read state. Explicit `browser_take_screenshot` works with a RELATIVE filename (lands in repo root; rm before staging).
---

## Tue Jul 01 2026 04:50 - US-033 (Listagem de comps no admin)
- Built the admin comps management list at `/admin/comps` with status filtering and create/edit links.
- Files changed:
  - `src/server/queries/admin.ts` (NEW `getAdminComps(status?)` + `AdminCompRow` type): a lean `Prisma.validator<Prisma.CompSelect>()` projection (id/slug/name/tier/status/situational/updatedAt + `patchIntroduced/patchUpdated.version`), set-scoped to `SiteConfig.currentSet` (falls back to all-sets), `orderBy updatedAt desc`, NOT `unstable_cache`'d (admin is force-dynamic — rows must be live, same rule as `getAdminDashboardStats`).
  - `src/app/admin/(admin)/comps/page.tsx` (NEW): the list page. Header with count + `Nova comp` → `/admin/comps/new`; a `Filtrar por status` tab nav (Todas / Rascunhos / Publicadas / Arquivadas) whose hrefs set `?status=draft|published|archived` (same tokens the dashboard StatCards deep-link with) and marks the active tab `aria-current="page"`; each comp is a bordered `<li>` with name + `/slug`, a tier chip (`TIER_META.chipClass` + `text-background`), a status badge, patch info (introduced version + `atualizado <updated>` when patchUpdated set), a md+-only last-edit date, and an `Editar` link → `/admin/comps/[id]`. Empty state + `Criar a primeira comp` link.
  - `src/server/CLAUDE.md`: documented `getAdminComps` on the `admin.ts` bullet.
  - `prd.json` (US-033 passes=true + notes), `progress.txt`.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 38/38 pass.
- Browser (Playwright, admin@metacomps.gg): inserted 1 temp DRAFT + 1 temp ARCHIVED comp so all three statuses have coverage (deleted after; DB back to 3 PUBLISHED). DESKTOP 1280x720 — /admin/comps shows all 5 with correct tier/status/patch (Meeple Jhin = "Patch 17.1 atualizado 17.2"), Nova comp → /admin/comps/new, Editar → /admin/comps/<id>; ?status=draft → "1 composição · Rascunho" + only the draft + "Rascunhos" tab aria-current; ?status=published → "3 composições · Publicada" + the 3 published + "Publicadas" active; no h-scroll (1280==1280); 0 console errors. MOBILE 360x720 — names full-width on their own line, metadata wraps below, no h-scroll (360==360).
- **Learnings for future iterations:**
  - See the new "Admin comps list (US-033)" Codebase Pattern. US-034 builds the `/admin/comps/new` + `/admin/comps/[id]` form routes those links point at (currently 404, which the AC allows).
  - RESPONSIVE GOTCHA (now in patterns): a `flex-1` primary-text div next to `shrink-0` chips in a `flex-wrap` row collapses to 0 width at 360px and the text disappears — use `basis-full sm:basis-0 sm:flex-1` so it takes its own line on mobile. Caught this in browser verification; the a11y snapshot listed all names but the SCREENSHOT + a `getBoundingClientRect().width` probe (was ~0, now 294) revealed the squeeze.
  - To verify status-filter coverage without seeded drafts/archived, insert temp `Comp` rows (only `slug/name/tier/status/set/patchIntroducedId` required — everything else has DB defaults) via a throwaway `tsx` script, screenshot, then `deleteMany` by slug (cascades children). `getAdminComps` is set-scoped, so give temp rows the current `SiteConfig.currentSet`.
  - The embedded Postgres (US-004 pattern) can be left running from a prior iteration — a stale `postmaster.pid` makes a fresh `start-pg.js` FATAL with "lock file already exists (PID N running)"; check `ps -p N` first — if that PID is alive, PG is already up and you don't need to restart it (my `/dev/tcp` port probe was flaky and falsely reported DOWN).
---

## Tue Jul 01 2026 05:15 - US-034 (Form de comp: campos basicos e guia — criar/editar)
- Built the admin comp base-fields form (create + edit) with server-action persistence and stable/unique slug generation.
- New files:
  - `src/lib/slug.ts` — pure/client-safe `slugify()` (mirrors the seed's algorithm) so the form previews the SAME slug the action canonicalizes on write.
  - `src/actions/comps.ts` (`"use server"`) — `createComp(input)` / `updateComp(id, input)` returning a discriminated `CompActionResult` (`{ok:true;id;slug} | {ok:false;error}`). Both `await requireRole("EDITOR")` first, validate enums/required fields, resolve a unique slug (`resolveUniqueSlug` loops `-2/-3/…`), stamp `publishedAt` on transition to PUBLISHED, and null out blank optional text. `createComp` derives `set` from `patchIntroduced.set` (also validates the patch). Also exports `CompFormInput`.
  - `src/components/admin/comp-form.tsx` (`"use client"`) — controlled `<CompForm patches comp?>`; slug auto-derives from name until the slug field is touched (edit mode starts touched); all AC fields present (name, slug, tier, situational, status, playstyle, difficulty, patchIntroduced/patchUpdated, whenToPlay/earlyGame/midGame/lateGame/tips). Awaits the action, `router.push`+`refresh` on success (create → the new comp's edit page; edit → the list), inline error on failure. Mirrors the login-form pattern (no `useActionState` in React 18.3).
  - `src/app/admin/(admin)/comps/new/page.tsx` + `src/app/admin/(admin)/comps/[id]/page.tsx` — server pages under `(admin)` (inherit force-dynamic guard/shell); load patches (+ the comp for edit), 404 on unknown id, render `<CompForm>`.
- Changed files:
  - `src/server/queries/admin.ts` — added `getAdminCompById(id)` → `AdminCompEdit | null` (base + guide select via `Prisma.validator`) and `getPatches()` → `PatchOption[]` (`{id,version}`, newest first).
  - `src/server/CLAUDE.md` (admin.ts bullet) + NEW `src/actions/CLAUDE.md` (server-action conventions).
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass.
- Browser (Playwright, logged in admin@metacomps.gg; DB restored to 3 comps + screenshots removed after): DESKTOP 1280x720 — `/admin/comps/new` renders every AC field; typing "Teste Anima Sniper" in Nome auto-fills slug "teste-anima-sniper" (live "/comps/…" preview); selecting Tier S + patch 17.2 + guide text and clicking "Criar comp" CREATES the comp and navigates to `/admin/comps/<id>` (edit page) pre-filled with the saved values (button now "Salvar alterações"); DB verified: `set=TFTSet17` (from patch), tier S, DRAFT, playstyle/whenToPlay/earlyGame saved, empty fields NULL, publishedAt null. Editing tier S→A + status Rascunho→Publicada + adding midGame and saving returns to the list (now 4 comps, "Teste Anima Sniper" tier A / Publicada at top) and DB shows tier A, PUBLISHED, midGame saved, publishedAt STAMPED. Slug uniqueness proven: a 2nd comp with the SAME name got slug `teste-anima-sniper-2`. 0 console errors on the form. MOBILE 360x720 — grids collapse to one column, slug preview shows, no h-scroll (360==360).
- **Learnings for future iterations:**
  - A `"use server"` action is a public POST endpoint — ALWAYS `await requireRole(...)` inside it, not just on the page. It can also export the input `interface`/`type`, which the `"use client"` form imports directly (the sanctioned client→Prisma bridge; never import `@/server/db`/queries into a client component).
  - React 18.3 has NO `useActionState` — use the login-form shape: `"use client"` + `useState`, await the action, handle its `{ok}` result, then `router.push`+`router.refresh()`. Return a discriminated result rather than throwing so validation errors render inline.
  - Slug stability: reuse `slugify` from `@/lib/slug` (pure, client-safe) in BOTH the form preview and the action so they can't diverge; loop `-N` on `findUnique({where:{slug}})` collisions (`excludeId` for updates). Empty optional text should be stored as `null`, not `""`.
  - Next routing: a STATIC segment (`comps/new`) takes precedence over a sibling DYNAMIC segment (`comps/[id]`), so `/admin/comps/new` correctly renders the create page (never `[id]="new"`). No config needed.
  - A comp's `set` should follow its `patchIntroduced.set` (fetch the patch — doubles as existence validation) so it stays aligned to a current-set patch without an extra form field. `updateComp` leaves `set` alone.
  - The Playwright MCP throws `EACCES: mkdir '/workspace/screenshots'` on many actions (navigate/click/select), but the action STILL RUNS — verify with a follow-up `browser_snapshot`/`browser_evaluate` instead of trusting the error. Explicit `browser_take_screenshot` with a RELATIVE filename works (lands in repo root — `rm` before staging).
  - Admin is already logged-in-able in this env: `admin@metacomps.gg` / `admin1234` (from `.env` `ADMIN_EMAIL`/`ADMIN_PASSWORD`, seeded by `seedAdminUser`); the dev server on :3000 + embedded Postgres on :5432 were already up.
---

## Tue Jun 30 2026 21:20 - US-035 (Form de comp: traits e listas de unidades early/flex)
- Added the comp composition editor (traits with level+order, and off-board EARLY/FLEX unit lists) to the admin edit page, persisted by a new server action.
- What was implemented:
  - `src/components/admin/comp-composition-form.tsx` (NEW, "use client"): `<CompCompositionForm compId champions traits initial>`. Traits section = a SINGLE-mode `<CatalogPicker value={null}>` used as a pure "add" control (options filtered to exclude already-added traits) + an ordered `<ol>` of trait rows, each with icon, a `level` number input (seeded from the trait's first breakpoint), up/down reorder buttons, and remove. EARLY + FLEX unit lists = two MULTIPLE-mode `<CatalogPicker>`s (add/remove chips, selection order = order). "Salvar composição" awaits the action, shows inline error / "Composição salva." status; maps the builder catalog to `CatalogOption` via `useMemo` (champion meta "Custo N", trait meta "Ativa em a / b").
  - `src/actions/comps.ts`: added `updateCompComposition(compId, {traits, earlyUnitIds, flexUnitIds})` + `CompTraitInput`/`CompCompositionInput` types. `requireRole("EDITOR")` first; dedupes ids; validates integer level>=1; validates every trait/champion id belongs to the comp's OWN `set`; then in a `db.$transaction` deleteMany+createMany replaces `CompTrait` (order=index) and replaces ONLY `role IN (EARLY,FLEX)` `CompUnit`s (order=index across early then flex). CORE board units are left untouched (US-037's builder owns them).
  - `src/server/queries/admin.ts`: added `getAdminCompComposition(id) -> AdminCompComposition` ({ traits:{traitId,level}[], earlyUnitIds, flexUnitIds }, CORE excluded) + `AdminCompTraitRow` type; imported `UnitRole` as a value.
  - `src/app/admin/(admin)/comps/[id]/page.tsx`: fetches composition + `getBuilderChampions(comp.set)` + `getBuilderTraits(comp.set)` (scoped to the comp's set) and renders `<CompCompositionForm>` below `<CompForm>`.
  - Updated `src/actions/CLAUDE.md` and `src/server/CLAUDE.md`.
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass.
- Browser verify (Playwright, admin@metacomps.gg, localhost:3000, 1280x720 + 360x720):
  - READ: /admin/comps/<n-o-v-a-blitzcrank id> loaded the saved composition — traits N.O.V.A. lvl3 / Anima 2 / Challenger 2 / Conduit 2 (each with a level input + ↑/↓ reorder, ↑ disabled at top / ↓ at bottom), Early = Aatrox/Briar/Caitlyn, Flex = Cho'Gath/Akali.
  - WRITE: seeded a temp DRAFT comp (`temp-us035`) with 1 trait, 1 EARLY unit, and 1 CORE Bel'Veth carry (★3, hex 3,3, item Edge of Night). In the UI: God-Blessed level -> 4, added Arbiter (default lvl 2), added Akali (Early) + Aurora (Flex), Salvar composição -> "Composição salva." DB verified: traits God-Blessed(lvl4,order0)+Arbiter(lvl2,order1); EARLY Aatrox(0)/Akali(1); FLEX Aurora(2); CORE Bel'Veth carry + Edge of Night UNTOUCHED.
  - 0 console errors; no horizontal scroll at 1280 (1280==1280) or 360 (360==360). Temp comp deleted (DB back to 3 comps); screenshots removed.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns lines (comp composition editor + Playwright MCP EACCES workaround). Key reuse for US-036: a SINGLE-mode `<CatalogPicker value={null}>` is the clean "add" primitive when each chosen row needs extra fields (like a level or a priority order); a MULTIPLE-mode picker is the whole editor when it's just an ordered set. Server-action replace-all lives in a `db.$transaction` (deleteMany+createMany, order=index) — and role/scope the delete so you don't clobber a sibling collection (here CORE units belong to US-037).
  - The comp's composition editor only renders in EDIT mode (needs a comp id); `createComp` already redirects to `/admin/comps/[id]` so the flow is: create base fields -> land on edit page -> fill traits/units. US-036 (item/augment priority) should slot another section into this same edit page below the composition form.
---

## Mon Jun 30 2026 21:30 - US-036 (Form de comp: prioridade de itens e de augments)
- Added the item-priority + augment-category-priority editor to the comp edit page.
- What was implemented:
  - NEW `src/components/admin/comp-priority-form.tsx` (`"use client"`): item-priority editor (SINGLE-mode `<CatalogPicker value={null}>` add control + ordered `<ol>` with ↑/↓/× reorder) and augment-category editor (ordered `<ol>` of Economia/Itens/Combate with ↑/↓/× + `+ <Category>` add buttons). Reuses the US-035 add-picker/reorder pattern; category labels mirror the public `comp-augments.tsx` for parity.
  - NEW server action `updateCompPriority(compId, { itemIds, augmentPriority })` in `src/actions/comps.ts`: `requireRole("EDITOR")`, dedupe+validate itemIds vs the comp's own set, validate categories vs the `AugmentCategory` enum (dedupe keeping first order), then ONE `db.$transaction` = `deleteMany`+`createMany` `CompItemPriority` (order=index) + `db.comp.update` overwriting the scalar `augmentPriority`. Only touches those two — per-unit `CompUnitItem` and `CompAugment` (builder's, US-037) are untouched.
  - NEW query `getAdminCompPriority(id)` → `AdminCompPriority` (`{ itemIds, augmentPriority }`) in `src/server/queries/admin.ts` (not cached / not set-scoped — admin is force-dynamic).
  - Wired `src/app/admin/(admin)/comps/[id]/page.tsx` to fetch `getAdminCompPriority(comp.id)` + `getBuilderItems(comp.set)` and render `<CompPriorityForm>` below `<CompCompositionForm>`.
- Files changed: src/actions/comps.ts, src/server/queries/admin.ts, src/components/admin/comp-priority-form.tsx (new), src/app/admin/(admin)/comps/[id]/page.tsx, src/actions/CLAUDE.md, src/server/CLAUDE.md, prd.json, progress.txt.
- Quality: `npm run typecheck` exit 0; `npm run lint` "No ESLint warnings or errors"; `npm test` 38/38 pass.
- Browser (Playwright, admin login, localhost:3000): seeded a throwaway `temp-us036` DRAFT comp (1 pre-set item priority, augmentPriority=[ITEMS], a CORE Aatrox carry ★2 hex(1,2) with item Cho'Gath, 1 CompAugment). READ path loaded the pre-set item + "Itens" category. WRITE path: added Economia+Combate categories, searched+added item Briar, reordered Briar>Aatrox and Combate>Economia, Salvar → "Prioridade salva.". DB verified `itemPriority=[0:Briar,1:Aatrox]`, `augmentPriority=[ITEMS,COMBAT,ECON]`, and the CORE carry+item + the CompAugment SURVIVED. No h-scroll at 1280 or 360. Temp comp deleted (DB back to 3); throwaway scripts + screenshot removed.
- **Learnings for future iterations:**
  - `augmentPriority` is a `Comp` SCALAR column (`AugmentCategory[]`), not a join table — overwrite it with a plain `db.comp.update`, unlike the `deleteMany`+`createMany` join-table replaces.
  - `AugmentCategory`/`ItemType` are enums usable as `import type` in `"use client"` code; pin their pt-BR labels + display order in local `Record<>`/`readonly []` literals (reuse the exact labels from `comp-augments.tsx` so admin ⇄ public stay in parity).
  - The US-036 priority save deliberately does NOT clobber per-unit `CompUnitItem` or the recommended `CompAugment` list — that separation is what lets US-037's builder own the CORE board/items/augments independently. Proved with a temp comp that a CORE carry's item + a CompAugment survive.
  - Env was already provisioned this session (embedded PG on :5432 + `next dev` on :3000) — verify with `ps`/`curl` before re-spinning either.
---

## Tue Jun 30 2026 21:40 - US-037 (Admin: editar a comp via Builder / board final)
- Built the admin final-board editor by REUSING the public `<Builder>` (not a fork) at `/admin/comps/[id]/build`, wired to a new `saveCompBoard` server action that persists CORE units + items + augments with parity to the public comp page.
- Files changed:
  - `src/lib/builder.ts` — added REQUIRED `isCarry: boolean` to `PlacedUnit` (like US-026's `stars`); updated docstring.
  - `src/lib/board-code.ts` — `SharedUnit = Omit<PlacedUnit,'id'|'isCarry'>` (share code UNCHANGED); `toPlacedUnits` adds `isCarry:false`.
  - `src/lib/builder.test.ts` — test `unit()` helper adds `isCarry:false`.
  - `src/components/builder/builder.tsx` — added optional `onSave` prop + `BuilderSaveResult` export; admin save mode: carry toggle, "Salvar board" button, save-status line, URL-mirroring skipped + "Copiar link" hidden when `onSave` present; `placeChampion` sets `isCarry:false`; `toggleSelectedCarry`/`handleSave` handlers (carry edit undoable via `commit()`).
  - `src/components/builder/builder-board.tsx` — amber "C" carry badge + ", carry" in the hex aria-label when `unit.isCarry`.
  - `src/components/admin/admin-builder.tsx` (NEW) — `"use client"` wrapper embedding `<Builder>`, bridging `onSave` → `saveCompBoard(compId,…)` + `router.refresh()`.
  - `src/actions/comp-builder.ts` (NEW) — `saveCompBoard(compId, {units, augmentIds})`: guard, validate ids vs comp.set + clamp positions/stars/items, one `$transaction` replacing ONLY role:CORE CompUnits (nested CompUnitItem create + carryOrder counter) and CompAugments.
  - `src/server/queries/admin.ts` — `getAdminCompBoard(id)` → `{ units:{championId,row,col,stars,isCarry,items[]}[], augmentIds[] }` (CORE on-board only).
  - `src/app/admin/(admin)/comps/[id]/build/page.tsx` (NEW) — build route; fetches board + `getBuilder*(comp.set)`, maps to `PlacedUnit[]` (deterministic ids), sanitizes vs catalog, renders `<AdminBuilder>`.
  - `src/app/admin/(admin)/comps/[id]/page.tsx` — added an "Editar board no Builder" link.
  - Docs: `src/actions/CLAUDE.md`, `src/server/CLAUDE.md`, progress.txt Codebase Patterns.
- Quality: `rm -rf .next` then `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass.
- Browser (Playwright, localhost:3000, admin login admin@metacomps.gg): created a throwaway PUBLISHED comp (CORE Teemo carry 2★ + item "Edge of Night", EARLY Nasus, augment Thresh's Boon), opened the build page → existing board loaded EXACTLY (Teemo hex ★★ + "C" badge + aria "Teemo, 2 estrelas, carry", augment picker 1/3, "Salvar board" shown / no "Copiar link"). Placed Aatrox, marked carry, set 3★, equipped "Adaptive Helm", added "Ahri's Boon", saved. DB read confirmed: CORE Teemo(carry/carryOrder0/2★/hex1,2/item preserved) + CORE Aatrox(carry/carryOrder1/3★/hex0,0/Adaptive Helm) + EARLY Nasus SURVIVED + augments Thresh's/Ahri's ordered. PARITY: public /comps/temp-us037 rendered the identical board (Carries Teemo★★/Aatrox★★★, Posicionamento Aatrox top-left + Teemo below, both items, both augments); 0 console errors. Throwaway comp + tsx scripts + screenshot deleted before staging.
- **Learnings for future iterations:**
  - See the new "Admin final board = REUSE the public `<Builder>`" Codebase Pattern — the key moves are the optional `onSave` prop, the thin client wrapper as the server-component→client-prop bridge, and keeping the US-028 share code unchanged by omitting `isCarry` from `SharedUnit`.
  - CRITICAL scope symmetry with US-035/036: `saveCompBoard` deletes ONLY `role: CORE` CompUnits (+ all CompAugments), so EARLY/FLEX units, CompTrait and CompItemPriority survive — mirrors the composition/priority actions scoping to their own rows. Deleting a CompUnit cascades its CompUnitItems, so a plain replace works. Nested `items:{create:[…]}` must go through `tx.compUnit.create` in a loop (createMany can't nest).
  - Adding a REQUIRED field to `PlacedUnit` touches exactly 3 construction sites (builder.tsx placeChampion, board-code.ts toPlacedUnits, builder.test.ts helper); all the move/star/item handlers already spread `{...u}` so they preserve new fields for free.
  - Browser-testing the admin builder: React needs a re-render between clicks, so `browser_evaluate` one `.click()` per call (arm→place→disarm→select→toggle→save as SEPARATE calls); reading `button[aria-label]` hex labels via evaluate is a reliable way to assert board state without giant snapshots. Public comp ISR cache (`comp:<slug>` tag) isn't revalidated until US-038, but a FRESH comp's public page is a cache-miss on first visit so it shows current data.
---

## Tue Jun 30 2026 21:55 - US-038 (Publicação: validação + revalidação ISR)
- Implemented validated publish/unpublish/archive server actions + ISR revalidation, and made a comp's moderation status owned exclusively by them (not the base form).
- What was implemented:
  - `src/actions/comps.ts`: added `publishComp(id)` / `unpublishComp(id)` / `archiveComp(id)` (all `requireRole("EDITOR")`, all return the shared `CompActionResult`). `publishComp` enforces FR-20 via a new `collectPublishBlockers(comp)` helper (name, tier, ≥1 trait, ≥1 carry, non-empty final board = ≥1 on-board CORE unit) and only then sets `PUBLISHED` (stamps `publishedAt` first time). All three call a shared `revalidateComp(slug)` = `revalidateTag("tierlist")` + `revalidateTag("comp:"+slug)`.
  - Removed `status` from `CompFormInput`/`baseData`/`validateInput` and dropped the `STATUSES` const + unused `CompStatus` import. `createComp` now always creates a DRAFT (schema default); `updateComp` no longer writes `status`/`publishedAt` but now revalidates the public caches when the edited comp `existing.status === "PUBLISHED"` (so guide/tier edits to a live comp reflect on the site).
  - `src/components/admin/comp-form.tsx`: removed the Status `<select>` + `STATUS_OPTIONS` + `status` from `initialValues` (status is now driven by the lifecycle controls).
  - NEW `src/components/admin/comp-status-controls.tsx` (`"use client"`): the "Publicação" section — status badge + FR-20 helper + context-aware buttons (Publicar / Despublicar / Arquivar / Voltar para rascunho) + "Ver página pública ↗" when published; inline blocker error/success; local `useState` status flipped optimistically + `router.refresh()`.
  - `src/app/admin/(admin)/comps/[id]/page.tsx`: renders `<CompStatusControls>` between the header and `<CompForm>`.
- Files changed: src/actions/comps.ts, src/components/admin/comp-form.tsx, src/components/admin/comp-status-controls.tsx (new), src/app/admin/(admin)/comps/[id]/page.tsx, src/actions/CLAUDE.md, progress.txt, prd.json.
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass (no `.next` wipe needed — no page moved/deleted).
- Browser (Playwright, localhost:3000, 1280x720, admin login admin@metacomps.gg):
  - Edit page (Dark Star Master Yi, PUBLISHED) → "Publicação" shows "Publicada" badge + "Ver página pública ↗" + Despublicar/Arquivar; the base form no longer has a Status field.
  - Despublicar → badge "Rascunho", notice "Comp despublicada"; public `/` dropped from 3→2 comps (Dark Star gone) AND cleaned up a STALE phantom "Temp US037" comp (deleted from DB in US-037 but never revalidated) — `revalidateTag` regenerated the `tierlist` cache from the live DB; `fetch('/comps/dark-star-master-yi')` → 404 (comp tag revalidated).
  - Publicar (complete comp) → validation passed, badge "Publicada", notice "Comp publicada"; public `/` back to 3 comps with Dark Star in B Tier.
  - New comp "US-038 Publish Test" created as DRAFT (confirms createComp→DRAFT) → Publicar BLOCKED with alert "Não é possível publicar. Faltam: ao menos 1 sinergia, ao menos 1 carry, composição final no board." (status stayed Rascunho). Arquivar → badge "Arquivada" + "Voltar para rascunho".
  - 0 unexpected console errors (only the documented favicon 404). Throwaway comp + tsx scripts + screenshot deleted before staging; DB restored to 3 published / 0 draft / 0 archived.
- **Learnings for future iterations:**
  - Owning `status` in the lifecycle actions (not the form) is the clean way to enforce FR-20 AND dodge the "server `router.refresh()` doesn't reinit a client component's `useState`, so a stale status gets written back on the next form save" trap. If US-039/US-040 add more status-ish state, keep it out of the base form.
  - `revalidateTag` regenerates the `unstable_cache` entry from the LIVE DB — it doubles as a cleanup for anything deleted directly from the DB while the cache was stale. Conversely, deleting a comp straight from the DB does NOT refresh the tier list; only the actions revalidate. A DRAFT/ARCHIVED throwaway is safe to hard-delete because it was never on the public list.
  - The public tier list at `/` and comp pages are served from `unstable_cache` (tags `tierlist` / `comp:<slug>`), so ISR staleness is real — always drive public-facing changes through an action that revalidates, or the change won't show for up to `revalidate=3600`.
  - Playwright MCP's auto-screenshot-on-action tries to `mkdir /workspace/screenshots` and throws EACCES on EVERY navigate/click/type — the ACTION still succeeds; just call `browser_snapshot`/`browser_evaluate` afterward to read state. Explicit `browser_take_screenshot` works only with a RELATIVE `filename` (lands in repo root; delete before staging).
---

## Tue Jun 30 2026 22:05 - US-039 (Fluxo de patch: carimbo + snapshots + badges)
- Implemented the admin patch flow: create patches, define the site's current patch, and stamp per-patch tier history — the last write-side story that touches `SiteConfig`/badges.
- Files added:
  - `src/actions/patches.ts` — `"use server"`: `createPatch({version,releasedAt,set})` and `setCurrentPatch(patchId)`; shared `PatchActionResult` (`{ok:true;id;snapshotCount?} | {ok:false;error}`). Both `requireRole("EDITOR")` first. `createPatch` validates non-empty version/set + a `yyyy-mm-dd` date (`parseIsoDate` → UTC midnight) and rejects duplicate `version` before the DB `@@unique` throws. `setCurrentPatch` snapshots the OUTGOING patch's tier for every PUBLISHED comp of the outgoing set (upsert on `compId_patchId`) then repoints `SiteConfig` (id:1, currentSet follows patch.set) in one `$transaction`, then `revalidateTag("tierlist")`.
  - `src/app/admin/(admin)/patches/page.tsx` — server page (inherits the `(admin)` force-dynamic guard/shell). Current-patch summary + `<PatchForm>` + `<PatchList>`; pre-formats `releasedAt` to a UTC DD/MM/YYYY string so the client list takes no Date props.
  - `src/components/admin/patch-form.tsx` — `"use client"` create form (version/date/set; set pre-filled with currentSet). `useState` + await action + `router.refresh()`.
  - `src/components/admin/patch-list.tsx` — `"use client"` list with a per-row "Definir como atual" button; `current` in local `useState` is the source of truth for the "Patch atual" badge, flipped optimistically. Exports `PatchListRow`.
- Files changed:
  - `src/server/queries/admin.ts` — added `getAdminPatchesData()` → `AdminPatchesData` (patches with `_count.compsIntroduced` + `isCurrent`, `currentPatchId`, `currentSet`, `publishedCompCount`); not-cached (admin is force-dynamic). `getPatches()` (US-034) left untouched.
  - `src/actions/CLAUDE.md`, `src/server/CLAUDE.md` — documented the patch flow + `getAdminPatchesData`.
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass.
- Browser (Playwright, localhost:3000, 1280x720, admin@metacomps.gg login):
  - `/admin/patches` initial: Patch atual 17.2 (15/06/2026 · TFTSet17); create form; list 17.2 (Patch atual, 1 comp) + 17.1 (Definir como atual, 2 comps); helper "Ao trocar o patch atual, 3 comps publicadas terão o tier carimbado no patch anterior."
  - Created patch 17.3 (29/06/2026, TFTSet17) → "Patch 17.3 criado." + new row at top (0 comps).
  - "Definir como atual" on 17.3 → summary flips to 17.3, badge moves, notice "Patch 17.3 definido como atual. 3 comps carimbadas no patch anterior." DB verified: `SiteConfig.currentPatchId`=17.3 + 3 `CompTierSnapshot` pinned to 17.2 (N.O.V.A. Blitzcrank=S, Meeple Jhin=A, Dark Star Master Yi=B).
  - Public `/` after switch: header "Patch 17.3" + NO Novo/Atualizado badges on any card (badges re-derived; route still "Static route"). Confirms revalidateTag("tierlist") refreshes the badges.
  - Restored 17.2 via the UI → public `/` header "Patch 17.2" + N.O.V.A. Blitzcrank shows "Novo" and Meeple Jhin shows "Atualizado" again. Round-trips the whole flow.
- Cleanup: deleted the throwaway patch 17.3 (cascades its snapshots) + all `CompTierSnapshot`s; DB restored to seeded state (patches 17.1/17.2, current 17.2, 0 snapshots). No screenshot/temp-script artifacts left.
- **Learnings for future iterations:**
  - One `revalidateTag("tierlist")` covers BOTH the tier list and every comp page for a badge refresh, because the comp-detail `unstable_cache` entries (US-017) carry the `tierlist` tag alongside `comp:<slug>`. Don't loop over comp slugs to revalidate a global patch change.
  - The badge-driving `currentPatchId` is read INSIDE the `tierlist`-tagged cache (`getTierListData` in `src/app/(public)/page.tsx`), so changing `SiteConfig.currentPatchId` only shows publicly after a `tierlist` revalidate — always switch the patch through `setCurrentPatch`, never a raw DB write.
  - `CompTierSnapshot` cascades on patch delete — deleting a throwaway test patch also removes its snapshots, which keeps browser-test cleanup simple.
  - Snapshot ONLY when there's a real, different outgoing patch (`previousPatchId !== null && previousPatchId !== patchId`); the first-ever set or re-selecting the same patch has nothing to freeze.
---

## 2026-06-30 - US-040 (Preview de draft, noindex)
- Added the curator draft-preview route `/admin/preview/[id]` so drafts can be reviewed exactly as they'd appear published, before publishing.
- Files changed:
  - `src/app/admin/(admin)/preview/[id]/page.tsx` (NEW) — preview page under the `(admin)` group (inherits the `requireRole` guard + force-dynamic shell). Renders the comp via the SAME comp-detail section components as the public `/comps/[slug]` page, wrapped in a "Preview interno" banner (status badge + "Voltar para edição"). Sets `robots: { index:false, follow:false }`.
  - `src/server/queries/comp.ts` — exported `compDetailInclude` (was a local const) so the preview query can reuse the exact `CompDetail` include shape the section components expect.
  - `src/server/queries/admin.ts` — added `getAdminCompForPreview(id)` = `db.comp.findUnique({ where:{id}, include: compDetailInclude })` (no status filter → drafts resolve; not cached — admin force-dynamic).
  - `src/app/admin/(admin)/comps/[id]/page.tsx` — added a "Ver preview" link next to "Editar board no Builder".
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 38/38 pass.
- Browser (Playwright, localhost:3001, admin@metacomps.gg): unauth `/admin/preview/<id>` → 302 to `/admin/login?callbackUrl=…` (auth-only). After login the page rendered the full draft comp (header/NOVO badge, carries, item priority, board, units, augments+priority, guide with name highlighting; 38 icons, 0 broken; OpenInBuilder link present), `<meta name=robots>`=`noindex, nofollow`, banner "Preview interno" + "Rascunho". No h-scroll at 1280 or 360. Only console error = expected favicon 404. Draft-exclusion verified by temporarily flipping the S comp to DRAFT: tier list `/` and `/sitemap.xml` both excluded it, `/comps/<draft-slug>`=404 while published `/comps/meeple-jhin`=200; comp restored to PUBLISHED afterward.
- **Learnings for future iterations:**
  - To render a comp exactly like the public page from a NON-public context (admin preview), reuse the public `compDetailInclude` + `CompDetail` type from `src/server/queries/comp.ts` (now exported) and the `src/components/comp-*` section components — DON'T rebuild the layout. Just drop the `status: "PUBLISHED"` filter in the query (`findUnique({where:{id}})`).
  - Any page placed under `src/app/admin/(admin)/**` is auth-gated for free (the group layout's `requireRole("EDITOR")`) and inherits the layout's `robots: noindex,nofollow` — but set `robots` on the page too when an AC explicitly requires noindex.
  - Drafts are ALREADY excluded from every public surface (tier list, sitemap, `/comps/[slug]`) by the `status: "PUBLISHED"` filters in `src/server/queries/*` — a preview feature needs NO changes there. Verify it by flipping a comp's status directly in the DB (temp `tsx` script), curling `/`, `/sitemap.xml`, `/comps/<slug>`, then flipping it back.
  - GOTCHA (env, embedded-postgres): NEVER `rm` `pgdata/postmaster.pid` while Postgres is running — Postgres watches that file and SELF-TERMINATES within seconds when it disappears (looked like a flaky DB). If a "lock file already exists" error appears, first `pgrep -f "bin/postgres -D /tmp/pgscratch"` — if a live postmaster owns it, the pid file is NOT stale; only remove it after `pkill`-ing the process. The `/tmp/claude-*/tasks/<id>.output` file of a `run_in_background` Bash task is the reliable place to poll for `PG_READY`/dev-server "Ready" (grep it in a bounded loop).
---

## Tue Jun 30 2026 22:40 - US-041 (Analytics e monitoramento de erros)
- Added a dependency-free, provider-agnostic, production-gated, privacy-first analytics + error-monitoring layer (chose NOT to install `@sentry/nextjs` — "ex: Sentry" is an example, and its heavy webpack-plugin/OpenTelemetry deps conflict with the minimal/focused mandate; the layer is Sentry-ready via one env var).
- New files:
  - `src/lib/analytics.ts` — client-safe. Cookieless Plausible via a `window.plausible` command-queue stub; `track(name, props)` no-ops unless `NEXT_PUBLIC_ANALYTICS_DOMAIN` set AND consent granted (localStorage `metacomps.analytics-consent`); `ANALYTICS_EVENTS` map (pageview/filter_apply/comp_open/builder_open); consent + enable helpers.
  - `src/lib/error-monitoring.ts` — client-safe. `captureError`/`toCapturedError`; POSTs via `navigator.sendBeacon` (fetch keepalive fallback) only in production with `NEXT_PUBLIC_SENTRY_DSN`|`NEXT_PUBLIC_ERROR_REPORTING_URL`; else `console.error`.
  - `src/components/analytics/analytics-provider.tsx` — root-layout `"use client"` provider: consent state + `<ConsentBanner>` + provider `<Script>` + `<PageviewTracker>` (usePathname, no Suspense, stays Static). Returns null when analytics unconfigured.
  - `src/components/analytics/consent-banner.tsx` — pt-BR "sem cookies" bottom banner (Recusar/Aceitar).
  - `src/components/analytics/track-event.tsx` — generic mount-once event tracker (`useRef` guard) for server pages.
  - `src/components/analytics/builder-open-link.tsx` — client anchor emitting `builder_open` on click.
  - `src/components/error-monitor.tsx` — root-layout window `error`/`unhandledrejection` listeners.
  - `src/app/error.tsx` + `src/app/global-error.tsx` — App Router boundaries that report on mount + pt-BR retry.
  - `src/lib/analytics.test.ts` + `src/lib/error-monitoring.test.ts` — node:test (+7 → 45 total).
- Edited: `src/app/layout.tsx` (mount `<AnalyticsProvider/>` + `<ErrorMonitor/>`), `src/components/tier-list-filters.tsx` (fire `filter_apply` in `toggleValue`), `src/components/open-in-builder.tsx` (server encode → render `<BuilderOpenLink>`), `src/app/(public)/comps/[slug]/page.tsx` (`<TrackEvent>` for `comp_open`), `.env.example` (4 new NEXT_PUBLIC vars).
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 45/45 pass.
- Browser (Playwright, localhost:3000, temp analytics env + a `public/_analytics-test-stub.js` capture stub, both reverted/removed after): (1) analytics DISABLED default → tier list unchanged, "Static route" preserved, 0 scripts/banner in HTML; (2) with domain set + consent cleared → banner shows (pt-BR "sem cookies", Recusar/Aceitar) and PRE-consent `window.plausible` is undefined / no script; (3) Aceitar → localStorage "granted", banner dismissed, script loads, pageview fires; (4) Recusar → "denied", banner dismissed, NOTHING loads; (5) events fired with correct props — `filter_apply` {type,value,active}, `comp_open` {slug,tier}, `builder_open` {slug,from}; (6) ErrorMonitor captured a synthetic window error (`[error-monitoring]` console log, source `window.onerror`).
- **Learnings for future iterations:**
  - The AC did NOT list "Verify in browser" (unlike US-040/US-042) — this is an instrumentation story — but I browser-verified anyway since it adds a consent banner + error UI.
  - `NEXT_PUBLIC_*` env vars are inlined at COMPILE time; changing `.env` requires a FULL dev-server restart (HMR alone won't pick them up). `rm -rf .next` first for a clean reload.
  - Playwright MCP still throws `EACCES mkdir /workspace/screenshots` on every navigate/click, but THE ACTION STILL HAPPENS — follow with `browser_snapshot`/`browser_evaluate` to read state (per the US-035 pattern). `browser_take_screenshot` works with a RELATIVE filename.
  - Events fired before consent are intentionally DROPPED (privacy) — so to test `comp_open` I granted consent FIRST (persists in localStorage), THEN navigated to the comp; a mount-tracker won't re-fire after a late consent grant.
  - `next/link` does CLIENT-SIDE navigation, so a global capture array (`window.__events`) survives the nav — that let me assert `builder_open` fired even though the click also navigated to `/builder/[code]`.
  - Keeping `<PageviewTracker>` on `usePathname` (not `useSearchParams`) is what keeps the tier list Static AND avoids a pageview on every filter toggle (those emit `filter_apply` instead).
---

## Tue Jun 30 2026 22:45 - US-042 (Performance e responsividade: imagens + breakpoints)
- Final story. Performance + responsiveness pass over the public surface (tier list, comp, builder).
- Implemented:
  - `next.config.mjs` images tuned for perf on top of the existing CDN whitelist: `formats:['image/avif','image/webp']`, `minimumCacheTTL:2678400` (31d — icons are immutable per set), pinned `imageSizes`/`deviceSizes` (icons are 14–72 CSS px, so no oversized srcset variants).
  - `src/app/(public)/layout.tsx`: added `overflow-x-clip` to `<main>` to kill phantom horizontal scroll from the absolutely-positioned `IconTooltip` near the right edge (comp page @768 was +21px).
- Audit findings before/after:
  - Images were ALREADY well done from earlier stories: 0 raw `<img>` (all `next/image`), lazy-load by default, explicit `sizes` on every `fill` icon. My change adds AVIF/WebP negotiation + long cache. VERIFIED: optimizer returns `Content-Type: image/avif` + `Cache-Control: max-age=2678400` on a real icon (763 bytes).
  - No-hscroll measured at 360/768/1280 on `/`, `/comps/[slug]`, `/builder`: all 0 after the fix (only comp@768 was broken; now 0). Sticky header still pins (headerTop=0 @scrollY600), mobile Filtros drawer (fixed dialog inside main) still opens full-viewport un-clipped.
  - REAL mobile Lighthouse 12 (Playwright chromium, production build): tier list Performance 99, comp page 100 — both CLS 0, FCP <1s, LCP <2s. >= 90 AC met.
- Files changed: next.config.mjs, src/app/(public)/layout.tsx, prd.json (US-042 passes=true), progress.txt.
- Quality: typecheck exit 0, lint clean, 45/45 tests pass, production build succeeds (`/` Static, `/comps/[slug]` SSG, `/builder` Static; ~117–119 kB First Load JS).
- **Learnings for future iterations:**
  - See the "Image perf + no-horizontal-scroll (US-042)" Codebase Pattern above for the full recipe (AVIF/cache config, the `overflow-x-clip` vs `overflow-x-hidden` distinction, and how to run a real mobile Lighthouse in this env against the Playwright chromium + a production build).
  - `overflow-x-clip` (not `-hidden`) is the safe way to contain absolutely-positioned decorations: `clip` leaves `overflow-y:visible` so it never becomes a scroll container that would break `position: sticky`.
  - Lighthouse must run against `npm run build` + `npm start` (NOT `npm run dev`) — dev is unoptimized and would score far below the real number.
  - ALL 42 user stories now pass — project is COMPLETE.
---

## 2026-06-30 - US-043 (Remover filtros da tier list)
- Removed the entire filter bar (tier / sinergia-trait / campeão principal / busca textual) from the public tier list, reverting US-016. The tier list now shows every PUBLISHED comp in the S/A/B/C/X bands with zero filter controls, no query params, and no mobile drawer.
- Files changed:
  - DELETED src/components/tier-list-filters.tsx (the whole "use client" filter component + bottom-sheet drawer).
  - src/app/(public)/page.tsx — dropped the `Suspense` boundary + `TierListFilters` import; renders `<TierBands groups={groupByTier(comps)} currentPatchId/>` directly. Kept the `getTierListData` unstable_cache (tag `tierlist`) and `getSiteConfig()` (still needed for `currentPatchId` badges). Updated the file doc comment.
  - src/components/tier-bands.tsx — removed the now-dead `hideEmpty` prop/branch (only the filter passed `hideEmpty=true`); every band always renders with its US-014 per-band empty state. Updated the doc comment.
  - prd.json (US-043 passes=true + notes), progress.txt.
- Quality: `rm -rf .next` then `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 45/45 pass.
- Browser (Playwright, localhost:3001 — a stale prior-iteration dev server held :3000, fresh dev landed on :3001):
  - DESKTOP 1280x720 — DOM probe: hasFilterButton=false, hasSearchInput=false, hasFilterDialog=false; 5 bands in order S/A/B/C/X (S=N.O.V.A. Blitzcrank NOVO, A=Meeple Jhin ATUALIZADO, B=Dark Star Master Yi, C+X per-band empty states); badges/tier-borders/trait-icons/carries intact; dev overlay "Static route"; no h-scroll (1280==1280).
  - MOBILE 360x720 — no Filtros button, no drawer, 5 bands, no h-scroll (360==360).
  - Only console error is the expected favicon 404.
- **Learnings for future iterations:**
  - Removing `useSearchParams` + `Suspense` KEEPS the page static (confirmed "Static route"). The tier-list page still needs `getSiteConfig()`/`getPublishedComps()` (badges + cards), so those server imports stay — only the client filter layer was removed.
  - The tier-list query (`getPublishedComps` → `CompCard`) did NOT need trimming: its `traits`/`units` are the SAME data `CompCard` renders (carries via `isCarry`, main trait icons). The US-016 filter merely reused the card payload — there was no filter-only query chunk to delete.
  - Left `src/lib/analytics.ts` `ANALYTICS_EVENTS.filterApply` in place: it's a general analytics-event constant (US-041), not one of the "estados/handlers/imports/query chunks" the AC enumerates, and removing it would drag in `analytics.test.ts` — out of scope for this story.
  - `src/components/tier-bands.tsx` is the shared band renderer; after this change its only caller is the server page (no more client filter). If a future story re-introduces filtering, re-add a `hideEmpty` variant rather than forking the component.
---

## Mon Jun 30 2026 23:40 - US-044 (Rework visual do Builder: hexagonos estilo tftacademy)
- Reworked the builder board hexes to the tftacademy visual style (crisper, better-defined hexagons) while keeping the exact 4x7 organization/positions. Inspected the reference https://tftacademy.com/tools/builder/aaakal in-browser FIRST (per AC): its hexes are a two-tone frame — an outer `.hexagon` grey-gradient RIM + an inner dark `.asset-hexagon` WELL, clipped with `polygon(50% 0%, 95% 25%, 95% 75%, 50% 100%, 5% 75%, 5% 25%)` (5%/95% horizontal insets = clean column gaps), no CSS borders/shadows (clip-path clips them).
- Files changed:
  - `src/lib/builder.ts`: added `BUILDER_HEX_CLIP` (5%/95% inset pointy-top hex) — same vertical geometry as the shared `HEX_CLIP`, so the honeycomb tiling math (`w-[13.333%]` + odd-row `pl-[6.667%]` + `marginTop:-2.5%`) is unchanged; only the visible hex is narrower with column gaps.
  - `src/components/builder/builder-board.tsx`: rewrote `Hex` as RIM span (`inset-0`, clipped, state-colored top-lit gradient) + WELL span (`inset-[7.5%]`, clipped, dark `bg-[#0a0e18]`, hover-brightens for empty non-armed hexes) + champion image filling the same well. State rims are full class strings (no interpolation): empty slate-600/70->800/60, armed primary/70->30 (cyan), occupied slate-400/85->600/75, carry amber-300->600, selected cyan brand gradient; selected/carry add a `drop-shadow` glow (the one shadow that respects clip-path). Repositioned stars/carry-badge/items/name overlays to fit the narrower hex. Darkened the board container to a subtle vignette (`bg-gradient-to-b from-[#0c1220] to-[#070b14]`).
- SCOPE / no regression: builder-only. `comp-board.tsx` defines its OWN local `HEX_CLIP` (doesn't import from builder.ts), so the comp-detail board is untouched. The board is presentational — a visual `Hex` change can't affect place/move/remove/stars/items/augments/synergy logic (all in the parent `builder.tsx`).
- Quality: `rm -rf .next`, `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 45/45 pass.
- Browser (Playwright, localhost:3000): EMPTY board = 28 crisp hexes with visible slate rims + dark wells + clean gaps (honeycomb clearly readable); computed rim clip=5%/95%, rim gradient slate-600->800, well bg rgb(10,14,24). END-TO-END no regression: placed 6 champions, swapped 2 (move), removed + undo restored a unit; set Aatrox 3star / Rek'Sai 2star (team value ◆14 respects cost*3^(stars-1)); equipped 2 items (overlay renders on the hex); selected an augment; synergy panel showed 3 active (Bastion 2/4, Vanguard 2/4, Space Groove 1/3). Selected unit shows the cyan brand rim + glow. Share-code URL mirroring still updates on every edit. 0 console errors. No h-scroll at 1280 or 360 (scrollWidth==clientWidth==360, board fits 16-344px). Dark slate + cyan identity preserved.
- **Learnings for future iterations:**
  - On a clip-path hex you CANNOT outline with `ring`/`border`/`box-shadow` (clip cuts them off) — build the "border" from a two-tone layered frame (outer rim span + inset dark well span) and use `filter: drop-shadow(...)` (which follows the clip silhouette) for any glow. See the new "Builder hex visual style (US-044)" Codebase Pattern.
  - Keep hex geometry (positions) and shape (clip) as SEPARATE concerns: narrowing the clip to 5%/95% adds column gaps without touching the tiling percentages, so "same organization" holds while the look changes.
  - When browser-testing the builder: an armed palette champion stays armed after a place, so a subsequent hex click re-places it (replacing the occupant) rather than selecting — disarm first; and arm+place must be SEPARATE `browser_evaluate` calls (React re-render between them).
  - The dev server on :3000 was serving stale `.next` chunks (all 400s → CSS-less page); if the builder renders as an unstyled bullet list, kill the stale `next-server`/`npm run dev` procs and restart after `rm -rf .next`.
---

## 2026-06-30 - US-045 (Login de curador tftlab/tftlab)
- Added a shared, simple EDITOR curator login (tftlab/tftlab) alongside the existing ADMIN email login, and made the login accept a plain-username identifier (not just an e-mail).
- Files changed:
  - `prisma/seed.ts`: new `seedCuratorUser()` — a SEPARATE fn from `seedAdminUser` (never removes/alters the ADMIN). Upserts a role `EDITOR` `User` keyed on the unique `email` column, storing a `bcryptjs` hash. Identifier defaults to `"tftlab"` (trimmed+lowercased into the email column) / password `"tftlab"`, overridable via `CURATOR_EMAIL`/`CURATOR_PASSWORD`. Idempotent (upsert on email). `main()` now runs seedCatalog → seedSampleData → seedAdminUser → seedCuratorUser.
  - `src/auth.ts`: Credentials provider field renamed `email` → `identifier` (`label: "Usuário ou e-mail"`, `type: "text"`) so a non-email username authenticates. `authorize()` still normalizes trim+lowercase and looks the User up by that value in the `email` column, so `admin@metacomps.gg` keeps working (its email is already lowercase).
  - `src/components/admin/login-form.tsx`: input is now `name="identifier" type="text"` (was `type="email"`), label `"Usuário ou e-mail"`, no email-format validation (form already `noValidate`). `signIn("credentials", { identifier, password })`. Error copy → `"Usuário/e-mail ou senha inválidos."`.
  - `.env.example`: documented `CURATOR_EMAIL` / `CURATOR_PASSWORD` (default `tftlab`/`tftlab`).
- Permissions unchanged: `requireRole` default is `EDITOR`, so the curator already has full panel access (same as today's admin) — no permission code touched.
- Quality: `npm run typecheck` exit 0, `npm run lint` "No ESLint warnings or errors", `npm test` 45/45 pass.
- Browser (Playwright, localhost:3000, 1280x720): `/admin/login` shows a `Usuário ou e-mail` TEXT input (accessibility snapshot = `textbox`, not an email field). `tftlab`/`tftlab` → `/admin` Dashboard, header shows `tftlab` + `EDITOR` badge, full nav (Dashboard/Comps/Catálogo/Patches); `/admin/comps` reachable ⇒ EDITOR full access. Logout (`Sair`) → re-login with `admin@metacomps.gg`/`admin1234` → `/admin` Dashboard with `ADMIN` badge ⇒ existing e-mail login intact. 0 console errors on the dashboard.
- **Learnings for future iterations:**
  - The Credentials provider field is now `identifier` (NOT `email`) — any future login/signIn code must post `{ identifier, password }`. The `User` model still has only `email` as its unique column, so a username like `tftlab` is stored there; login normalizes trim+lowercase before `findUnique({ where: { email } })`, so seeded identifiers MUST be stored lowercased or login won't match.
  - The dev server already running on :3000 (started before this change) picked up the auth/form edits via HMR — no restart / `rm -rf .next` needed (no page was moved/deleted this story, so no stale `.next/types`).
  - To browser-verify a login without re-seeding the whole catalog: create just the user with a throwaway repo-root `_x.ts` that does the same `db.user.upsert` (bcryptjs hash), run with `./node_modules/.bin/tsx ./_x.ts`, then `rm` it. A `.mjs` throwaway breaks tsx path resolution (`ERR_MODULE_NOT_FOUND` on `@/` alias) — use `.ts` and import `./src/server/db`.
  - Playwright `browser_navigate`/`browser_click`/`browser_take_screenshot` in this env throw `EACCES: permission denied, mkdir '/workspace/screenshots'` on the wrapper's auto-screenshot, but the underlying action STILL RAN — follow with `browser_snapshot` to confirm the result instead of treating the error as a failure. `browser_take_screenshot` with a RELATIVE `filename` succeeds (lands in repo root; `rm` before staging).
---

## Tue Jun 30 23:53 PDT 2026 - US-046
- Backend do editor de tier list (query + action) — backend-only story.
- QUERY: added `getAdminTierListComps()` to `src/server/queries/admin.ts` → `TierGroup<AdminTierListComp>[]`. `AdminTierListComp` is a lean `Prisma.validator` `select` of `id/name/slug/tier/status`. Returns the current set's comps of ALL statuses (a curator can retier a draft too — `status` rides along so the UI can badge which are live), grouped into the S/A/B/C/X bands by REUSING `groupByTier` from `@/lib/tiers` (the same grouping the public tier list uses; every band present in display order). Set-scoped (fallback all-sets when unconfigured) + NOT `unstable_cache`'d, matching every other admin query (admin shell is force-dynamic).
- ACTION: added `setCompTier(compId, tier)` to `src/actions/comps.ts`. `requireRole("EDITOR")` → validate `tier` with the new `isTier()` guard → `db.comp.update` only `Comp.tier`. When the comp is PUBLISHED, `revalidateTag("tierlist")` + `revalidateTag("comp:"+slug)` (same shape as `updateComp`) so the live tier list re-buckets it immediately (a draft retier touches no public page → no revalidate). Returns `CompActionResult`: `{ ok:false, error }` for an invalid tier ("Tier inválido.") or a missing comp ("Comp não encontrada."). Did NOT touch/reimplement `updateComp`.
- HELPER: added `isTier(value): value is Tier` to `src/lib/tiers.ts` (pure, client-safe — accepts only the S/A/B/C/X strings). Reused by `setCompTier` for its trust-boundary validation.
- TESTS: new `src/lib/tiers.test.ts` (relative-import style like the other tests) covers `isTier` (all 5 bands true; "D"/"s"/"SS"/""/null/undefined/number/object false) and `groupByTier` (5 bands in order even when empty; correct bucketing). `npm test` → 49/49 pass (4 new).
- Files changed: src/lib/tiers.ts, src/lib/tiers.test.ts (new), src/server/queries/admin.ts, src/actions/comps.ts, src/server/CLAUDE.md, src/actions/CLAUDE.md, prd.json, progress.txt.
- Quality: typecheck exit 0, lint "No ESLint warnings or errors", tests 49/49. No browser AC on US-046 (browser verification is US-047's).
- **Learnings for future iterations:**
  - DECISION on AC "reutilizar/ampliar src/server/queries/tierlist.ts": I put the admin query in `admin.ts`, NOT `tierlist.ts`. `tierlist.ts` is documented + used as PUBLISHED-only + `unstable_cache`'d (public pages); an all-statuses, uncached admin read belongs with the other admin queries. The "reuse tierlist" intent is satisfied by reusing `groupByTier` from `@/lib/tiers` — the single source both `tierlist.ts` (which delegates to it) and this admin query share. When an AC names a file but the clean home is elsewhere, reuse the shared helper and document the divergence.
  - `groupByTier<T extends { tier: Tier }>` in `@/lib/tiers` is GENERIC — it groups any row with a `tier` field, so admin/lean projections group through the exact same code as the public `CompCard`s. No need for a tier-list-specific grouper.
  - US-047 (the `/admin/tier-list` UI) consumes `getAdminTierListComps()` for the bands + calls `setCompTier` per retier (optimistic UI), and must add a "Tier list" nav item to `src/components/admin/admin-nav.tsx` + reuse `TIER_META` (colors/labels) from `@/lib/tiers`.
  - Server actions that take an enum arg from the client are a trust boundary — validate at runtime even when the TS param type is the enum (a `"use server"` fn is a public POST). `isTier()` is the reusable guard for `Tier` now; there's a parallel `TIERS` array + `validateInput` inside comps.ts (left untouched to keep the change minimal).
---

## Wed Jul 01 2026 00:15 - US-047 (Editor de tier list dedicado no admin)
- Built the dedicated admin tier-list editor at `/admin/tier-list`.
- **Files changed:**
  - NEW `src/app/admin/(admin)/tier-list/page.tsx` — server component under the `(admin)` group (inherits `force-dynamic` + `requireRole("EDITOR")` from the group layout, so a logged-in curator reaches it and an unauthenticated user is redirected to `/admin/login`). Loads `getAdminTierListComps()` (US-046) → `TierGroup[]`, flattens to a plain `AdminTierListComp[]`, hands it to the client editor. Copy notes guides are still edited per comp in `/admin/comps/[id]`.
  - NEW `src/components/admin/tier-list-editor.tsx` (`"use client"`) — keeps the whole comp list in `useState` (source of truth) and re-derives the S/A/B/C/X bands with the shared `groupByTier` from `@/lib/tiers` (name-sorted per band so a moved card lands stably). Retier control = one button per band per comp (the "controle de seleção" AC option, keyboard-accessible, `aria-pressed`/`aria-label`). OPTIMISTIC: click moves the card to its new band immediately, calls `setCompTier(compId, tier)`, reverts local state + shows a `role="alert"` on `!ok`, shows a `role="status"` notice on `ok` + `router.refresh()` (re-runs the force-dynamic page but does NOT reinit `useState`). Reuses `TIER_META` colors/labels (band chip `bg-tier-*`, `border-l-tier-*` left border, per-band colored picker buttons) + a `STATUS_META` badge map (Rascunho/Publicada/Arquivada). Per-band empty state + global empty state.
  - EDIT `src/components/admin/admin-nav.tsx` — added `{ href: "/admin/tier-list", label: "Tier list" }` between Comps and Catálogo (active-highlight via `usePathname` already handled).
  - EDIT `tailwind.config.ts` — added `./src/lib/**/*` to `content` (see the Codebase Pattern above): the tier chip `bg-tier-*` classes live only as literals in `src/lib/tiers.ts` and were being purged, leaving the chips transparent site-wide. This fix restores the tier chips on the public tier list / comp cards / comp header too.
- **Learnings for future iterations:**
  - The `bg-tier-*` chips were a LATENT, app-wide visual bug (transparent) from US-016 onward — moving `TIER_META` into `src/lib` took its literal class strings out of Tailwind's content scan. Fixed by adding `src/lib` to `content`. This is the reusable pattern I promoted to the top.
  - Optimistic-list admin editors follow the SAME shape as `PatchList`/`CompStatusControls`: local `useState` seeded from server props is the source of truth; `router.refresh()` keeps sibling server reads fresh but never re-seeds client state, so flip state optimistically and revert on `!ok`. `setCompTier` returns the shared `CompActionResult` discriminated union.
  - `import type { AdminTierListComp } from "@/server/queries/admin"` into a `"use client"` component is SAFE (type-only, fully erased — same as the `import type { CompDetail }` pattern in the builder); it does NOT pull Prisma into the client bundle.
  - When a published comp is retiered, `setCompTier` already `revalidateTag("tierlist")`s, so the public tier list re-buckets it — verified: moved a PUBLISHED comp B→C, DB persisted `tier=C` and the comp still rendered on `/`; restored to B.
  - BROWSER-TEST ENV GOTCHAS (this container, 2026-07-01): (1) the Playwright MCP is launched with `--output-dir /workspace/screenshots`, but `/workspace` is a root-owned bind mount node can't write, so EVERY `browser_navigate`/`browser_click` returns an `EACCES: mkdir /workspace/screenshots` error — the action still SUCCEEDS; just call `browser_snapshot` after to read the real state. Screenshots with a RELATIVE `filename` land in the repo root (worked); `rm` them before staging. (2) admin login is identifier/password `tftlab`/`tftlab` (EDITOR curator) at `localhost:3000/admin/login` — there is NO host.docker.internal:3001 flow in this repo. (3) The dev server is a long-running `next dev`; a stale one can serve outdated CSS — after a `tailwind.config`/content change, kill it, `rm -rf .next`, restart, and re-verify (do NOT `pkill -f "next dev"` from a shell whose own command line contains "next dev" — it self-matches and kills the launcher; kill by explicit PID or `pkill -f next-server`).
---
